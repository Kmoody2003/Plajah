rules_version = '2';
service firebase.storage {
  // Storage Rules (Updated)
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
