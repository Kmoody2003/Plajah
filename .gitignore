# Local AI-generated media scratch folders (not part of the app build)
Club Cover Images/
Loera animation/
.claude/launch.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local Firebase emulator / config files
.firebase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code agent worktrees
.claude/worktrees/

tsc_out.txt
public/models/demucs/*.onnx
public/models/demucs/ort/

# Built APKs — large binaries, not source
dist-apk/

dist-tizen/

# Combat Atlas acquisitions — downloaded source archives (~760MB: Beni Hasan
# scans, Met CC0 batch, UMONS-TAICHI mocap zip). Re-fetchable via
# acquisitions/ingest.mjs; destined for Firebase Storage, not git.
# NOTE: the app-served clips in public/motion/ ARE tracked — the Motion Lab
# needs them at runtime.
acquisitions/holdings/
acquisitions/motion/
acquisitions/work/

# Generated preview/scratch + release bundles (not source)
artifacts/
release-production-*.files
release-production-*.patch
release-production-*.stat

# Undo files written by scripts/clearFillerDescriptions.ts — keep them locally, never commit
filler-descriptions-backup-*.json

# Rust OFX shell build output
rust/fabula-ofx/target/
.tela-proofs/

# Fabula FX preview: raw source images (only the curated webps are bundled)
components/plajahPixels/engine/fx/previewAssets/_source/
