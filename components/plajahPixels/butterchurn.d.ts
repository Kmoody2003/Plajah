// butterchurn / butterchurn-presets ship no TypeScript types — declare them so
// the dynamic imports type-check cleanly (we use them via a thin `any` surface).
declare module 'butterchurn';
declare module 'butterchurn-presets';
