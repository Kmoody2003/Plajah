// signatureShaders.ts — the Plajah Pixels Signature Series.
//
// Sixty original audio-reactive shaders in five series, generated from the
// showcase gallery so the two cannot drift apart. Do not hand-edit: regenerate.
//
// Every work is Shadertoy-convention GLSL (`void mainImage(out vec4, in vec2)`)
// and compiles as a drop-in string through the existing ShaderLayer /
// shaderRenderer path — no renderer or engine change is required to run any of
// them. What they add on top of the engine's four coarse scalars is the audio
// kit below.
//
// THE AUDIO KIT is the reusable part. The engine exposes iBass/iMid/iTreble/
// iLevel, and iBass alone spans 0–1.9 kHz — a kick, a bassline and half a vocal
// in one number. `SIGNATURE_KIT` re-reads the 512×2 texture engine/core/
// audioTexture.ts already uploads and resolves six musically-useful channels:
// sub / low / presence / sibilance / air / voice. The voice channel was fitted
// against labelled mixes rather than guessed — it reads roughly four times
// higher with a lead than without.
//
// SIGNATURE_KIT_3D is a second preamble used only by Series V, which raymarches
// real geometry. Series V is deliberately the expensive series.
//
// Two constraints shaped all sixty, both from ShaderLayer being single-pass with
// no feedback buffer:
//   1. There is no frame-to-frame state, so nothing is integrated over time.
//   2. Audio never drives *position* — only amplitude, colour, width and light.
//      A beam that aims on the audio jitters; one that brightens does not.

export const SIGNATURE_KIT = "// ── Plajah Audio Kit v1 ───────────────────────────────────────────────────────\n// Shared preamble for the Signature Series. Reads the 512x2 audio texture the\n// Pixels engine already uploads (row 0 = FFT, row 1 = waveform) and resolves it\n// into six musically-meaningful channels instead of the three coarse bands the\n// engine exposes. 14 texture taps for the bands, 16 more if a work opts into the\n// transient channel — all inside a 4KB texture that lives in L1, and every pixel\n// reads the same few texels, so it stays cheap even on an integrated GPU.\n#define SPEC(F) texture(iChannel0, vec2(F, 0.25)).x\n#define WAVE(F) (texture(iChannel0, vec2(F, 0.75)).x * 2.0 - 1.0)\n\nstruct Aud { float sub; float low; float pres; float sib; float air; float voice; };\n\nAud plajahAudio(){\n  Aud a;\n  a.sub  = (SPEC(0.002) + SPEC(0.008)) * 0.5;                      //   50 - 190 Hz  kick, sub\n  a.low  = (SPEC(0.016) + SPEC(0.034)) * 0.5;                      //  380 - 820 Hz  bass body, low mids\n  float p0=SPEC(0.046), p1=SPEC(0.068), p2=SPEC(0.090),\n        p3=SPEC(0.112), p4=SPEC(0.134), p5=SPEC(0.158);\n  a.pres = (p0+p1+p2+p3+p4+p5) / 6.0;                              //  1.1 - 3.8 kHz presence, formants\n  a.sib  = (SPEC(0.22) + SPEC(0.33)) * 0.5;                        //  5.0 - 8.0 kHz sibilance, snare crack\n  a.air  = (SPEC(0.48) + SPEC(0.72)) * 0.5;                        // 11.0 - 17  kHz hats, air, cymbals\n  // Voice: presence energy that the hats and the cymbal wash cannot account for,\n  // weighted by formant peakiness -- a sung or played lead is spiky across the\n  // presence band while a pad or a wash sits flat, so the weighting keeps the\n  // channel quiet through instrumental passages instead of just tracking mids.\n  // Tuned against labelled mixes; it reads 3-4x higher with a lead than without.\n  float peak    = max(max(max(p0,p1), max(p2,p3)), max(p4,p5));\n  float formant = peak / (a.pres + 0.035);\n  float v = a.pres*1.75 - a.air*0.55 - a.sib*0.22 + a.low*0.10;\n  float raw = clamp(v*2.2, 0.0, 1.0) * (0.12 + 0.88*smoothstep(1.10, 1.58, formant));\n  a.voice = raw*raw*(3.0 - 2.0*raw);                               // pull the floor down\n  return a;\n}\n\n// Crest factor of the current waveform window -- an instantaneous transient\n// reading that needs no history buffer. Opt-in: only shaders that strike on hits\n// pay for it.\nfloat plajahPunch(){\n  float pk = 0.0, e = 0.0;\n  for (int i = 0; i < 16; i++){ float w = WAVE((float(i)+0.5)/16.0); pk = max(pk, abs(w)); e += w*w; }\n  float rms = sqrt(e/16.0);\n  return clamp((pk/(rms+0.04) - 1.35) * 0.7, 0.0, 1.0) * clamp(pk*2.2, 0.0, 1.0);\n}\n\n// Soft-knee compressor. Maps 0->0 and 1->1 but lifts the quiet end, so motion\n// still reads during verses instead of only during drops.\nfloat comp(float x, float k){ return x/(x+k)*(1.0+k); }\nfloat sat(float x){ return clamp(x, 0.0, 1.0); }\n\n// Sin-free hash (Dave Hoskins style) -- transcendentals are the expensive part\n// of the usual Shadertoy hash, and these shaders call it a lot.\nfloat h21(vec2 p){ vec3 q = fract(vec3(p.xyx) * 0.1031); q += dot(q, q.yzx + 33.33); return fract((q.x + q.y) * q.z); }\nfloat h11(float p){ return h21(vec2(p, p*1.7)); }\nfloat vnoise(vec2 p){\n  vec2 i = floor(p), f = fract(p); f = f*f*(3.0-2.0*f);\n  return mix(mix(h21(i), h21(i+vec2(1.0,0.0)), f.x),\n             mix(h21(i+vec2(0.0,1.0)), h21(i+vec2(1.0,1.0)), f.x), f.y);\n}\nfloat fbm(vec2 p, int oct){\n  float s = 0.0, a = 0.5;\n  for (int i = 0; i < 5; i++){ if (i >= oct) break; s += a*vnoise(p); p = p*2.03 + 19.7; a *= 0.5; }\n  return s;\n}\n// Counter-clockwise by +a. Note mat2() takes COLUMNS, so the signs look\n// transposed from the usual textbook matrix — writing it the textbook way gives\n// you a clockwise rotation and a very confusing afternoon.\nmat2 rot(float a){ float c = cos(a), s = sin(a); return mat2(c, s, -s, c); }\n// Resolution-adaptive detail: a 4K TV sheds an octave, a phone keeps them all.\nint plajahOct(){ return iResolution.y > 1250.0 ? 3 : 4; }\n// Hand-mixed 3-stop ramp. Deliberately not the cos-rainbow -- each work carries\n// its own palette.\nvec3 ramp(vec3 a, vec3 b, vec3 c, float t){\n  t = clamp(t, 0.0, 1.0);\n  return t < 0.5 ? mix(a, b, t*2.0) : mix(b, c, t*2.0-1.0);\n}";

export const SIGNATURE_KIT_3D = "// ── Plajah 3D Kit ─────────────────────────────────────────────────────────────\n// Second preamble, used only by Series V. Everything here is standard signed-\n// distance-field raymarching: the scene is a function from a point in space to a\n// distance, and the renderer walks along each ray taking steps no larger than\n// that distance. No feedback buffer is needed for any of it, which is why the\n// series can be fully three-dimensional inside the same single-pass shader the\n// engine already runs.\n//\n// Every work defines `vec2 map(vec3 p)` — distance in x, material id in y — and\n// the helpers below call it through this prototype.\nvec2 map(vec3 p);\n\n// Step budget. Series V is deliberately the expensive series; this is the one\n// dial that decides how expensive.\nint  marchSteps(){ return iResolution.y > 1300.0 ? 72 : 104; }\n\nvec2 raymarch(vec3 ro, vec3 rd, float tmax){\n  float t = 0.02, id = -1.0;\n  int N = marchSteps();\n  for (int i = 0; i < 128; i++){\n    if (i >= N) break;\n    vec2 h = map(ro + rd*t);\n    if (abs(h.x) < 0.0012*t){ id = h.y; break; }      // relax the hit test with distance\n    t += h.x*0.92;\n    if (t > tmax){ id = -1.0; break; }\n  }\n  return vec2(t, id);\n}\n\nvec3 calcNormal(vec3 p){\n  // Tetrahedral differences: four map() calls instead of six.\n  vec2 e = vec2(1.0, -1.0)*0.5773*0.0012;\n  return normalize(e.xyy*map(p + e.xyy).x + e.yyx*map(p + e.yyx).x +\n                   e.yxy*map(p + e.yxy).x + e.xxx*map(p + e.xxx).x);\n}\n\n// Penumbra widens with distance from the occluder, which is what makes a shadow\n// look like light rather than like a stencil.\nfloat softShadow(vec3 ro, vec3 rd, float mint, float maxt, float k){\n  float res = 1.0, t = mint, ph = 1e20;\n  for (int i = 0; i < 40; i++){\n    float h = map(ro + rd*t).x;\n    if (h < 0.0008) return 0.0;\n    float y = h*h/(2.0*ph);\n    res = min(res, k*sqrt(max(h*h - y*y, 0.0))/max(t - y, 1e-4));\n    ph = h; t += clamp(h, 0.012, 0.28);\n    if (t > maxt) break;\n  }\n  return clamp(res, 0.0, 1.0);\n}\n\nfloat calcAO(vec3 p, vec3 n){\n  float occ = 0.0, sca = 1.0;\n  for (int i = 0; i < 5; i++){\n    float h = 0.012 + 0.14*float(i)/4.0;\n    occ += (h - map(p + n*h).x)*sca;\n    sca *= 0.78;\n  }\n  return clamp(1.0 - 2.2*occ, 0.0, 1.0);\n}\n\n// Camera. `roll` banks the whole frame about the view axis.\nmat3 camera(vec3 ro, vec3 ta, float roll){\n  vec3 f = normalize(ta - ro);\n  vec3 up = vec3(sin(roll), cos(roll), 0.0);\n  vec3 r = normalize(cross(f, up));\n  return mat3(r, cross(r, f), f);\n}\n\n// A real bank angle. An aircraft rolls because it is turning: the lift vector\n// has to tilt to supply the lateral acceleration. So sample the flight path\n// either side of now, take the second difference — which IS the acceleration —\n// and roll by its component along the wing. Banking authored by hand always\n// looks like it is lagging; this one cannot, because it is the same quantity.\nfloat autoBank(vec3 pPrev, vec3 pNow, vec3 pNext, float gain){\n  vec3 v = normalize(pNext - pPrev + 1e-5);\n  vec3 acc = pNext - 2.0*pNow + pPrev;\n  vec3 wing = normalize(cross(v, vec3(0.0, 1.0, 0.0)) + 1e-5);\n  return clamp(dot(acc, wing)*gain, -1.15, 1.15);\n}\n\n// ── Distance fields ───────────────────────────────────────────────────────────\nfloat sdSphere(vec3 p, float r){ return length(p) - r; }\nfloat sdBox(vec3 p, vec3 b){ vec3 d = abs(p) - b; return min(max(d.x, max(d.y, d.z)), 0.0) + length(max(d, 0.0)); }\nfloat sdRoundBox(vec3 p, vec3 b, float r){ return sdBox(p, b - r) - r; }\nfloat sdTorus(vec3 p, vec2 t){ return length(vec2(length(p.xz) - t.x, p.y)) - t.y; }\nfloat sdCyl(vec3 p, float r, float h){ vec2 d = abs(vec2(length(p.xz), p.y)) - vec2(r, h); return min(max(d.x,d.y),0.0) + length(max(d,0.0)); }\nfloat sdCapsule(vec3 p, vec3 a, vec3 b, float r){\n  vec3 pa = p - a, ba = b - a;\n  return length(pa - ba*clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0)) - r;\n}\nfloat opSmoothU(float a, float b, float k){\n  float h = clamp(0.5 + 0.5*(b - a)/k, 0.0, 1.0);\n  return mix(b, a, h) - k*h*(1.0 - h);\n}\nvec2 opU(vec2 a, vec2 b){ return a.x < b.x ? a : b; }\nfloat rep1(float p, float c){ return p - c*floor(p/c + 0.5); }        // infinite tiling on one axis\nmat2 r2(float a){ float c = cos(a), s = sin(a); return mat2(c, s, -s, c); }\n\n// ── Noise in three dimensions ─────────────────────────────────────────────────\nfloat h31(vec3 p){ p = fract(p*0.1031); p += dot(p, p.zyx + 31.32); return fract((p.x + p.y)*p.z); }\nfloat vnoise3(vec3 p){\n  vec3 i = floor(p), f = fract(p); f = f*f*(3.0 - 2.0*f);\n  return mix(mix(mix(h31(i + vec3(0,0,0)), h31(i + vec3(1,0,0)), f.x),\n                 mix(h31(i + vec3(0,1,0)), h31(i + vec3(1,1,0)), f.x), f.y),\n             mix(mix(h31(i + vec3(0,0,1)), h31(i + vec3(1,0,1)), f.x),\n                 mix(h31(i + vec3(0,1,1)), h31(i + vec3(1,1,1)), f.x), f.y), f.z);\n}\nfloat fbm3(vec3 p, int oct){\n  float s = 0.0, a = 0.5;\n  for (int i = 0; i < 6; i++){ if (i >= oct) break; s += a*vnoise3(p); p = p*2.03 + 11.7; a *= 0.5; }\n  return s;\n}\n// Project a 2D texture function onto a surface from all three axes and blend by\n// the normal — the standard way to texture an implicit surface that has no UVs.\nfloat triNoise(vec3 p, vec3 n, float sc){\n  vec3 w = pow(abs(n), vec3(4.0)); w /= (w.x + w.y + w.z);\n  return vnoise(p.yz*sc)*w.x + vnoise(p.zx*sc)*w.y + vnoise(p.xy*sc)*w.z;\n}\n\n// ── Shading ───────────────────────────────────────────────────────────────────\nfloat fresnel(float ndv, float f0){ return f0 + (1.0 - f0)*pow(1.0 - ndv, 5.0); }\n// GGX, so highlights have the long tail a real rough surface has instead of the\n// hard disc a Blinn lobe gives.\nfloat ggx(vec3 n, vec3 v, vec3 l, float rough){\n  vec3 h = normalize(v + l);\n  float a = max(rough*rough, 1e-3);\n  float d = max(dot(n, h), 0.0), nl = max(dot(n, l), 0.0), nv = max(dot(n, v), 0.0);\n  float den = d*d*(a*a - 1.0) + 1.0;\n  float D = a*a/(3.14159265*den*den);\n  float k = a*0.5;\n  float G = (nl/(nl*(1.0 - k) + k))*(nv/(nv*(1.0 - k) + k));\n  return D*G/max(4.0*nl*nv, 1e-4)*nl;\n}\nvec3 aces(vec3 x){\n  return clamp((x*(2.51*x + 0.03))/(x*(2.43*x + 0.59) + 0.14), 0.0, 1.0);\n}";

export interface SignatureParam { name: string; def: number; }

export interface SignatureWork {
  /** Stable id — safe to persist in a project file. */
  id: string;
  /** Position in the series, 1–60. */
  n: number;
  name: string;
  /** 'I' … 'V' */
  series: string;
  /** Set id, e.g. 'surge' | 'vespers' | 'stillpoint'. */
  set: string;
  setTitle: string;
  /** Series V works need the 3D preamble as well as the audio kit. */
  kit3d: boolean;
  /** One line on what it is and how it is built. */
  line: string;
  /** Named iParam0..3 controls this work actually reads. */
  params: SignatureParam[];
  /** Which band drives which part of the picture: [channel, what it does]. */
  reacts: [string, string][];
  /** The work itself, without any preamble. */
  body: string;
}

export const SIGNATURE_WORKS: SignatureWork[] = [
  {
    id: "hypergate", n: 1, name: "Hypergate",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "A gate whose wall is the spectrum — twenty panels, each one lit by its own slice of the FFT, so a filter sweep travels around the ring.",
    params: [{"name":"Travel","def":0.45},{"name":"Prism","def":0.35},{"name":"Panel gap","def":0.4},{"name":"Halo","def":0.5}],
    reacts: [["sub","Barrel inflates; ring cadence"],["pres","Panel hue rotation"],["sib","Sparks riding the ring edges"],["air","Struts between panels light up"],["voice","Corkscrews the whole gate and opens the core bloom"],["hit","Strikes the nearest ring only"]],
    body: "// HYPERGATE — the wall of the tunnel IS the spectrum: twenty panels around the\n// ring, each one lit by its own slice of the FFT, so a filter sweep visibly\n// travels around the gate.\nvec3 gateInk(float t){ return ramp(vec3(0.16,0.24,0.95), vec3(0.95,0.16,0.62), vec3(1.00,0.80,0.34), t); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float r0 = length(uv);\n\n  // Sub-bass inflates the barrel. Squared falloff so the centre stays stable and\n  // only the rim breathes — a full-frame zoom would read as a cheap bounce.\n  uv *= 1.0 + (0.05 + comp(a.sub, 0.30)*0.22) * r0*r0;\n\n  float rad = max(length(uv), 1e-3);\n  float ang = atan(uv.y, uv.x);\n  float depth = 0.34/rad;                                   // tunnel coordinate\n  float z = iTime * (0.55 + iParam0*1.7);\n\n  // Vocals twist the gate along its length — the further down the tunnel, the\n  // more shear, so a held note visibly corkscrews the architecture.\n  float sa = ang + (0.20 + a.voice*1.90) * depth * 0.24 + iTime*0.04;\n\n  float segs = 20.0;\n  float s  = (sa/6.28318 + 0.5) * segs;\n  float si = floor(s), sf = fract(s);\n  float bin = fract(si/segs);\n  float energy = SPEC(pow(bin, 2.0)*0.68 + 0.004);\n  float gap  = smoothstep(0.02, 0.16 + iParam2*0.22, min(sf, 1.0-sf));\n  float rail = smoothstep(0.045, 0.0, min(sf, 1.0-sf));      // the strut between panels\n\n  // Rings, sampled once per channel with a small depth offset: prismatic\n  // fringing on the leading edge, which is what sells the speed.\n  float ca = (0.004 + iParam1*0.014 + pu*0.010);\n  float f0 = fract(depth*3.0 + z),        f1 = fract(depth*3.0 + z + ca), f2 = fract(depth*3.0 + z + ca*2.0);\n  vec3 ring = vec3(pow(1.0-abs(f0*2.0-1.0), 5.0), pow(1.0-abs(f1*2.0-1.0), 5.0), pow(1.0-abs(f2*2.0-1.0), 5.0));\n\n  vec3 tint = gateInk(bin*0.9 + a.pres*0.18);\n  vec3 col  = ring * gap * (0.07 + energy*2.6) * tint;\n  col += rail * ring.g * (0.05 + a.air*1.1) * vec3(0.70,0.82,1.00);\n\n  // Structure fades toward the mouth of the gate so the corners never smear.\n  col *= 1.0 - smoothstep(0.30, 1.00, r0)*0.92;\n  col *= smoothstep(0.0, 0.10, r0);\n\n  // Vocal halo: a lens bloom that only the lead line can open.\n  col += gateInk(0.74) * (0.04 + a.voice*1.45) * exp(-r0*(9.5 - a.voice*5.0)) * (0.6 + iParam3);\n\n  // Sibilance throws sparks that ride the ring edges.\n  col += step(0.986 - a.sib*0.060, h21(vec2(si, floor(depth*3.0 + z)))) * ring.g * a.sib * 2.6 * vec3(1.0,0.94,0.88);\n\n  // Transients strike the nearest rings only, never the whole frame.\n  col += pu * ring.b * smoothstep(0.10, 0.42, r0) * 0.60 * vec3(0.75,0.86,1.0);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.72);   // filmic rolloff keeps hue at full tilt\n  o = vec4(pow(col, vec3(0.84)), 1.0);\n}",
  },
  {
    id: "riot-bloom", n: 2, name: "Riot Bloom",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "A seven-fold kaleidoscopic fold drawn as line work. Each fold level answers to a different octave, so the flower opens from the inside out as the mix brightens.",
    params: [{"name":"Rate","def":0.3},{"name":"Petal spread","def":0.35},{"name":"Line weight","def":0.4}],
    reacts: [["sub","Pulls the frame in on the kick"],["low","Fold scale and frame rotation"],["pres","Opens the petals; palette drift"],["air","Softens the line into filigree"],["voice","The only thing that blooms the centre"],["hit","Rings a shockwave outward"]],
    body: "// RIOT BLOOM — a seven-fold kaleidoscopic IFS drawn as line work, not as fill.\n// Each fold level is lit by a different octave of the spectrum and contributes\n// less than the one before it, so the flower keeps a legible centre instead of\n// blooming into a wall of colour.\nvec3 riotInk(float t){ return ramp(vec3(0.05,0.35,0.85), vec3(0.98,0.15,0.42), vec3(1.00,0.86,0.30), t); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  vec2 p  = (C - 0.5*iResolution.xy)/iResolution.y;\n  float r0 = length(p);\n  float t = iTime * (0.22 + iParam0*0.75);\n\n  p *= 1.20 - comp(a.sub, 0.32)*0.24;               // the kick pulls the frame in\n  p  = rot(t*0.28 + a.low*0.50) * p;\n\n  vec3 col = vec3(0.012,0.014,0.030);\n  float sc = 1.0, wgt = 1.0;\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    p  = abs(p) - (0.34 + 0.08*sin(t*0.6 + fi*1.7) + a.pres*(0.09 + iParam1*0.18));\n    p  = rot(0.56 + 0.11*sin(t*0.37 + fi) + a.voice*0.50) * p;\n    float k = 1.26 + a.low*0.18;\n    p *= k; sc *= k;\n    float d = (length(p) - 0.52)/sc;\n    float w = exp(-abs(d) * (140.0 + 110.0*iParam2 - a.air*45.0));\n    col += w * wgt * riotInk(fi*0.15 + t*0.05 + a.pres*0.30) * (0.35 + SPEC(0.010 + fi*0.085)*2.1);\n    wgt *= 0.72;                                     // deeper folds recede\n  }\n\n  // The vocal is the only thing that can bloom the centre.\n  col += a.voice*a.voice * 1.45 * riotInk(0.50) * exp(-r0*2.6);\n  // Transients ring a shockwave outward instead of flashing the whole frame.\n  col += pu * 0.95 * exp(-abs(r0 - pu*0.70)*18.0) * vec3(1.0,0.93,0.86);\n  // Sibilance dusts the filigree that is already lit.\n  col += a.sib * 0.6 * pow(max(col.g, 0.0), 3.0) * vec3(0.85,0.95,1.00);\n  col *= 1.0 - smoothstep(0.45, 1.05, r0)*0.7;\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.72);   // filmic rolloff keeps hue at full tilt\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "laser-cathedral", n: 3, name: "Laser Cathedral",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "Ten heads on a truss, angled back across the room so the beams cross into a nave. Position is on a slow mechanical sweep; the audio owns everything else.",
    params: [{"name":"Sweep","def":0.4},{"name":"Splay","def":0.35},{"name":"Blinders","def":0.35}],
    reacts: [["sub","Beam width and the weight of the fog"],["low","Lifts the haze off the deck"],["pres","Outer heads carry the top of the mix"],["sib","Dust catching in the beams"],["voice","Splays the fan wide open"],["hit","Blinder strobe across the truss"]],
    body: "// LASER CATHEDRAL — ten heads spread along a truss, angled inward so the beams\n// cross into a nave instead of fanning from one point. Head position is on a slow\n// mechanical sweep (never on the audio, or it jitters); the audio owns width,\n// colour, haze and the blinders.\nvec3 beamInk(float t){ return ramp(vec3(0.22,0.55,1.00), vec3(0.62,0.28,1.00), vec3(1.00,0.30,0.50), t); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float sweep = sin(iTime*0.29) * (0.22 + iParam0*0.42);\n  float deck = -0.40;\n\n  vec3 col = vec3(0.010,0.012,0.026);\n  for (int i = 0; i < 10; i++){\n    float k = (float(i) - 4.5)/4.5;                          // -1 .. 1 along the truss\n    vec2 src = vec2(k*0.72, 0.40);\n    vec2 d = uv - src;\n    float dist = length(d);\n    float ang  = atan(d.x, -d.y);\n    // Angled back across the room, so left heads throw right and vice versa.\n    float aa = -k*(0.78 + 0.34*sin(iTime*0.21 + k*1.7)) + sweep*(1.0 - abs(k))*1.1\n             + a.voice*k*(0.30 + iParam1*0.45);\n    float e = SPEC(0.006 + abs(k)*0.32);\n    float w = 0.0035 + 0.010*e + comp(a.sub,0.28)*0.010;\n    float beam = w / (abs(ang - aa)*dist*2.6 + w);           // 1/r falloff reads volumetric\n    beam *= smoothstep(1.85, 0.02, dist) * step(uv.y, src.y) * step(abs(ang), 1.35);\n    vec3 ink = beamInk(0.04 + abs(k)*0.58);\n    col += beam * ink * (0.14 + e*2.1);\n    // The pool the beam lays on the deck, and the head can flare.\n    float hit = src.x + tan(clamp(aa, -1.25, 1.25))*(src.y - deck);\n    col += exp(-abs(uv.x - hit)*(30.0 - e*10.0)) * exp(-abs(uv.y - deck)*22.0) * (0.02 + e*0.22) * ink;\n    col += exp(-length(uv - src)*90.0) * (0.15 + e*1.1) * ink;\n  }\n\n  // Haze deck. The low end is what you feel in the floor, so it lifts the fog.\n  col += vec3(0.06,0.07,0.16) * smoothstep(0.75,-0.55, uv.y) * (0.30 + a.low*1.3);\n  col += vec3(0.09,0.11,0.20) * exp(-abs(uv.y - deck)*8.0) * (0.22 + a.low*0.9);\n\n  // Sibilance catches on airborne dust rather than brightening the beams.\n  col += step(0.9975 - a.sib*0.004, h21(floor(uv*120.0) + floor(iTime*7.0))) * a.sib * 1.8 * vec3(0.9,0.95,1.0);\n  // Blinder strobe on the transient, weighted to the truss.\n  col += pu * (0.06 + iParam2*0.38) * smoothstep(-0.30, 0.70, uv.y) * vec3(0.85,0.90,1.0);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.72);   // filmic rolloff keeps hue at full tilt\n  o = vec4(pow(col, vec3(0.86)), 1.0);\n}",
  },
  {
    id: "black-sun", n: 4, name: "Black Sun",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "An eclipse with a corona of curl noise in log-polar space, so the plasma climbs outward the way real plasma does instead of scrolling past.",
    params: [{"name":"Disc size","def":0.35},{"name":"Climb","def":0.4}],
    reacts: [["sub","The disc breathes on the kick"],["low","Turbulence in the corona"],["pres","Chromosphere rim and corona heat"],["air","Ember field drifting off the limb"],["voice","Anamorphic flare across the frame; corona turns gold"],["hit","Snaps a thin shock off the limb"]],
    body: "// BLACK SUN — an eclipse whose corona is a curl of fbm in log-polar space, so the\n// flame climbs outward the way real plasma does instead of scrolling sideways.\nvec3 coronaInk(float t){ return ramp(vec3(0.75,0.10,0.03), vec3(1.00,0.48,0.06), vec3(1.00,0.93,0.72), t); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  int  OCT = plajahOct();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float r  = length(uv);\n  float th = atan(uv.y, uv.x);\n\n  float R = (0.27 + iParam0*0.16) + comp(a.sub, 0.35)*0.045;   // the disc breathes on the kick\n\n  // Log-polar domain warp: constant detail at every radius, motion always radial.\n  vec2 q = vec2(th*1.45, log(max(r, 1e-3)) - iTime*(0.06 + iParam1*0.16));\n  float warp = fbm(q*1.15 + iTime*0.04, 3);\n  float w    = fbm(q*2.5 + warp*(1.3 + a.low*1.1), OCT);\n\n  float flame = pow(max(w,0.0), 1.65) * exp(-(r - R)*(3.4 - a.low*1.3));\n  flame *= smoothstep(R - 0.004, R + 0.035, r);\n  vec3 col = flame * coronaInk(w*0.85 + a.pres*0.30) * (0.85 + a.pres*1.5);\n\n  // Chromosphere rim.\n  col += smoothstep(0.011, 0.0, abs(r - R)) * (0.35 + a.pres*2.1) * vec3(1.0,0.88,0.70);\n\n  // Air band is the ember field — sparse, drifting, never a uniform sparkle.\n  vec2 eg = floor(uv*46.0 + vec2(0.0, iTime*0.9));\n  float ember = step(0.992 - a.air*0.010, h21(eg));\n  col += ember * a.air * 1.7 * coronaInk(0.75) * smoothstep(R*0.9, R*3.0, r);\n\n  // Vocals cut an anamorphic flare across the frame and push the corona to gold.\n  float flare = a.voice * exp(-abs(uv.y)*(110.0 - a.voice*55.0)) * exp(-abs(uv.x)*0.9);\n  col += flare * vec3(1.00,0.87,0.62) * 1.7;\n  col += a.voice * 0.55 * vec3(1.00,0.90,0.66) * exp(-abs(r - R - 0.02)*11.0);\n\n  // Transient snaps a thin white shock off the limb.\n  col += pu * 0.7 * exp(-abs(r - R - pu*0.28)*30.0) * vec3(1.0,0.96,0.90);\n\n  col *= 1.0 - smoothstep(R + 0.02, R - 0.02, r);           // the disc stays truly black\n  o = vec4(pow(max(col,0.0), vec3(0.90)), 1.0);\n}",
  },
  {
    id: "liquid-chrome", n: 5, name: "Liquid Chrome",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "Seven metaballs with an analytic dome normal, lit through a matcap of a dark studio and three strip lights. Chrome only reads as chrome when most of what it reflects is black.",
    params: [{"name":"Flow","def":0.4},{"name":"Mass","def":0.4},{"name":"Film","def":0.35}],
    reacts: [["sub","Throws the masses apart"],["low","Bounce light off the floor"],["pres","Strip lights brighten in the reflection"],["sib","Capillary ripple on the skin"],["voice","Flushes the metal iridescent and sends a ring across the pool"],["hit","A clean specular pop"]],
    body: "// LIQUID CHROME — seven metaballs summed into a height field. The gradient falls\n// out of the same loop analytically, so the normal is exact and free, and the\n// surface is lit through a matcap of a dark studio with three strip lights.\n// Chrome only reads as chrome when most of what it reflects is black.\nvec3 studioCap(vec2 m, float pres, float voice){\n  float y = m.y;\n  vec3 c = mix(vec3(0.020,0.022,0.032), vec3(0.075,0.082,0.105), smoothstep(-1.0, 1.0, y));\n  c += exp(-abs(y - 0.46)* 7.0) * vec3(1.00,0.24,0.44) * (0.45 + pres*1.7);   // key strip, high\n  c += exp(-abs(y + 0.52)* 9.0) * vec3(0.16,0.78,1.00) * (0.28 + pres*0.9);   // fill strip, low\n  c += exp(-abs(y + 0.90)*16.0) * vec3(1.00,0.62,0.18) * (0.18 + voice*2.2);  // floor bounce\n  c += exp(-abs(m.x - 0.62)*8.0) * vec3(0.45,0.62,1.00) * (0.12 + pres*0.35); // a wall, off to one side\n  c += smoothstep(0.88, 1.00, y) * vec3(0.94,0.96,1.00) * 0.85;               // ceiling softbox\n  vec3 n = vec3(m, sqrt(max(1.0 - dot(m,m), 0.0)));\n  c += pow(max(dot(n, normalize(vec3(-0.40,0.62,0.68))), 0.0), 70.0) * vec3(1.0,0.99,0.96) * 3.0;\n  c += pow(max(dot(n, normalize(vec3( 0.55,0.20,0.80))), 0.0), 26.0) * vec3(0.55,0.75,1.0) * 0.5;\n  return c;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime * (0.35 + iParam0*0.8);\n\n  float f = 0.0; vec2 g = vec2(0.0);\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    vec2 c = 0.30*vec2(cos(t*(0.31 + fi*0.07) + fi*2.4), sin(t*(0.26 + fi*0.09) + fi*1.6));\n    c *= 1.0 + comp(a.sub, 0.32)*0.26;                       // the kick throws them apart\n    float rr = 0.115 + 0.035*sin(t*0.7 + fi) + SPEC(0.010 + fi*0.055)*(0.06 + iParam1*0.08);\n    vec2 dp = uv - c;\n    float x  = dot(dp,dp)/(rr*rr);\n    float k1 = 1.0/(1.0 + x*x);          // bounded: 1 at the centre, smooth everywhere\n    f += k1;\n    g += dp * (-4.0*x*k1*k1/(rr*rr));\n  }\n\n  float edge = smoothstep(0.62, 0.76, f);\n  // Sibilance is capillary ripple on the skin: high frequency, low amplitude.\n  float rip = sin(length(uv)*115.0 - iTime*7.0) * a.sib * 0.09;\n  // Dome normal: outward from the core, tilting to grazing at the rim.\n  vec2  outward = normalize(-g + vec2(1e-6));\n  float rimness = smoothstep(1.20, 0.64, f);\n  vec2  m = outward * sin(clamp(rimness + rip, 0.0, 1.0)*1.5708);\n  m = clamp(m, vec2(-0.999), vec2(0.999));\n  vec3  n = vec3(m, sqrt(max(1.0 - dot(m,m), 0.0)));\n\n  vec3 col = studioCap(m, a.pres, a.voice) * edge;\n  // Rim: the bead of light that always sits on the silhouette of poured metal.\n  col += smoothstep(0.86, 0.72, f) * edge * (0.5 + a.pres) * vec3(0.70,0.82,1.00) * 0.55;\n\n  // Vocals flush the chrome iridescent: a thin-film shift keyed to the lead only.\n  float fres = pow(1.0 - abs(n.z), 3.0);\n  col += edge * a.voice*a.voice * 0.30 * (0.5 + 0.5*cos(fres*(4.0 + iParam2*7.0) + a.voice*5.0 + vec3(0.0,2.2,4.1)));\n  col += edge * a.voice * 0.6 * exp(-abs(length(uv) - fract(iTime*0.30)*1.3)*26.0) * vec3(1.0,0.92,0.80);\n\n  // Off the metal: a dark floor lit only by the bounce off the pool.\n  col += (1.0 - edge) * vec3(0.013,0.015,0.024) * (1.0 + a.low*1.4);\n  col += (1.0 - edge) * smoothstep(0.30, 0.72, f) * (0.16 + a.pres*0.35) * vec3(1.00,0.34,0.52);\n  col += (1.0 - edge) * smoothstep(0.05, 0.50, f) * 0.045 * vec3(0.30,0.60,1.00);\n\n  col += pu * edge * 0.22 * vec3(1.0);\n  o = vec4(pow(max(col,0.0), vec3(0.90)), 1.0);\n}",
  },
  {
    id: "shatter-prism", n: 6, name: "Shatter Prism",
    series: "I", set: "surge", setTitle: "Surge",
    kit3d: false,
    line: "The frame breaks into shards and every shard owns one FFT bin — so a snare lights a scattered set of facets and a bass note lights a completely different set.",
    params: [{"name":"Shard count","def":0.35},{"name":"Blast","def":0.3},{"name":"Fracture","def":0.35}],
    reacts: [["low","Fills the unlit shards with a floor glow"],["pres","Rotates which hue each shard takes"],["sib","Dispersion along the fracture lines"],["voice","A wavefront lighting shards by position, not by frequency"],["hit","Blows the mosaic apart from the centre"]],
    body: "// SHATTER PRISM — the frame breaks into Voronoi shards. Every shard owns one FFT\n// bin, so the mix is legible as a mosaic: a snare lights a scattered set of\n// shards, a bass note lights a different set entirely. Unlit shards stay black,\n// which is what keeps the mosaic reading as light rather than as wallpaper.\nvec3 prismInk(float h){\n  h = fract(h);\n  if (h < 0.20) return vec3(0.10,0.78,1.00);   // cyan\n  if (h < 0.40) return vec3(0.32,0.34,1.00);   // ultramarine\n  if (h < 0.60) return vec3(1.00,0.22,0.62);   // magenta\n  if (h < 0.80) return vec3(1.00,0.70,0.18);   // amber\n  return                vec3(0.55,1.00,0.52);  // acid green\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud  a  = plajahAudio();\n  float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  vec2 p  = rot(iTime*0.05) * uv * (2.6 + iParam0*4.5);\n\n  vec2 gi = floor(p), gf = fract(p);\n  float d1 = 9.0, d2 = 9.0, id = 0.0;\n  for (int y = -1; y <= 1; y++){\n    for (int x = -1; x <= 1; x++){\n      vec2 off = vec2(float(x), float(y));\n      float hh = h21(gi + off);\n      vec2 site = off + 0.5 + 0.42*vec2(cos(hh*23.1 + iTime*0.55), sin(hh*17.7 + iTime*0.44));\n      // Transients blow the shards apart from the centre of the frame.\n      site += normalize(gi + off + 0.001) * pu * (0.10 + iParam1*0.30);\n      float d = length(site - gf);\n      if (d < d1){ d2 = d1; d1 = d; id = hh; }\n      else if (d < d2){ d2 = d; }\n    }\n  }\n\n  float energy = SPEC(pow(id, 1.8)*0.72 + 0.004);\n  float crack  = smoothstep(0.0, 0.045 + iParam2*0.09, d2 - d1);\n  // Facet shading — each shard is a slightly tilted plane catching the light.\n  float facet  = 0.70 + 0.30*cos(id*31.0 + d1*3.2);\n\n  vec3 col = prismInk(id + a.pres*0.10) * pow(energy, 1.5) * 4.4 * crack * facet;\n  // Dispersion along the fracture lines — the prism part of the name.\n  float seam = pow(1.0 - crack, 2.0);\n  col += seam * (0.03 + a.sib*1.5) * mix(vec3(1.0), 0.5 + 0.5*cos(id*20.0 + vec3(0.0,2.1,4.2)), 0.75);\n\n  // Vocals sweep a wavefront across the mosaic, lighting shards by position\n  // rather than by frequency — a completely different read to the FFT fill.\n  float sweep = exp(-abs(fract(id*3.0 + uv.x*0.35 + 0.5) - fract(iTime*0.18))*9.0);\n  col += a.voice * sweep * 1.7 * vec3(1.00,0.86,0.55) * crack;\n\n  col += a.low * 0.07 * prismInk(0.10) * crack;\n  col *= 1.0 - smoothstep(0.55, 1.15, length(uv))*0.5;\n  col = max(col, 0.0); col = col/(1.0 + col*0.72);   // filmic rolloff keeps hue at full tilt\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "valley-wind", n: 7, name: "Valley Wind",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Cel-painted hill bands with a sunlit ridge and cumulus lit hard on top, blue underneath. Nothing in the frame moves faster than wind.",
    params: [{"name":"Wind","def":0.35},{"name":"Sun height","def":0.4},{"name":"Shafts","def":0.45}],
    reacts: [["sub","Part of the gust that leans the valley"],["low","The gust itself; cloud drift"],["pres","Warms the cloud tops; wingbeat of the flight"],["air","Grass catching light across the near field"],["voice","Opens a shaft through the deck and lifts seed heads off the grass"]],
    body: "// VALLEY WIND — the first of the two landscape works. Painted, not rendered:\n// flat cel hill bands with a sunlit ridge, cumulus with a hard bright top and a\n// cool blue underside, and nothing in the frame that moves faster than wind.\nfloat ridge(float x, float f, float off, float amp, float base){\n  return base + amp*(fbm(vec2(x*f + off, off*0.7), 3) - 0.5);\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime * (0.35 + iParam0*0.9);\n\n  // A gust, not a pulse: the low end drives one slow-moving air mass, so the\n  // whole valley leans instead of throbbing.\n  float gust = comp(a.low, 0.30)*0.6 + comp(a.sub, 0.40)*0.4;\n\n  vec3 col = ramp(vec3(0.86,0.92,0.95), vec3(0.47,0.71,0.90), vec3(0.13,0.38,0.75), smoothstep(-0.16, 0.52, p.y));\n  vec2 sun = vec2(0.46 + iParam1*0.36, 0.34);\n  col += vec3(1.00,0.95,0.76) * exp(-length(p - sun)*3.6) * 0.55;\n  col += vec3(1.00,0.97,0.85) * exp(-length(p - sun)*11.0) * 0.9;\n\n  // Cumulus. Two evaluations, one a step higher, give the vertical gradient a cel\n  // cloud needs: bright where the field falls away above, blue underneath.\n  vec2 cp = vec2(p.x*1.05 + t*0.013 + gust*0.045, (p.y - 0.12)*2.5);\n  float cl  = fbm(cp*1.35, OCT);\n  float clU = fbm(cp*1.35 + vec2(0.0, 0.26), OCT);\n  float th  = 0.545 + smoothstep(0.0, 0.48, p.y)*0.028;\n  float cov = smoothstep(th, th + 0.040, cl) * smoothstep(-0.02, 0.14, p.y);\n  float lit = smoothstep(0.0, 0.050, cl - clU);\n  vec3 cloudCol = mix(vec3(0.56,0.66,0.82), vec3(1.00,0.99,0.97), lit);\n  cloudCol = mix(cloudCol, vec3(1.00,0.95,0.84), a.pres*0.40);\n  cloudCol += smoothstep(0.60, 0.0, length(p - sun)) * 0.18 * vec3(1.0,0.92,0.75);\n  col = mix(col, cloudCol, cov*0.97);\n\n  // Rays through the deck — vocals only. Masked to the sky, so the land stays flat.\n  vec2 sd = p - sun;\n  float rays = fbm(vec2(atan(sd.y, sd.x)*1.7, t*0.05), 3);\n  float shaft = smoothstep(0.34, 0.86, rays) * exp(-length(sd)*0.85);\n\n  // Three hill bands, far to near, each with its own haze and its own light.\n  float land = 0.0, treeBase = 0.0;\n  vec3 skyAtHorizon = ramp(vec3(0.86,0.92,0.95), vec3(0.47,0.71,0.90), vec3(0.13,0.38,0.75), 0.10);\n  for (int i = 0; i < 3; i++){\n    float fi = float(i);\n    float hy = ridge(p.x, 3.0 - fi*0.85, 4.0 + fi*11.0, 0.09 + fi*0.075, -0.030 - fi*0.155);\n    if (i == 1) treeBase = hy;\n    hy += gust*0.006*(fi + 1.0);\n    float m = smoothstep(hy + 0.0025, hy - 0.0025, p.y);\n    vec3 hcol = ramp(vec3(0.44,0.60,0.68), vec3(0.26,0.52,0.36), vec3(0.06,0.22,0.15), fi/2.0);\n    // Aerial perspective: the far bands are mostly sky, which is what gives the\n    // valley its depth. Each band also darkens toward its own foot.\n    hcol = mix(skyAtHorizon, hcol, 0.58 + fi*0.21);\n    hcol *= 1.0 - smoothstep(hy, hy - 0.34, p.y)*0.26*(fi/2.0);\n    col = mix(col, hcol, m);\n    col += smoothstep(0.010, 0.0, abs(p.y - hy)) * m * mix(vec3(0.80,0.90,0.72), vec3(0.62,0.88,0.36), fi/2.0) * (0.30 + a.pres*0.55);\n    // Grass on the nearest band only: the air band is the shimmer, so hats and\n    // cymbals read as wind crossing the field.\n    if (i == 2){\n      float bl = sin(p.x*165.0 + fbm(p*6.0 + t*0.20, 2)*10.0 + t*1.5)*0.5 + 0.5;\n      col += m * pow(bl, 7.0) * a.air * 0.70 * vec3(0.78,1.00,0.52);\n    }\n    land = max(land, m);\n  }\n\n  col += shaft * (1.0 - land*0.75) * a.voice * (1.2 + iParam2*1.4) * vec3(1.00,0.94,0.72);\n\n  // One tree on the middle ridge. A valley needs something to be a valley around.\n  vec2 tq = p - vec2(-0.38, treeBase);\n  float trunk = smoothstep(0.006, 0.002, abs(tq.x)) * step(0.0, tq.y) * step(tq.y, 0.075);\n  float crown = smoothstep(0.062, 0.052, length((tq - vec2(0.0, 0.098))*vec2(1.0, 1.35))\n                                        + 0.018*fbm(vec2(atan(tq.y - 0.098, tq.x)*2.5, 0.0), 2));\n  float tree = max(trunk, crown) * step(p.y, treeBase + 0.175);\n  col = mix(col, vec3(0.07,0.19,0.13), tree*0.92);\n  col += crown * smoothstep(0.0, 0.05, tq.x) * (0.10 + a.pres*0.30) * vec3(0.55,0.80,0.32);\n\n  // Seed heads lifting off the grass when the lead comes in.\n  vec2 pg = p*72.0 + vec2(t*0.26, t*0.44);\n  vec2 pi = floor(pg), pf = fract(pg) - 0.5;\n  float sp = smoothstep(0.17, 0.02, length(pf - (vec2(h21(pi + 5.0), h21(pi + 9.0)) - 0.5)*0.55));\n  col += sp * step(0.974, h21(pi)) * (0.25 + a.voice*1.5) * vec3(1.00,0.98,0.82) * smoothstep(0.26, -0.36, p.y);\n\n  // A flight crossing, wingbeat on the presence band.\n  for (int b = 0; b < 5; b++){\n    float fb = float(b);\n    vec2 bp = vec2(fract(t*0.028 + fb*0.075)*2.3 - 1.15, 0.24 + 0.05*sin(t*0.5 + fb*1.3) + fb*0.014);\n    vec2 q = p - bp;\n    float flap = 0.42 + 0.38*sin(t*7.0 + fb*1.1 + a.pres*6.0);\n    q.x = abs(q.x);\n    float wing = smoothstep(0.0045, 0.0, abs(q.y - q.x*flap)) * step(q.x, 0.020);\n    col = mix(col, vec3(0.13,0.19,0.25), wing*0.9*(1.0 - land));\n  }\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "cloud-sea", n: 8, name: "Cloud Sea",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Above the deck at the last of the light. The cumulus sea is in true perspective and hazes into the horizon, so the frame holds real distance rather than a gradient.",
    params: [{"name":"Drift","def":0.35},{"name":"Golden hour","def":0.45},{"name":"Spirit light","def":0.45}],
    reacts: [["sub","The swell moving through the sea"],["pres","Lights the rim of the far island"],["sib","Glints on the cloud tops near the sun"],["air","Twinkles the high stars"],["voice","Turns the hour from blue dusk to gold and draws one arc across the sky"]],
    body: "// CLOUD SEA — the second landscape: above the deck at the last of the light. The\n// cumulus sea is drawn in true perspective (world width scales with depth) and\n// hazes into the horizon, so the frame holds real distance rather than a gradient.\nvec3 duskSky(float y, float warm){\n  vec3 lowStop = mix(vec3(0.98,0.66,0.40), vec3(1.00,0.84,0.56), warm);\n  return ramp(lowStop, vec3(0.42,0.40,0.66), vec3(0.04,0.06,0.19), smoothstep(-0.04, 0.56, y));\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime * (0.30 + iParam0*0.8);\n  float hz = 0.02;\n  vec2 sun = vec2(-0.36, hz + 0.03);\n\n  // Vocals turn the hour: blue dusk toward gold as the lead comes in.\n  float warm = a.voice * (0.7 + iParam1*0.6);\n  vec3 col = duskSky(p.y, warm);\n  col += vec3(1.00,0.76,0.42) * exp(-length((p - sun)*vec2(0.85,1.9))*3.4) * (0.45 + warm*0.8);\n  col += vec3(1.00,0.90,0.70) * exp(-length((p - sun)*vec2(0.85,1.9))*16.0) * 0.8;\n\n  // High stars, only where the sky is already dark. The air band twinkles them.\n  vec2 sg = floor(p*72.0);\n  col += step(0.9955, h21(sg)) * (0.35 + a.air*2.2*h21(sg + floor(t*3.0)))\n       * smoothstep(0.14, 0.55, p.y) * vec3(0.85,0.90,1.00);\n\n  // A far island, floating, with a lit rim on the sun side.\n  vec2 ip = (p - vec2(0.46, hz + 0.105)) * vec2(1.0, 2.3);\n  float prof = 0.135 + 0.030*fbm(vec2(atan(ip.y, ip.x)*2.2 + 3.0, 0.0), 3) - 0.016*ip.x;\n  float isl = smoothstep(prof + 0.003, prof - 0.003, length(ip));\n  col = mix(col, mix(vec3(0.05,0.06,0.13), vec3(0.15,0.12,0.21), 0.5 + 0.5*ip.y), isl);\n  col += smoothstep(0.007, 0.0, abs(length(ip) - prof)) * smoothstep(0.3, -0.6, ip.x)\n       * (0.30 + a.pres*1.6) * vec3(1.00,0.74,0.44);\n\n  if (p.y < hz){\n    float depth = 0.30/(hz - p.y + 0.016);                  // near the bottom is close\n    vec2 cp = vec2(p.x*depth*1.5, depth*1.10 + t*(0.10 + comp(a.sub,0.35)*0.10));\n    float c   = fbm(cp*1.75, OCT);\n    float cU  = fbm(cp*1.75 + vec2(0.0, 0.30), OCT);\n    float grp = fbm(cp*0.42 + 9.0, 2);                       // which parts of the sea have cloud\n    float top = smoothstep(0.0, 0.055, c - cU);\n    float cov = smoothstep(0.435, 0.520, c*0.55 + grp*0.60);\n\n    vec3 gapCol  = mix(vec3(0.11,0.13,0.28), vec3(0.34,0.24,0.38), smoothstep(0.30, 0.60, c));\n    vec3 deckCol = mix(vec3(0.66,0.57,0.68), vec3(1.00,0.94,0.82), top);\n    deckCol = mix(deckCol, vec3(1.00,0.84,0.58), smoothstep(1.2, 0.0, abs(p.x - sun.x))*0.55);\n    vec3 deckPx = mix(gapCol, deckCol, cov);\n    deckPx += top*cov * a.sib * 1.3 * vec3(1.00,0.92,0.74) * smoothstep(0.8, 0.0, abs(p.x - sun.x));\n    // Aerial perspective: the sea dissolves into the sky at the horizon.\n    float haze = smoothstep(0.0, 0.30, (hz - p.y)*2.4);\n    col = mix(mix(duskSky(hz, warm), vec3(1.00,0.80,0.56), 0.28), deckPx, haze);\n  }\n\n  // The vocal is a spirit-light: one arc, drawn only while the lead is singing.\n  float ph = fract(t*0.055);\n  vec2 cm = vec2(mix(-0.95, 0.95, ph), 0.46 - 0.60*ph*ph);\n  col += exp(-length((p - cm)*vec2(0.85, 2.4))*(9.0 - a.voice*3.0)) * a.voice * (2.2 + iParam2*2.0)\n       * vec3(1.00,0.94,0.78);\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "lily-water", n: 9, name: "Lily Water",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Impressionist. Every pixel belongs to a brushstroke: flat colour per stroke, a third of it broken toward an unmixed pigment, and the light falling from the upper left.",
    params: [{"name":"Tempo","def":0.3},{"name":"Brush size","def":0.35},{"name":"Ripple","def":0.4}],
    reacts: [["sub","The water swells and the flow field warps"],["low","Direction of the brushwork"],["pres","Breaks more colour into each stroke"],["air","Impasto edge where the bristle lifts"],["voice","A ripple that turns the strokes to follow it and opens the blossoms"]],
    body: "// LILY WATER — impressionist. Every pixel belongs to a brushstroke: the frame is\n// cut into elongated cells along a slow flow field, each cell takes ONE flat\n// colour sampled at its own centre, and a third of that colour is broken toward\n// an unmixed accent. Flat colour per cell plus broken colour is what makes paint\n// read as paint rather than as a gradient.\nvec3 pondInk(float v){\n  v = clamp(v, 0.0, 1.0);\n  if (v < 0.22) return mix(vec3(0.030,0.075,0.150), vec3(0.045,0.230,0.245), v/0.22);\n  if (v < 0.44) return mix(vec3(0.045,0.230,0.245), vec3(0.145,0.400,0.480), (v-0.22)/0.22);\n  if (v < 0.64) return mix(vec3(0.145,0.400,0.480), vec3(0.480,0.470,0.680), (v-0.44)/0.20);\n  if (v < 0.82) return mix(vec3(0.480,0.470,0.680), vec3(0.840,0.680,0.740), (v-0.64)/0.18);\n  return              mix(vec3(0.840,0.680,0.740), vec3(1.000,0.930,0.780), (v-0.82)/0.18);\n}\nfloat pondValue(vec2 q, float t, int oct){\n  float w    = fbm(q*1.15 + vec2(t*0.20, -t*0.08), oct);\n  // Broad bands of reflected sky lying flat on the water.\n  float sky  = smoothstep(0.30, 0.85, 0.5 + 0.5*sin(q.y*2.6 + w*3.0 + t*0.20));\n  // The light falls from the upper left, so the far water is deep and the near\n  // water carries the reflection. Without this the frame has no composition.\n  float fall = smoothstep(0.55, -0.45, q.y - q.x*0.35);\n  float v = mix(w*1.55 - 0.30, 0.30 + sky*0.72, 0.52);\n  return clamp(v*(0.55 + fall*0.80), 0.0, 1.0);\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.16 + iParam0*0.34);\n\n  // The water swells on the low end — the flow field warps, so the brushwork\n  // itself moves rather than the colours strobing.\n  float swell = comp(a.sub, 0.34)*0.55 + comp(a.low, 0.30)*0.45;\n  vec2 wp = p + vec2(0.0, swell*0.035*sin(p.x*3.4 + t*0.9));\n\n  // A ripple expands whenever there is a voice in the mix; strokes on its\n  // circumference turn to follow it.\n  float rr   = fract(t*0.22)*1.15;\n  float ring = exp(-abs(length(wp) - rr)*7.0) * a.voice;\n  float ang  = (fbm(wp*0.95 + t*0.12, 3) - 0.5)*2.2 + 0.30*sin(wp.x*1.7 + t*0.4) + swell*0.5;\n  ang = mix(ang, atan(wp.y, wp.x) + 1.5708, ring*0.85);\n\n  vec3 col = vec3(0.0);\n  for (int L = 0; L < 2; L++){\n    float fl   = float(L);\n    float sz   = (0.058 - fl*0.028) * (1.0 + iParam1*1.2);\n    float a2   = ang + fl*0.9;\n    mat2  M    = rot(a2);\n    vec2  cell = vec2(sz*4.6, sz);\n    vec2  sp   = (M*wp)/cell;\n    float row = floor(sp.y);\n    float shift = 0.5*row + 0.31*sin(row*2.7) + 0.21*sin(row*11.3);   // stagger, or the cells read as plaid\n    sp.x += shift;\n    vec2  si = floor(sp), sf = fract(sp) - 0.5;\n    vec2  cc = rot(-a2) * ((si + vec2(0.5 - shift, 0.5)) * cell);\n\n    float jit = h21(si + fl*37.0);\n    float v   = pondValue(cc, t, OCT) + ring*0.32;\n    v = clamp((v - 0.5)*1.22 + 0.49 + (jit - 0.5)*(0.10 + a.pres*0.12), 0.0, 1.0);\n    vec3  sc  = pondInk(v);\n\n    // Broken colour: a third of the strokes carry an unmixed pigment straight\n    // from the palette. This is the whole trick of the style.\n    float j2 = h21(si + 71.0 + fl*13.0);\n    vec3 accent = j2 < 0.34 ? vec3(0.93,0.55,0.55)\n                : j2 < 0.67 ? vec3(0.52,0.72,0.30)\n                            : vec3(0.55,0.46,0.86);\n    sc = mix(sc, accent, 0.05 + 0.17*h21(si + 3.0) + a.pres*0.10);\n\n    // Loaded-brush edge: brighter where the bristle lifts off the canvas.\n    float body = smoothstep(0.52, 0.30, max(abs(sf.x)*0.80, abs(sf.y)));\n    sc += smoothstep(0.30, 0.50, abs(sf.y)) * body * (0.10 + a.air*0.60) * vec3(1.00,0.97,0.88);\n    sc -= smoothstep(0.44, 0.52, abs(sf.y)) * 0.10;\n\n    vec3 under = pondInk(pondValue(wp*0.9, t, 2));\n    col = (L == 0) ? mix(under, sc, 0.25 + body*0.75)\n                   : mix(col, sc, body * (0.55 + 0.35*jit));\n  }\n\n  // Lily pads: flat cadmium-green discs with a cool crescent shadow.\n  vec2 lg = wp*2.3;\n  vec2 li = floor(lg), lf = fract(lg) - 0.5;\n  float lh = h21(li);\n  vec2  lc = lf - (vec2(h21(li + 3.0), h21(li + 7.0)) - 0.5)*0.5;\n  float pang = atan(lc.y, lc.x);\n  float pd   = length(lc*vec2(1.0, 1.55));\n  float pedge = 0.275 + 0.022*sin(pang*7.0 + lh*40.0);                  // an irregular rim\n  float pad  = smoothstep(pedge, pedge - 0.045, pd) * step(0.60, lh);\n  pad *= smoothstep(0.10, 0.20, abs(mod(pang - lh*6.28 + 3.14159, 6.28318) - 3.14159)); // the wedge notch\n  vec3 padCol = mix(vec3(0.11,0.31,0.15), vec3(0.56,0.71,0.25), 0.5 + 0.5*lc.y);\n  padCol = mix(padCol, vec3(0.36,0.52,0.28), h21(li + 13.0)*0.55);      // no two pads the same green\n  col = mix(col, padCol, pad*0.86);\n  col += smoothstep(pedge - 0.055, pedge - 0.015, pd) * pad * (0.14 + a.air*0.35) * vec3(0.82,1.00,0.58);\n  col -= pad * smoothstep(0.0, 0.28, lc.y) * 0.06;\n\n  // Blossoms — rose madder and cadmium, opening with the lead voice.\n  float bl = smoothstep(0.085, 0.050, length(lc*vec2(1.0,1.2))) * step(0.87, lh);\n  col = mix(col, mix(vec3(0.97,0.66,0.72), vec3(1.00,0.94,0.70), 0.5 + 0.5*sin(lh*30.0)),\n            bl*(0.45 + a.voice*0.55));\n\n  col += ring * (0.45 + iParam2*0.70) * vec3(0.99,0.74,0.66);\n  col += a.voice * 0.10 * vec3(0.95,0.72,0.60);      // the whole pond warms while the lead holds\n  col *= 0.962 + 0.038*h21(floor(C*0.5));                     // canvas tooth\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "dream-meridian", n: 10, name: "Dream Meridian",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Surrealist. An impossible plane, five suspended bodies, two suns and a doorway with nothing behind it. Each body is tuned to a different octave.",
    params: [{"name":"Drift","def":0.4},{"name":"Column","def":0.4}],
    reacts: [["sub","Raises the horizon; the plane breathes"],["low","Stretches the long shadows"],["pres","Sky hue and the second sun"],["sib","Fine dust in the low light"],["air","The star field"],["voice","Aligns the five bodies and raises a column of light behind the horizon"]],
    body: "// DREAM MERIDIAN — surrealist. An impossible plane, five suspended bodies and a\n// second sun. Each body is tuned to a different octave of the spectrum, so the\n// mix arrives as five separate lights in one still room.\nvec3 bodyField(vec2 q, float t, float voice, out float cov){\n  cov = 0.0;\n  vec3 acc = vec3(0.0);\n  for (int i = 0; i < 5; i++){\n    float fi = float(i);\n    float e  = SPEC(0.006 + fi*0.075);\n    vec2  c  = vec2(-0.62 + fi*0.31, 0.10 + 0.075*sin(t*0.33 + fi*1.9) + fi*0.018);\n    c.y = mix(c.y, 0.16, voice*0.75);                       // the lead aligns them\n    float r  = 0.062 + fi*0.010 + e*0.028;\n    vec2  d  = q - c;\n    float sd = length(d) - r;\n    float m  = smoothstep(0.004, -0.004, sd);\n    // Lit from the low sun on the left, with a cool terminator.\n    float lam = clamp(dot(normalize(vec3(d/r, sqrt(max(1.0 - dot(d,d)/(r*r), 0.0)))), normalize(vec3(-0.65,0.30,0.62))), 0.0, 1.0);\n    vec3 col = mix(vec3(0.16,0.15,0.22), vec3(0.94,0.86,0.78), pow(lam, 0.8));\n    col += vec3(1.00,0.72,0.42) * pow(lam, 6.0) * 0.5;\n    col  = mix(col, ramp(vec3(0.30,0.55,1.00), vec3(0.85,0.45,1.00), vec3(1.00,0.80,0.45), fi*0.25), e*0.75);\n    col += e*e*1.1 * ramp(vec3(0.30,0.55,1.00), vec3(0.85,0.45,1.00), vec3(1.00,0.80,0.45), fi*0.25);\n    acc = mix(acc, col, m);\n    cov = max(cov, m);\n    acc += exp(-max(sd,0.0)*26.0) * e * 0.6 * vec3(0.55,0.60,1.00);   // halo\n  }\n  return acc;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.4 + iParam0*0.8);\n  float hz = -0.06 + comp(a.sub,0.40)*0.010;\n\n  vec3 col = ramp(vec3(0.90,0.62,0.52), vec3(0.42,0.36,0.58), vec3(0.07,0.08,0.20), smoothstep(-0.05, 0.55, p.y));\n  col = mix(col, mix(col, vec3(0.98,0.78,0.60), 0.30), a.pres*0.6);\n\n  // Two suns: one setting, one impossible.\n  col += vec3(1.00,0.74,0.45)*exp(-length((p - vec2(-0.72, hz+0.05))*vec2(1.0,1.5))*5.0)*0.9;\n  col += vec3(0.70,0.80,1.00)*exp(-length(p - vec2(0.58, 0.34))*11.0)*(0.35 + a.pres*0.9);\n\n  vec2 sg = floor(p*64.0);\n  col += step(0.9965, h21(sg)) * (0.3 + a.air*2.0) * smoothstep(0.05,0.5,p.y) * vec3(0.9,0.92,1.0);\n\n  // The monolith — a doorway with nothing behind it.\n  float door = step(abs(p.x - 0.70), 0.052) * step(hz, p.y) * step(p.y, 0.30);\n  col = mix(col, vec3(0.05,0.05,0.09), door*0.95);\n  col += smoothstep(0.004,0.0,abs(abs(p.x-0.70)-0.052))*step(hz,p.y)*step(p.y,0.30)*(0.25+a.pres*1.1)*vec3(1.00,0.80,0.55);\n\n  if (p.y < hz){\n    float depth = 0.10/(hz - p.y + 0.011);\n    vec2 gp = vec2(p.x*depth, depth - t*0.06);\n    float marble = fbm(gp*0.9, OCT);\n    vec3 ground = mix(vec3(0.10,0.10,0.16), vec3(0.34,0.30,0.36), marble);\n    ground = mix(ground, vec3(0.55,0.42,0.40), smoothstep(2.5, 0.2, depth)*0.5);\n    // Mirrored bodies, squashed and smeared by the ripple in the plane.\n    float cov2;\n    vec2 mq = vec2(p.x, hz + (hz - p.y)*2.4 + 0.02*sin(p.x*9.0 - t*1.2)*(0.4 + a.low));\n    vec3 refl = bodyField(mq, t, a.voice, cov2);\n    ground = mix(ground, refl*0.45, cov2*smoothstep(0.35, 0.0, hz - p.y)*0.85);\n    // Long shadows, and the low end stretches them.\n    ground *= 1.0 - 0.45*smoothstep(0.20, 0.0, abs(p.x + 0.10 - (hz - p.y)*(2.4 + a.low*2.5)));\n    // Haze the plane into the sky at the horizon, or the seam reads as a cut.\n    col = mix(col, ground, smoothstep(0.0, 0.09, hz - p.y));\n  }\n\n  float cov;\n  vec3 bodies = bodyField(p, t, a.voice, cov);\n  col = mix(col + bodies*0.0, bodies, cov);\n  col += bodies * (1.0 - cov) * 0.55;\n\n  // Vocals raise a column of light behind the horizon.\n  col += a.voice * (1.4 + iParam1*1.6) * exp(-abs(p.x - 0.06)*(16.0 - a.voice*6.0))\n       * smoothstep(-0.30, 0.70, p.y) * vec3(1.00,0.92,0.76) * 0.55;\n\n  // Sibilance is fine dust in the low light.\n  col += step(0.9975 - a.sib*0.004, h21(floor(p*120.0 + vec2(0.0, t*2.0)))) * a.sib * 1.2;\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "still-aurora", n: 11, name: "Still Aurora",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Four curtains over a mirror lake, each one wired to a different channel so they never move together. The fifth is the voice.",
    params: [{"name":"Rate","def":0.35},{"name":"Voice curtain","def":0.35},{"name":"Mist","def":0.35}],
    reacts: [["sub","Swell in the lake, and the reflection with it"],["low","The green curtain"],["pres","The teal curtain; snowline on the peaks"],["sib","The blue curtain"],["air","The violet curtain; stars"],["voice","A fifth curtain — narrow, gold-green, and it only exists while someone sings"]],
    body: "// STILL AURORA — four curtains over a mirror lake, each wired to a different\n// channel so they never move together. The fifth is the voice: narrow, gold-green\n// and it only exists while someone is singing.\nfloat curtain(vec2 q, float seed, float t, float lean){\n  float x = q.x*1.15 + seed;\n  float d = fbm(vec2(x*0.85, t*0.06 + seed), 2);\n  float base = -0.05 + d*0.32 + lean;\n  float y = q.y - base;\n  if (y < 0.0) return 0.0;\n  float w = 0.13 + 0.09*sin(x*1.5 + seed*4.0 + d*3.2);\n  float v = smoothstep(0.0, 0.045, y) * exp(-y/max(w, 0.045));\n  // Striations, irregular and fading with height so the tops feather out.\n  float st = 0.62 + 0.26*sin(x*17.0 + d*15.0 + t*0.35) + 0.18*sin(x*41.0 + d*6.0 - t*0.5);\n  v *= mix(1.0, clamp(st, 0.0, 1.4), exp(-y*2.2));\n  // Whole stretches of the curtain are simply absent.\n  v *= smoothstep(0.18, 0.62, 0.5 + 0.5*sin(x*0.62 + seed*2.0 + d*4.5));\n  return v;\n}\nvec3 auroraAt(vec2 q, float t, Aud a, float boost){\n  vec3 c = vec3(0.0);\n  c += curtain(q,  0.0, t, 0.00) * (0.09 + comp(a.low, 0.30)*1.10) * vec3(0.16,1.00,0.52);\n  c += curtain(q, 11.3, t, 0.07) * (0.07 + a.pres*1.30)            * vec3(0.28,0.95,0.85);\n  c += curtain(q, 23.7, t, 0.13) * (0.04 + a.sib *1.50)            * vec3(0.42,0.58,1.00);\n  c += curtain(q, 37.1, t, 0.20) * (0.03 + a.air *1.65)            * vec3(0.85,0.42,0.95);\n  c += curtain(q*vec2(2.6,1.0), 51.9, t*1.4, 0.10) * a.voice * (2.6 + boost) * vec3(1.00,0.92,0.42);\n  return c;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.5 + iParam0*1.0);\n  float hz = -0.16;\n\n  vec3 col = ramp(vec3(0.045,0.085,0.190), vec3(0.020,0.038,0.100), vec3(0.006,0.010,0.038),\n                  smoothstep(-0.12, 0.55, p.y));\n\n  vec2 sg = floor(p*78.0);\n  float sh = h21(sg);\n  col += step(0.9945, sh) * (0.35 + a.air*1.9*h21(sg + floor(t*2.5)))\n       * vec3(0.88,0.92,1.00) * smoothstep(hz, 0.35, p.y);\n\n  col += auroraAt(p, t, a, iParam1*2.5) * smoothstep(hz - 0.02, hz + 0.06, p.y);\n\n  // Mountains, sitting on the waterline.\n  float m = hz + 0.075 + 0.085*fbm(vec2(p.x*1.8 + 5.0, 0.0), 3);\n  float mm = smoothstep(m + 0.002, m - 0.002, p.y) * step(hz, p.y);\n  col = mix(col, mix(vec3(0.010,0.016,0.038), vec3(0.030,0.046,0.085), smoothstep(hz, m, p.y)), mm);\n  col += smoothstep(0.005, 0.0, abs(p.y - m)) * step(hz, p.y) * (0.10 + a.pres*0.45) * vec3(0.32,0.78,0.62);\n\n  if (p.y < hz){\n    // The lake. Sub-bass is a swell, so the reflection breathes rather than shakes.\n    float wob = 0.018*sin(p.x*7.0 - t*0.8) * (0.35 + comp(a.sub, 0.35)*1.1);\n    vec2 mq = vec2(p.x, hz + (hz - p.y)*1.30 + wob);\n    float fade = smoothstep(0.45, 0.0, hz - p.y);\n    vec3 refl = auroraAt(mq, t, a, iParam1*2.5)*0.50;\n    // The mountains reflect too, or the waterline looks pasted on.\n    float mr = smoothstep(m + 0.003, m - 0.003, hz + (hz - p.y)*1.30 + wob);\n    refl = mix(refl, vec3(0.008,0.012,0.028), mr*0.85);\n    col = mix(vec3(0.008,0.011,0.026), refl, fade*0.92) + vec3(0.012,0.018,0.040)*(1.0 - fade);\n    col += exp(-abs(p.y - hz)*80.0) * 0.20 * vec3(0.50,0.85,0.88);        // the line of light\n  }\n\n  // A cold mist sitting on the surface.\n  col += exp(-abs(p.y - hz)*24.0) * (0.05 + a.low*0.18) * vec3(0.28,0.55,0.62) * (0.6 + iParam2);\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "suminagashi", n: 12, name: "Suminagashi",
    series: "I", set: "drift", setTitle: "Drift",
    kit3d: false,
    line: "Floating ink, and the one work in Series I on a light ground. Three drops on paper, combed by the low end, drawn with real weight variation rather than as a contour plot.",
    params: [{"name":"Tempo","def":0.3},{"name":"Scale","def":0.35},{"name":"Ink","def":0.4}],
    reacts: [["sub","The sheet breathes"],["low","Combs the ink across the paper"],["pres","Tightens the ring spacing"],["sib","Crackles the paper fibre"],["air","Silver catching on the ink edges"],["voice","A single gold thread tracing one isoline"]],
    body: "// SUMINAGASHI — floating ink, and the one work in the set on a light ground. Real\n// suminagashi is concentric: ink dropped in a few places, then combed. So the\n// field here is the distance to the nearest drop, warped by noise, which makes\n// the isolines nest around each drop and collide along a natural seam.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.25 + iParam0*0.6);\n\n  vec2 q = p*(1.30 + iParam1*1.5);\n  q *= 1.0 - comp(a.sub, 0.40)*0.055;                        // the sheet breathes\n  q = rot(t*0.012) * q;\n\n  // The comb. Low mids steer it, so a bassline slowly drags the ink.\n  vec2 drag = vec2(fbm(q*0.70 + t*0.020, 3), fbm(q*0.70 + 41.0 - t*0.018, 3)) - 0.5;\n  q += drag * (0.75 + comp(a.low, 0.30)*0.85);\n\n  // Three drops. min() of the distances gives nested rings with a merge seam.\n  vec2 c1 = vec2(-0.52 + 0.05*sin(t*0.11), 0.16 + 0.04*cos(t*0.09));\n  vec2 c2 = vec2( 0.46 + 0.05*cos(t*0.08), -0.22 + 0.04*sin(t*0.13));\n  vec2 c3 = vec2( 0.02 + 0.06*sin(t*0.07), 0.44 + 0.03*cos(t*0.10));\n  float dmin = min(min(length(q - c1), length(q - c2)*1.08), length(q - c3)*1.22);\n  float w = dmin*1.70 + fbm(q*0.95 + vec2(t*0.010, 0.0), OCT)*0.42;\n\n  float freq = 8.0 + a.pres*7.0;                           // presence tightens the rings\n  float ph = w*freq + t*0.20;\n  // The screen-space derivative keeps every line one width no matter how the\n  // field is stretched; a slow second field makes some passes heavy, others hairline.\n  float grad = max(abs(dFdx(ph)) + abs(dFdy(ph)), 1e-4);\n  float load = 0.30 + 1.60*fbm(q*0.42 + 7.0, 2);\n  float ink  = 1.0 - smoothstep(0.0, grad*load*2.6, abs(sin(ph)));\n\n  vec3 paper = vec3(0.962, 0.948, 0.918);\n  paper -= h21(floor(C*0.5))*0.026;                          // laid grain\n  paper -= smoothstep(0.30, 1.05, length(p))*0.055;          // the shadowed edge of the sheet\n\n  // A wash bleeding out of the heavier passes, laid down before the line goes on.\n  float wash = smoothstep(1.15, 0.05, dmin);\n  vec3 col = mix(paper, mix(vec3(0.60,0.66,0.79), vec3(0.79,0.75,0.70), 0.5 + 0.5*sin(w*5.0)), wash*0.40);\n\n  vec3 inkCol = mix(vec3(0.050,0.075,0.180), vec3(0.25,0.19,0.31), smoothstep(0.20, 1.10, dmin));\n  inkCol = mix(inkCol, vec3(0.09,0.25,0.31), a.pres*0.35);\n  col = mix(col, inkCol, ink*(0.84 + iParam2*0.16));\n\n  // Air is a silver shimmer catching only on the ink edges.\n  col += ink*(1.0 - ink)*4.0 * a.air * 0.18 * vec3(0.80,0.88,1.00);\n  // Sibilance crackles the paper fibre.\n  col -= step(0.994 - a.sib*0.010, h21(floor(C*0.34) + floor(t*6.0))) * a.sib * 0.20;\n\n  // The gold thread: one isoline of the same field, drawn only for the voice.\n  float target = 0.62 + 0.16*sin(t*0.09);\n  float dphi = max(abs(dFdx(w)) + abs(dFdy(w)), 1e-5);\n  float thread = 1.0 - smoothstep(0.0, dphi*4.5, abs(w - target));\n  col = mix(col, vec3(0.78,0.60,0.18), clamp(thread*a.voice*1.7, 0.0, 1.0));\n  col += thread * a.voice * 0.55 * vec3(1.00,0.88,0.52);\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "kanagawa", n: 13, name: "Kanagawa",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "Ukiyo-e. A steep hollow face, a lip thrown clear of it, foam that forks into claws, and a keyblock line on every edge — because a woodblock print is a drawing before it is a picture.",
    params: [{"name":"Swell","def":0.35},{"name":"Lip reach","def":0.4},{"name":"Foam","def":0.35}],
    reacts: [["sub","Raises the wave and the far sea with it"],["low","Reach of the overhanging lip"],["pres","Where the foam threshold falls, so how much claws"],["sib","Spray thrown off the crest; streaks down the face"],["voice","Lays gold leaf into the sky, the way a deluxe printing would"],["hit","Throws the spray further"]],
    body: "// KANAGAWA — ukiyo-e. Built the way the print is drawn rather than the way water\n// works: a steep concave face, a lip thrown clear of it, a hollow behind the lip,\n// and foam that forks into claws. Flat Prussian blue over cream paper, and a\n// keyblock line on every edge because a woodblock print is a drawing first.\nfloat clawTex(vec2 q, float t, int oct){\n  return fbm(q*vec2(2.6, 5.5) + vec2(t*0.10, 0.0), oct)*0.70\n       + fbm(q*vec2(6.5, 13.0) - vec2(t*0.17, 0.0), 2)*0.30;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.25 + iParam0*0.6);\n\n  vec3 CREAM = vec3(0.930,0.892,0.802), PRUSSIAN = vec3(0.045,0.115,0.270),\n       MID   = vec3(0.120,0.290,0.460), PALE = vec3(0.520,0.660,0.740),\n       FOAM  = vec3(0.980,0.972,0.950), SLATE = vec3(0.44,0.49,0.55),\n       VERM  = vec3(0.720,0.200,0.140), LINE = vec3(0.030,0.070,0.170);\n\n  // Sky: flat cream, one printed grey band, one red disc.\n  vec3 col = mix(CREAM, CREAM*0.94, smoothstep(0.02, 0.48, p.y));\n  col = mix(col, mix(CREAM, SLATE, 0.42),\n            smoothstep(0.56, 0.61, fbm(vec2(p.x*1.5 + t*0.02, p.y*3.2), 3)) * smoothstep(0.02, 0.32, p.y)*0.55);\n  col = mix(col, VERM, smoothstep(0.046, 0.040, length(p - vec2(-0.66, 0.34))));\n\n  // Fuji, small and far.\n  float fx = abs(p.x + 0.34);\n  float fuji = step(fx*1.75, 0.145 - (p.y + 0.07)) * step(-0.07, p.y);\n  col = mix(col, mix(PRUSSIAN, SLATE, 0.28), fuji*0.95);\n  col = mix(col, FOAM, fuji*smoothstep(0.042, 0.066, p.y - fx*1.4)*0.92);\n\n  // Far sea.\n  float sea0 = -0.20 + 0.022*(fbm(vec2(p.x*3.0 + t*0.20, 4.0), 3) - 0.5) + comp(a.sub,0.32)*0.012;\n  float far = smoothstep(sea0 + 0.004, sea0 - 0.004, p.y);\n  col = mix(col, PALE, far);\n  col = mix(col, FOAM, far*smoothstep(0.013, 0.002, abs(p.y - sea0))*(0.5 + a.sib*0.5));\n\n  // ── The great wave ────────────────────────────────────────────────────────\n  float cx = 0.30, cy = 0.34 + comp(a.sub, 0.30)*0.06;\n  // Back slope away to the right; a steep, hollow face to the left of the crest.\n  // The exponent below 1 is what makes the face fall away fast and then flatten,\n  // which is the shape of the wall in the print.\n  float back = cy - 0.55*max(0.0, p.x - cx);\n  float face = cy - 1.55*pow(max(0.0, cx - p.x), 0.72);\n  float crest = (p.x > cx ? back : face) + 0.020*(fbm(vec2(p.x*7.0 + t*0.3, 1.0), 3) - 0.5);\n  float body = smoothstep(crest + 0.004, crest - 0.004, p.y);\n\n  // The lip: an arc thrown clear of the face, curling left and down.\n  vec2 lc = vec2(cx - 0.285, cy - 0.135);\n  vec2 lq = (p - lc)*vec2(1.0, 1.30);\n  float lr = length(lq), la = atan(lq.y, lq.x);\n  float LR = 0.225 + comp(a.low, 0.30)*0.024 + iParam1*0.06;\n  float arc = smoothstep(0.052, 0.030, abs(lr - LR)) * smoothstep(-2.55, -2.30, la) * smoothstep(1.05, 0.80, la);\n  // The tongue thins as it reaches: a lip that ends in a point, not a sausage.\n  arc *= smoothstep(1.05, 0.10, la)*0.6 + 0.4;\n  body = max(body, arc);\n  // The hollow behind the lip stays sky.\n  float hollow = smoothstep(LR - 0.055, LR - 0.075, lr) * smoothstep(-2.40, -2.20, la) * smoothstep(1.00, 0.70, la);\n  body *= 1.0 - hollow;\n  body = max(body, far*0.0);\n\n  // A second, smaller wave in front, so the frame is not one shape.\n  float cx2 = -0.42, cy2 = 0.02 + comp(a.sub,0.30)*0.03;\n  float crest2 = (p.x > cx2 ? cy2 - 0.62*(p.x - cx2) : cy2 - 1.35*pow(max(0.0, cx2 - p.x), 0.75))\n               + 0.014*(fbm(vec2(p.x*11.0 + t*0.4, 6.0), 3) - 0.5);\n  float body2 = smoothstep(crest2 + 0.003, crest2 - 0.003, p.y);\n\n  // Two flat blues and a lighter plane, as a two-block print would have it.\n  float depth = smoothstep(cy, cy - 0.85, p.y);\n  vec3 sea = mix(PRUSSIAN, MID, depth);\n  sea = mix(sea, PALE, smoothstep(0.06, 0.0, abs(p.y - crest))*0.55);\n  col = mix(col, mix(MID, PRUSSIAN, smoothstep(cy2, cy2 - 0.5, p.y)), body2*0.96);\n  col = mix(col, FOAM, body2 * smoothstep(0.030, 0.002, abs(p.y - crest2)) * (0.5 + a.sib*0.5));\n  col = mix(col, sea, body*0.98);\n\n  // ── Foam ──────────────────────────────────────────────────────────────────\n  // Along the crest, in the hollow, and on the lip. Fingers, not fuzz.\n  float claw = clawTex(vec2(p.x*3.6, p.y*3.6), t, OCT);\n  float thr = 0.400 + 0.085*(1.0 - a.pres) - iParam2*0.10;\n  float crestBand = smoothstep(0.155, 0.0, abs(p.y - crest));\n  float lipBand = smoothstep(0.095, 0.0, abs(lr - LR)) * smoothstep(-2.60, -2.30, la) * smoothstep(1.10, 0.80, la);\n  // Foam is allowed above the crest line: that is the entire point of a claw.\n  float reach = smoothstep(0.14, 0.0, max(0.0, p.y - crest)) * smoothstep(-0.35, 0.02, p.y - crest + 0.35);\n  float foam = step(thr, claw) * max(crestBand*max(body, reach), lipBand);\n  // Streaks running down the face — the marks that give the wall its scale.\n  float streak = smoothstep(0.60, 0.88, fbm(vec2(p.x*22.0 + p.y*3.0, p.y*4.0 - t*0.4), 2));\n  foam = max(foam, streak * body * smoothstep(0.42, 0.02, cy - p.y) * (0.35 + a.sib*0.55));\n  col = mix(col, FOAM, clamp(foam*1.6, 0.0, 1.0));\n\n  // Keyblock lines.\n  col = mix(col, LINE, smoothstep(0.006, 0.0, abs(p.y - crest)) * step(-0.95, p.x) * 0.9);\n  col = mix(col, LINE, smoothstep(0.005, 0.0, abs(lr - LR)) * smoothstep(-2.55, -2.35, la) * smoothstep(1.05, 0.85, la) * 0.8);\n\n  // Spray off the crest; sibilance seeds it and a transient throws it further.\n  vec2 sg = floor((p - vec2(cx, cy))*(58.0 - pu*10.0) - vec2(t*1.2, -t*0.6));\n  col = mix(col, FOAM, step(0.976 - a.sib*0.016 - pu*0.022, h21(sg))\n                       * smoothstep(0.85, 0.05, length(p - vec2(cx - 0.1, cy))) * step(p.y, 0.46) * (0.35 + a.sib));\n\n  // Vocals lay gold leaf into the sky, the way a deluxe printing would.\n  float gleaf = smoothstep(0.50, 0.72, fbm(p*2.4 + 31.0, 3));\n  col = mix(col, vec3(0.855,0.700,0.300), gleaf*a.voice*0.75*smoothstep(-0.12, 0.42, p.y)*(1.0 - body));\n  col += a.voice*0.30*FOAM*crestBand*body;\n\n  col *= 0.975 + 0.025*h21(floor(C*0.6));          // paper fibre\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "gold-ground", n: 14, name: "Gold Ground",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "The Byzantine technique: no perspective, no sky, only beaten gold and ornament. A mandorla at the centre, a band around it, and an outer field split into territories.",
    params: [{"name":"Sheen","def":0.4},{"name":"Mandorla","def":0.35},{"name":"Aureole","def":0.45}],
    reacts: [["sub","Strikes the leaf — a flash of raw metal"],["low","The spiral territory"],["pres","The eye-and-scale field; rays inside the mandorla"],["sib","The rectangle mosaic"],["air","The triangle scatter"],["voice","Raises the aureole: a halo of gold rays out of the ground"],["hit","A second strike across the whole ground"]],
    body: "// GOLD GROUND — the Byzantine technique Klimt took up: no perspective, no sky,\n// only beaten gold and ornament. It is composed rather than tiled: a mandorla at\n// the centre, a band of ornament around it, and the outer field broken into\n// territories that each carry a different motif on a different channel.\nvec3 leafInk(float v, float sweep){\n  vec3 g = ramp(vec3(0.145,0.085,0.020), vec3(0.760,0.545,0.130), vec3(1.00,0.930,0.640), clamp(v, 0.0, 1.0));\n  return g + vec3(1.00,0.95,0.78)*pow(clamp(sweep, 0.0, 1.0), 9.0)*0.75;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.16 + iParam0*0.40);\n  float r = length(p*vec2(1.0, 0.80)), th = atan(p.y, p.x);\n\n  vec3 BLACK = vec3(0.045,0.038,0.032), VERM = vec3(0.560,0.095,0.075),\n       TEAL  = vec3(0.045,0.215,0.265);\n\n  // Hammered ground, with a sheen that travels across the beaten metal.\n  float hammer = fbm(p*20.0, 2)*0.55 + fbm(p*52.0, 2)*0.45;\n  float sheen  = 0.5 + 0.5*sin((p.x*0.9 + p.y*1.5)*2.4 - t*0.8);\n  vec3 col = leafInk(0.26 + hammer*0.34 + a.low*0.16, sheen*0.80);\n  // The ground is darker toward the edges so the centre carries the picture.\n  col *= 0.62 + 0.55*smoothstep(0.95, 0.10, r);\n\n  float RM = 0.30 + iParam1*0.10;                 // mandorla radius\n\n  // ── Outer field: three territories, three motifs, three channels ──────────\n  float terr = fbm(p*1.05 + vec2(t*0.03, 0.0), OCT);\n  float outer = smoothstep(RM + 0.06, RM + 0.14, r);\n\n  // Spirals, on the low end. Klimt put them everywhere; they are the motif.\n  vec2 sg = p*3.4; vec2 si = floor(sg), sf = fract(sg) - 0.5;\n  float sr = length(sf), sa = atan(sf.y, sf.x);\n  float spiral = sin(sr*30.0 - sa*3.0 + h21(si)*20.0 + t*0.45);\n  col = mix(col, BLACK, outer * smoothstep(0.36, 0.42, terr) * (1.0 - smoothstep(0.52, 0.58, terr))\n                        * smoothstep(0.58, 0.88, spiral) * smoothstep(0.48, 0.16, sr)\n                        * (0.55 + comp(a.low, 0.30)*0.45));\n\n  // The scale-and-eye field, on presence.\n  vec2 eg = p*vec2(6.0, 7.2); eg.x += 0.5*floor(eg.y);\n  vec2 ei = floor(eg), ef = fract(eg) - 0.5;\n  float ed = length(ef*vec2(1.0, 1.25)), eh = h21(ei);\n  float ring = smoothstep(0.34, 0.30, ed) - smoothstep(0.19, 0.155, ed);\n  vec3 eyeInk = eh < 0.35 ? VERM : (eh < 0.70 ? TEAL : BLACK);\n  float eyeM = outer * smoothstep(0.58, 0.64, terr);\n  col = mix(col, eyeInk, eyeM * ring * (0.55 + a.pres*0.45));\n  col = mix(col, leafInk(0.92, 1.0), eyeM * smoothstep(0.12, 0.09, ed) * (0.25 + a.pres*0.75));\n\n  // The rectangle mosaic, on sibilance. Hard edges against all those curves.\n  vec2 rg = p*vec2(9.0, 4.2); vec2 ri = floor(rg), rf = fract(rg) - 0.5;\n  float rh = h21(ri);\n  float tile = smoothstep(0.44, 0.40, max(abs(rf.x), abs(rf.y)));\n  vec3 tileInk = rh < 0.34 ? BLACK : (rh < 0.60 ? VERM : (rh < 0.82 ? TEAL : leafInk(0.95, 1.0)));\n  col = mix(col, tileInk, outer * (1.0 - smoothstep(0.30, 0.36, terr)) * tile\n                          * (0.60 + a.sib*0.40) * step(0.20, rh));\n\n  // Triangles on the air band, scattered through whatever is left.\n  vec2 tg = p*8.0; vec2 ti = floor(tg), tf = fract(tg);\n  col = mix(col, BLACK, outer * step(tf.x + tf.y, 0.55) * step(0.72, h21(ti + 4.0)) * (0.15 + a.air*0.70));\n\n  // ── The band: a ring of alternating cells between field and mandorla ──────\n  float bandN = 40.0;\n  float ba = (th/6.28318 + 0.5)*bandN;\n  float bi = floor(ba), bf = fract(ba);\n  float inBand = smoothstep(RM + 0.005, RM + 0.020, r) * (1.0 - smoothstep(RM + 0.075, RM + 0.090, r));\n  float cell = smoothstep(0.10, 0.20, min(bf, 1.0 - bf));\n  float be = SPEC(0.006 + fract(bi/bandN)*0.50);\n  vec3 bandInk = mod(bi, 3.0) < 1.0 ? VERM : (mod(bi, 3.0) < 2.0 ? TEAL : BLACK);\n  col = mix(col, bandInk, inBand * cell * (0.35 + be*0.85));\n  col = mix(col, leafInk(0.98, 1.0), inBand * (1.0 - cell) * 0.8);\n\n  // ── The mandorla: raw leaf, a ring of rays, and a spiral heart ────────────\n  float inM = smoothstep(RM, RM - 0.006, r);\n  col = mix(col, leafInk(0.30 + hammer*0.26 + a.pres*0.20, sheen*0.7), inM*0.94);\n  float rays = pow(0.5 + 0.5*sin(th*26.0 + t*0.35), 3.0);\n  col = mix(col, leafInk(0.98, 1.0), inM * rays * smoothstep(RM*0.55, RM, r) * (0.35 + a.pres*0.65));\n  // The heart: concentric black spiral coils with vermilion between them, which\n  // is what stops the mandorla reading as a mirror.\n  float heart = sin(r*46.0 - th*3.0 + t*0.5);\n  float inner = smoothstep(RM*0.70, RM*0.62, r);\n  col = mix(col, BLACK, inM * inner * smoothstep(0.20, 0.75, heart) * 0.80);\n  col = mix(col, VERM,  inM * inner * smoothstep(-0.20,-0.72, heart) * 0.55);\n  col = mix(col, leafInk(1.0, 1.0), inM * smoothstep(0.052, 0.040, r));\n  col = mix(col, BLACK, inM * smoothstep(0.006, 0.0, abs(r - RM*0.66)) * 0.8);\n  col = mix(col, VERM, smoothstep(0.006, 0.0, abs(r - RM))*0.75);\n\n  // The aureole: vocals raise a halo of gold out of the ground.\n  float halo = pow(0.5 + 0.5*sin(th*18.0 - t*0.30), 2.2);\n  col += a.voice * (1.0 + iParam2*1.0) * halo * exp(-max(r - RM, 0.0)*3.4) * vec3(1.00,0.88,0.52);\n  col = mix(col, leafInk(1.0, 1.0), a.voice * smoothstep(RM*0.85, 0.03, r) * 0.80);\n\n  // Sub-bass and transients strike the leaf: a flash of raw metal.\n  col += (comp(a.sub, 0.30)*0.09 + pu*0.26) * vec3(1.00,0.93,0.70) * (0.4 + hammer*0.6);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "interference", n: 15, name: "Interference",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "Op-art. One carrier of bands, phase-modulated by a slower field so the plate warps rather than checkers, with every edge antialiased against its own derivative.",
    params: [{"name":"Rate","def":0.35},{"name":"Lens","def":0.4},{"name":"Band count","def":0.4}],
    reacts: [["sub","Inflates the lens the bands report"],["low","A second pinch off to one side"],["pres","Depth of the slow modulator; tints the light bands"],["sib","A concentric modulator crossing the first"],["air","Brings the second ink forward where the carriers beat"],["voice","Turns the whole plate on its axis"],["hit","Inverts the plate for one frame"]],
    body: "// INTERFERENCE — op-art. One family of bands, phase-modulated by a second, so\n// the plate warps rather than checkers; a checkerboard is what you get when two\n// stripe fields are multiplied, and it is not what these plates look like. Every\n// edge is antialiased against its own derivative, because the effect dies the\n// instant the stripes alias.\nfloat band(float phase){\n  float w = fwidth(phase)*0.62 + 1e-4;\n  return smoothstep(-w, w, sin(phase));\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.14 + iParam0*0.40);\n\n  // Vocals turn the plate. Nothing else is allowed to move the axis.\n  vec2 q = rot(t*0.05 + a.voice*0.75) * p;\n\n  // The lens: one soft swelling that the sub-bass inflates. This is the whole\n  // armature of the image — the bands only report its shape.\n  float lens = (0.55 + comp(a.sub, 0.30)*1.05 + iParam1*0.5) * exp(-dot(q*vec2(1.0,1.35), q*vec2(1.0,1.35))*2.2);\n  float pinch = (0.30 + comp(a.low, 0.30)*0.55) * exp(-dot((q - vec2(0.52,-0.16)), (q - vec2(0.52,-0.16)))*7.0);\n\n  // Modulator: slow, wide, and it bends the carrier rather than cutting it.\n  float mod1 = sin(q.x*2.6 - t*0.7 + lens*4.0) * (1.4 + a.pres*2.6);\n  float mod2 = sin(length(q*vec2(1.0,1.25))*7.5 - t*0.9) * (0.7 + a.sib*1.9);\n\n  float kA = 34.0 + iParam2*46.0;\n  float phase = q.y*kA*(1.0 + lens*1.6 - pinch*1.1) + mod1 + mod2;\n  float m = band(phase);\n\n  // A second, much finer carrier on the air band: the one that actually beats.\n  float fine = band(q.y*kA*3.03*(1.0 + lens*1.6) + mod1*3.0 - t*0.4);\n  float beat = abs(m - fine);\n\n  vec3 PAPER = vec3(0.950,0.942,0.925), INK = vec3(0.070,0.062,0.082);\n  // Two inks, and Riley changed which two between plates but never mid-plate.\n  vec3 HOT = mix(vec3(0.93,0.26,0.30), vec3(0.16,0.42,0.86), 0.5 + 0.5*sin(t*0.13));\n\n  vec3 col = mix(INK, PAPER, m);\n  // Where the two carriers are out of step, one ink comes forward.\n  col = mix(col, HOT, smoothstep(0.45, 0.95, beat) * (0.18 + a.air*0.75));\n  // Presence lifts the bands off the paper by tinting only their light halves.\n  col = mix(col, mix(col, HOT, 0.35), m * a.pres * 0.35);\n\n  // A transient inverts the plate for one frame. It reads as a flashbulb.\n  col = mix(col, vec3(1.0) - col, pu*0.50);\n\n  col *= 1.0 - smoothstep(0.60, 1.05, length(p))*0.30;\n  col *= 0.986 + 0.014*h21(floor(C*0.5));          // paper tooth\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "composition", n: 16, name: "Composition",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "Bauhaus. Eighteen hard-edged elements on a warm ground, each belonging to one channel, arriving on transients rather than pulsing — so the picture assembles itself.",
    params: [{"name":"Orbit","def":0.35},{"name":"Presence","def":0.45},{"name":"Diagonal","def":0.4}],
    reacts: [["sub","One family of elements"],["low","A second family"],["pres","A third"],["sib","Ring weight, and a fourth family"],["air","Inks the drawn grid"],["voice","Rules one long diagonal straight across the plate"],["hit","Pops elements up in scale as they arrive"]],
    body: "// COMPOSITION — Bauhaus. Twelve hard-edged elements on a warm ground: circles,\n// arcs, triangles, rules and one checker. Each element belongs to a channel, and\n// elements arrive on transients rather than pulsing, so the picture assembles\n// itself instead of throbbing.\nvec3 bauInk(int k){\n  if (k == 0) return vec3(0.055,0.050,0.055);   // black\n  if (k == 1) return vec3(0.84,0.24,0.16);      // vermilion\n  if (k == 2) return vec3(0.13,0.32,0.66);      // ultramarine\n  return             vec3(0.95,0.72,0.09);      // chrome yellow\n}\nfloat ringSDF(vec2 q, float r, float w){ return abs(length(q) - r) - w; }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.16 + iParam0*0.45);\n\n  // Warm ground with a faint gesso tooth.\n  vec3 col = vec3(0.936,0.918,0.876) - fbm(p*3.0, 3)*0.035 - h21(floor(C*0.5))*0.020;\n  float aa = 2.0/iResolution.y;\n\n  // A drawn grid, on the air band. It sets the plane the elements sit on.\n  vec2 g = p*6.0;\n  float grid = min(abs(fract(g.x) - 0.5), abs(fract(g.y) - 0.5));\n  col = mix(col, vec3(0.62,0.60,0.58), smoothstep(0.030, 0.010, grid) * (0.10 + a.air*0.55));\n\n  float E[6];\n  E[0] = comp(a.sub, 0.30); E[1] = comp(a.low, 0.30); E[2] = a.pres;\n  E[3] = a.sib;             E[4] = a.air;             E[5] = a.voice;\n\n  for (int i = 0; i < 18; i++){\n    float fi = float(i);\n    float hh = h21(vec2(fi, 3.0));\n    int  ch  = int(mod(fi, 6.0));\n    float e  = E[ch];\n    // A slow, fixed orbit; the audio never moves an element, only reveals it.\n    vec2  c  = vec2(cos(fi*2.399 + t*0.19)*0.74, sin(fi*1.713 + t*0.15)*0.38)\n             + vec2(hh - 0.5, h21(vec2(fi, 9.0)) - 0.5)*0.30;\n    float base = 0.055 + hh*0.17;\n    float r    = base * (0.55 + e*0.70 + pu*0.45*step(0.5, hh));\n    vec2  q    = rot(fi*1.1 + t*0.08*(hh - 0.5)*4.0) * (p - c);\n    vec3  ink  = bauInk(int(mod(fi*3.0 + 1.0, 4.0)));\n    float alpha = (0.30 + 0.70*smoothstep(0.03, 0.26, e)) * (0.55 + iParam1*0.45);\n\n    float d;\n    if (hh < 0.28)      d = length(q) - r;                                    // disc\n    else if (hh < 0.50) d = ringSDF(q, r, r*(0.10 + a.sib*0.10));             // ring\n    else if (hh < 0.70) d = max(max(q.x*0.866 - q.y*0.5, -q.x*0.866 - q.y*0.5), q.y - r); // triangle\n    else if (hh < 0.86) d = max(abs(q.x) - r*2.4, abs(q.y) - r*0.055);        // rule\n    else                d = max(abs(q.x) - r*0.80, abs(q.y) - r*0.80);        // square\n\n    float m = smoothstep(aa, -aa, d) * alpha;\n    // A checker fills one element in four, which is the Bauhaus tell.\n    if (hh > 0.70 && hh < 0.86) ink = bauInk(0);\n    float chk = (hh > 0.93) ? step(0.0, sin(q.x*90.0)*sin(q.y*90.0)) : 1.0;\n    col = mix(col, ink, m*mix(0.45, 1.0, chk));\n    // Every shape carries a drawn contour, as it would on the press.\n    col = mix(col, bauInk(0), smoothstep(aa*1.6, 0.0, abs(d)) * alpha * 0.85);\n  }\n\n  // Vocals draw one long diagonal rule straight across the plate.\n  float diag = abs(dot(p - vec2(0.0, -0.06), normalize(vec2(0.86, 0.51))) );\n  col = mix(col, bauInk(1), smoothstep(0.006, 0.001, diag) * a.voice * (0.8 + iParam2*0.2));\n  col = mix(col, bauInk(3), smoothstep(0.028, 0.020, length(p - vec2(0.0, -0.06))) * a.voice);\n\n  col *= 1.0 - smoothstep(0.70, 1.10, length(p))*0.22;\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "rose-window", n: 17, name: "Rose Window",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "Leaded glass lit from behind. The tracery is fixed and only the light moves, which is what a real window does all day; every pane owns one bin of the spectrum.",
    params: [{"name":"Drift","def":0.3},{"name":"Window size","def":0.4},{"name":"Flare","def":0.45}],
    reacts: [["low","The broad light behind the glass"],["pres","Shifts which colour each pane was cut from"],["sib","Hot edges along the lead came"],["air","Dust in the beam"],["voice","Flares the light behind and throws a shaft into the room"],["hit","A pulse through the oculus"]],
    body: "// ROSE WINDOW — leaded glass, lit from behind. Every pane owns one FFT bin, so\n// the mix reads around the window as light rather than as colour: the tracery is\n// fixed and only the light moves, which is what a real window does all day.\nvec3 glassInk(float h){\n  h = fract(h);\n  if (h < 0.18) return vec3(0.10,0.26,0.86);   // cobalt\n  if (h < 0.36) return vec3(0.82,0.09,0.16);   // ruby\n  if (h < 0.52) return vec3(0.09,0.58,0.42);   // emerald\n  if (h < 0.68) return vec3(0.95,0.68,0.12);   // amber\n  if (h < 0.84) return vec3(0.52,0.20,0.72);   // violet\n  return              vec3(0.88,0.90,0.95);    // grisaille\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float r = length(p), th = atan(p.y, p.x);\n  float t = iTime*(0.14 + iParam0*0.34);\n\n  vec3 col = vec3(0.010,0.011,0.017);           // the stone interior\n\n  float R = 0.46 + iParam1*0.10;\n  if (r < R){\n    // Tracery: petals around, rings across. Both counts are fixed — a window\n    // does not rebuild itself between verses.\n    float rings = 4.0;\n    float ri = floor(r/R*rings);\n    float petals = ri < 1.0 ? 8.0 : (ri < 2.0 ? 16.0 : (ri < 3.0 ? 24.0 : 32.0));\n    float pa = (th/6.28318 + 0.5)*petals + (t*0.02)*(ri - 1.5);\n    float pi = floor(pa), pf = fract(pa);\n    float rf = fract(r/R*rings);\n\n    float id  = h21(vec2(pi, ri));\n    float bin = pow(fract(pi/petals + ri*0.19), 1.9)*0.66 + 0.004;\n    float e   = SPEC(bin);\n\n    // Lead came between the panes: dark, and it never lights.\n    float lead = smoothstep(0.030, 0.075, min(min(pf, 1.0 - pf), min(rf, 1.0 - rf)));\n\n    vec3 glass = glassInk(id*0.97 + a.pres*0.06);\n    // Glass is not flat: it has bubbles and a thickness that lets light pool.\n    float grain = 0.72 + 0.55*fbm(p*38.0 + id*20.0, 2);\n    float lit = (0.10 + pow(e, 1.15)*2.5) * grain;\n    col = mix(col, glass*lit, lead);\n\n    // The light behind the window: a broad source, low band for its weight.\n    col += glass * lead * exp(-r*2.2) * (0.10 + comp(a.low, 0.30)*0.55) * grain;\n    // Sibilance rings the lead lines with a hot edge, as leaded glass does.\n    col += (1.0 - lead) * a.sib * 0.55 * glass * 0.35;\n  }\n\n  // The oculus at the centre stays grisaille and always reads.\n  float oc = smoothstep(0.062, 0.052, r);\n  col = mix(col, vec3(0.95,0.93,0.86)*(0.35 + a.pres*1.5), oc);\n  col = mix(col, vec3(0.02,0.02,0.03), smoothstep(0.052, 0.048, abs(r - 0.057)));\n\n  // The masonry rim.\n  col = mix(col, vec3(0.075,0.070,0.068), smoothstep(R, R + 0.016, r) * (1.0 - smoothstep(R + 0.05, R + 0.075, r)));\n\n  // Vocals flare the light behind the glass and throw a shaft into the room.\n  float shaft = pow(0.5 + 0.5*sin(th*3.0 + t*0.3), 6.0);\n  col += a.voice * (1.1 + iParam2*1.2) * (exp(-r*1.5)*0.55 + shaft*exp(-r*1.1)*0.6) * vec3(1.00,0.90,0.62);\n\n  // Dust in the beam, and the air band moves it.\n  col += step(0.9965 - a.air*0.004, h21(floor(p*120.0 + vec2(0.0, iTime*1.5))))\n       * (0.35 + a.air) * smoothstep(1.1, 0.1, r) * vec3(1.0,0.95,0.85);\n  col += pu * 0.25 * exp(-r*2.0) * vec3(1.0,0.96,0.88);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.62);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "ink-storm", n: 18, name: "Ink Storm",
    series: "II", set: "combustion", setTitle: "Combustion",
    kit3d: false,
    line: "Sumi-e. Six strokes, each loaded at the head and dry at the tail, eroded by a fibre field so the dry end breaks up. The seal is always there; it is a signature, not a reaction.",
    params: [{"name":"Gesture","def":0.35},{"name":"Length","def":0.4},{"name":"Weight","def":0.4}],
    reacts: [["sub","Weight of the loaded head"],["low","How far each stroke reaches"],["pres","Which strokes are inked at all"],["sib","Dry-brush break-up and splatter"],["air","Light sitting on ink that has not dried"],["voice","Brings the vermilion brush in over the black"],["hit","Drives the attack of every stroke"]],
    body: "// INK STORM — sumi-e. Six brush strokes on paper, each a swept capsule whose\n// width runs from loaded to dry along its length, eroded by a fibre field so the\n// dry end breaks up. Transients drive the attack; the paper does the rest.\nfloat capsule(vec2 p, vec2 a, vec2 b, out float u){\n  vec2 pa = p - a, ba = b - a;\n  u = clamp(dot(pa, ba)/dot(ba, ba), 0.0, 1.0);\n  return length(pa - ba*u);\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.20 + iParam0*0.50);\n\n  // Paper first: a warm white with fibre and a shadowed edge.\n  float fibre = fbm(p*vec2(90.0, 26.0), 2);\n  vec3 col = vec3(0.955,0.945,0.925) - fibre*0.035 - h21(floor(C*0.5))*0.022;\n  col -= smoothstep(0.42, 1.05, length(p))*0.055;\n\n  float wet = 0.0, dry = 0.0;\n  for (int i = 0; i < 6; i++){\n    float fi = float(i);\n    float ph = h21(vec2(fi, 2.0));\n    // Fixed gesture, slow drift. A brushstroke that jitters on the beat reads as\n    // a glitch; one that is simply there and simply heavy reads as calligraphy.\n    float ang = fi*1.9 + sin(t*0.21 + fi)*0.55 + a.voice*0.35;\n    vec2  mid = vec2(cos(fi*2.4 + t*0.13), sin(fi*1.7 + t*0.11))*0.30;\n    float len = 0.32 + ph*0.30 + comp(a.low, 0.30)*0.20 + iParam1*0.20;\n    vec2  d   = vec2(cos(ang), sin(ang))*len;\n    // A stroke bends: sample the capsule about a bowed midpoint.\n    vec2 bow = vec2(-d.y, d.x)*0.22*sin(fi*3.1 + t*0.3);\n    float u;\n    float dd = min(capsule(p, mid - d*0.5, mid + bow, u),\n                   capsule(p, mid + bow, mid + d*0.5, u));\n\n    // Width: loaded at the head, dry at the tail, weighted by the low end.\n    float head = 0.052 + comp(a.sub, 0.30)*0.030 + ph*0.020;\n    float w = head*(1.15 - u*0.85) * (0.55 + iParam2*0.65);\n    float e = SPEC(0.008 + ph*0.30);\n    w *= 0.55 + e*1.1 + pu*0.45;\n\n    // The fibre erodes the stroke, and it erodes the dry end far more.\n    float erode = fbm(p*26.0 + fi*11.0, OCT);\n    float dryness = smoothstep(0.35, 1.0, u) + a.sib*0.45;\n    float mask = smoothstep(w, w*0.35, dd + (erode - 0.5)*0.030*(0.4 + dryness*2.2));\n    wet = max(wet, mask);\n    // Splatter around the head, on sibilance.\n    dry = max(dry, step(0.985 - a.sib*0.012, h21(floor(p*150.0 + fi*40.0)))\n                   * smoothstep(0.22, 0.0, dd) );\n  }\n\n  // Ink is not black: it is blue-black where it pools and warm where it thins.\n  vec3 ink = mix(vec3(0.26,0.25,0.28), vec3(0.045,0.048,0.075), smoothstep(0.25, 0.95, wet));\n  col = mix(col, ink, clamp(wet*1.05, 0.0, 1.0));\n  col = mix(col, vec3(0.10,0.10,0.13), dry*0.75);\n  // Air catches on the wet edge, the way light sits on ink that has not dried.\n  col += wet*(1.0 - wet)*3.4 * a.air * 0.10 * vec3(0.75,0.82,0.95);\n\n  // Vocals bring the vermilion brush in over the black.\n  float vu;\n  float vd = capsule(p, vec2(-0.42, -0.20 + 0.05*sin(t*0.4)), vec2(0.40, 0.24), vu);\n  float vw = (0.020 + a.voice*0.030)*(1.2 - vu*0.9);\n  col = mix(col, vec3(0.76,0.16,0.11),\n            smoothstep(vw, vw*0.3, vd + (fbm(p*30.0 + 5.0, 2) - 0.5)*0.02) * a.voice * 1.1);\n\n  // The seal, bottom right. Always there — it is the signature, not a reaction.\n  vec2 sq = (p - vec2(0.72, -0.36))*vec2(1.0, 1.0);\n  float seal = step(max(abs(sq.x), abs(sq.y)), 0.045);\n  float glyph = step(0.42, fbm(sq*44.0 + 9.0, 2));\n  col = mix(col, vec3(0.72,0.14,0.10), seal*(0.92 - glyph*0.55));\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "squall", n: 19, name: "Squall",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "Turner. Sea, sky and weather are one substance and the only structure is where the light is — built in log-polar so the storm turns about its own eye.",
    params: [{"name":"Turn","def":0.3},{"name":"Sun","def":0.4},{"name":"Rays","def":0.4}],
    reacts: [["sub","Widens the eye of the storm"],["low","Depth of the warp; how hard it turns"],["pres","How much light survives the weather"],["sib","Spindrift torn along the wind"],["air","Scumble catching on the crests"],["voice","Burns the sun through, and sends rays out of it"]],
    body: "// SQUALL — Turner. Sea, sky and weather are one substance and the only structure\n// is where the light is. Built in log-polar so the storm turns about its own eye,\n// then scumbled horizontally so the paint drags the way a loaded brush does.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.12 + iParam0*0.30);\n\n  vec2 eye = vec2(-0.10 + 0.05*sin(t*0.17), 0.06 + 0.03*cos(t*0.13));\n  vec2 d = p - eye;\n  float r = max(length(d), 1e-3), th = atan(d.y, d.x);\n\n  // The storm: two turns of fbm in log-polar, the second warped by the first.\n  vec2 lp = vec2(th*1.25 + t*0.10, log(r)*1.15 - t*0.05);\n  float w1 = fbm(lp*1.5, 3);\n  float w  = fbm(lp*2.6 + w1*(1.6 + comp(a.low, 0.30)*1.4), OCT);\n  // Scumble: the brush drags sideways, so the field is stretched horizontally.\n  float scum = fbm(vec2(p.x*3.2, p.y*13.0) + t*0.06, 3);\n  w = w*0.76 + scum*0.24;\n\n  // Light lives at the eye and is eaten by weather with distance.\n  float lum = exp(-r*(2.9 - comp(a.sub, 0.35)*0.55)) * (0.60 + a.pres*1.05);\n  // Weather first, light second: the storm sets the value and the eye punches\n  // a hole in it, rather than the light washing the whole plate.\n  float v = clamp(lum*0.95 + (w - 0.5)*1.45 + 0.30, 0.0, 1.0);\n  v = pow(v, 1.25);\n\n  vec3 DARK = vec3(0.115,0.120,0.140), SLATE = vec3(0.345,0.355,0.375),\n       UMBER= vec3(0.520,0.400,0.255), OCHRE = vec3(0.870,0.680,0.330),\n       CREAM= vec3(0.985,0.955,0.870);\n  vec3 col;\n  if (v < 0.30)      col = mix(DARK,  SLATE, v/0.30);\n  else if (v < 0.55) col = mix(SLATE, UMBER, (v-0.30)/0.25);\n  else if (v < 0.80) col = mix(UMBER, OCHRE, (v-0.55)/0.25);\n  else               col = mix(OCHRE, CREAM, (v-0.80)/0.20);\n\n  // A horizon that is barely there — Turner leaves you to find it.\n  float hz = -0.10 + 0.02*sin(p.x*2.0 + t*0.2);\n  col = mix(col, col*0.86, smoothstep(0.10, 0.0, abs(p.y - hz))*0.45);\n  col = mix(col, mix(col, SLATE, 0.5), smoothstep(hz, hz - 0.55, p.y)*0.35);\n\n  // Spindrift, off the sibilance, torn along the wind rather than sprinkled.\n  float drift = fbm(vec2(p.x*7.0 - t*1.1, p.y*30.0), 2);\n  col = mix(col, CREAM, smoothstep(0.70, 0.92, drift) * a.sib * 0.55 * smoothstep(0.35, -0.35, p.y));\n  col += a.air * 0.16 * smoothstep(0.55, 0.95, scum) * CREAM;\n\n  // A hull, dissolving. It is what tells you the scale of the weather.\n  vec2 hq = (p - vec2(0.30, hz + 0.015))*vec2(1.0, 3.2);\n  float hull = smoothstep(0.085, 0.070, length(hq)) * smoothstep(0.42, 0.58, w);\n  col = mix(col, DARK*0.75, hull*0.55);\n  col = mix(col, DARK*0.9, smoothstep(0.004, 0.0, abs(p.x - 0.295)) * step(hz + 0.01, p.y) * step(p.y, hz + 0.115) * 0.45);\n\n  // Vocals burn the sun through the weather.\n  col = mix(col, CREAM, a.voice * (0.55 + iParam1*0.45) * exp(-r*3.4));\n  col += a.voice * 0.55 * OCHRE * pow(0.5 + 0.5*sin(th*5.0 + t*0.3), 3.0) * exp(-r*1.4) * (0.5 + iParam2);\n\n  col *= 1.0 - smoothstep(0.55, 1.15, length(p))*0.30;\n  col *= 0.975 + 0.025*fbm(p*70.0, 2);          // canvas\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "starfall", n: 20, name: "Starfall",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "The post-impressionist night. Short comma strokes laid along a curl field, heavy impasto so every stroke carries its own ridge of light, and stars with the halo rings the style is known for.",
    params: [{"name":"Tempo","def":0.3},{"name":"Brush size","def":0.35},{"name":"Corona","def":0.45}],
    reacts: [["low","Lifts the whole value field"],["pres","Breaks more colour into each stroke"],["air","The impasto ridge where the paint stands up"],["voice","Blazes one star into a full corona"]],
    body: "// STARFALL — the post-impressionist night. Short comma strokes laid along a curl\n// field so the sky turns; heavy impasto, so every stroke carries its own ridge of\n// light on the side the moon is on. The stroke engine is the one from Lily Water\n// run at night and in a vortex.\nvec3 nightInk(float v){\n  v = clamp(v, 0.0, 1.0);\n  if (v < 0.30) return mix(vec3(0.040,0.075,0.150), vec3(0.075,0.175,0.360), v/0.30);\n  if (v < 0.58) return mix(vec3(0.075,0.175,0.360), vec3(0.170,0.360,0.620), (v-0.30)/0.28);\n  if (v < 0.80) return mix(vec3(0.170,0.360,0.620), vec3(0.620,0.700,0.560), (v-0.58)/0.22);\n  return              mix(vec3(0.620,0.700,0.560), vec3(0.980,0.860,0.420), (v-0.80)/0.20);\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.13 + iParam0*0.28);\n\n  // Two vortices plus a drift — the curl field the whole sky is combed along.\n  vec2 v1 = vec2(-0.26, 0.14), v2 = vec2(0.30, 0.02);\n  vec2 d1 = p - v1, d2 = p - v2;\n  float sw = atan(d1.y, d1.x) + 1.5708 - length(d1)*2.2 + t*0.10;\n  float sw2 = atan(d2.y, d2.x) + 1.5708 - length(d2)*2.6 - t*0.08;\n  float blend = smoothstep(-0.1, 0.5, length(d1) - length(d2));\n  float ang = mix(sw, sw2, blend) + (fbm(p*1.5 + t*0.05, 3) - 0.5)*1.4;\n\n  // Where the light is: the stars and the horizon glow drive the value field.\n  float glow = 0.0;\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    vec2 sc = vec2(cos(fi*2.1)*0.62, 0.10 + sin(fi*1.6)*0.26);\n    float e = SPEC(0.008 + fi*0.055);\n    glow += (0.030 + e*0.075) / (length(p - sc) + 0.055);\n  }\n  float horizon = smoothstep(-0.02, -0.42, p.y);\n  float base = clamp(glow*0.16 + fbm(p*1.25 + t*0.06, OCT)*0.55 - horizon*0.35 + comp(a.low,0.3)*0.10, 0.0, 1.0);\n\n  vec3 col = vec3(0.0);\n  for (int L = 0; L < 2; L++){\n    float fl = float(L);\n    float sz = (0.052 - fl*0.026) * (1.0 + iParam1*1.1);\n    float a2 = ang + fl*0.55;\n    mat2  M = rot(a2);\n    vec2  cell = vec2(sz*3.2, sz);\n    vec2  sp = (M*p)/cell;\n    float row = floor(sp.y);\n    float shift = 0.5*row + 0.29*sin(row*2.7);\n    sp.x += shift;\n    vec2 si = floor(sp), sf = fract(sp) - 0.5;\n    vec2 cc = rot(-a2) * ((si + vec2(0.5 - shift, 0.5)) * cell);\n\n    float jit = h21(si + fl*29.0);\n    float g2 = 0.0;\n    for (int i = 0; i < 7; i++){\n      float fi = float(i);\n      vec2 sc2 = vec2(cos(fi*2.1)*0.62, 0.10 + sin(fi*1.6)*0.26);\n      g2 += (0.030 + SPEC(0.008 + fi*0.055)*0.075) / (length(cc - sc2) + 0.055);\n    }\n    float val = clamp(g2*0.16 + fbm(cc*1.25 + t*0.06, 3)*0.55\n                      - smoothstep(-0.02,-0.42,cc.y)*0.35 + (jit - 0.5)*0.14, 0.0, 1.0);\n    vec3 sc3 = nightInk(val);\n    // Broken colour, colder here than on the pond: viridian and violet.\n    float j2 = h21(si + 53.0 + fl*7.0);\n    vec3 accent = j2 < 0.34 ? vec3(0.12,0.42,0.42)\n                : j2 < 0.67 ? vec3(0.40,0.32,0.66)\n                            : vec3(0.86,0.72,0.28);\n    sc3 = mix(sc3, accent, 0.05 + 0.18*h21(si + 3.0) + a.pres*0.12);\n\n    // Impasto: the ridge of paint catches light on one side of every stroke.\n    float body = smoothstep(0.52, 0.28, max(abs(sf.x)*0.82, abs(sf.y)));\n    sc3 += smoothstep(0.10, 0.42, sf.y) * body * (0.10 + a.air*0.55) * vec3(1.00,0.95,0.80);\n    sc3 -= smoothstep(-0.10, -0.44, sf.y) * body * 0.10;\n    col = (L == 0) ? mix(nightInk(base), sc3, 0.22 + body*0.78)\n                   : mix(col, sc3, body*(0.52 + 0.36*jit));\n  }\n\n  // The stars themselves, each with the halo ring the style is known for.\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    vec2 sc = vec2(cos(fi*2.1)*0.62, 0.10 + sin(fi*1.6)*0.26);\n    float e = SPEC(0.008 + fi*0.055);\n    float rr = length(p - sc);\n    float core = smoothstep(0.030 + e*0.020, 0.0, rr);\n    float halo = smoothstep(0.10, 0.0, abs(rr - (0.055 + e*0.045)));\n    col = mix(col, vec3(1.00,0.94,0.66), core*0.95);\n    col += halo * (0.10 + e*0.85) * vec3(0.98,0.86,0.42) * 0.55;\n  }\n\n  // The cypress: black-green, and it is the only vertical in the frame.\n  float cx = abs(p.x + 0.66) * (2.6 - p.y*0.9);\n  float cy = smoothstep(0.55, -0.5, p.y);\n  float cyp = step(cx + fbm(vec2(p.y*9.0, 0.0), 2)*0.12, 0.20*cy);\n  col = mix(col, vec3(0.045,0.105,0.075), cyp*0.94);\n\n  // Vocals blaze one star into a full corona.\n  vec2 mc = vec2(cos(2.1)*0.62, 0.10 + sin(1.6)*0.26);\n  float mr = length(p - mc);\n  col += a.voice * (1.3 + iParam2*1.2) * exp(-mr*(6.0 - a.voice*2.5)) * vec3(1.00,0.90,0.52);\n  col += a.voice * 0.5 * smoothstep(0.16, 0.0, abs(mr - 0.12 - a.voice*0.06)) * vec3(1.00,0.94,0.66);\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "colour-field", n: 21, name: "Colour Field",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "The quietest work in either series and the hardest to get right, because there is nowhere to hide. Two stained rectangles, edges brushed rather than cut, and the audio only ever changes what colour the light in them is.",
    params: [{"name":"Drift","def":0.3},{"name":"Feather","def":0.35},{"name":"Third field","def":0.4}],
    reacts: [["sub","The ground the fields are floated on"],["low","The lower field, and its weight"],["pres","Heat in the upper field"],["sib","Canvas tooth catching the light"],["air","How far each edge bleeds"],["voice","Opens a third field between the two — the only thing that ever arrives"]],
    body: "// COLOUR FIELD — the quietest work in either series, and the hardest to get\n// right, because there is nowhere to hide. Two stained rectangles on a ground,\n// edges brushed rather than cut, and the audio is only ever allowed to change\n// what colour the light in them is.\nvec3 stain(vec3 base, float weave, float bleed){\n  // Thinned pigment on raw canvas: the weave shows through and the colour is\n  // never quite even across the field.\n  return base * (0.90 + weave*0.16) + base*bleed*0.22;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.05 + iParam0*0.10);          // glacial on purpose\n\n  float weave = fbm(p*vec2(64.0, 58.0), 2)*0.6 + fbm(p*vec2(9.0, 11.0), 2)*0.4;\n  float cloud = fbm(p*1.6 + t*0.4, OCT);\n\n  // Ground: a deep field the rectangles are floated on.\n  vec3 ground = mix(vec3(0.185,0.048,0.062), vec3(0.290,0.075,0.070),\n                    0.35 + cloud*0.45 + comp(a.sub, 0.35)*0.22);\n  vec3 col = stain(ground, weave, cloud*0.3);\n\n  float feather = 0.055 + a.air*0.075 + iParam1*0.06;\n\n  // Upper field: heat. Presence carries it.\n  {\n    float top = 0.34 + 0.012*sin(t*1.7), bot = 0.02 + 0.012*cos(t*1.3);\n    float wob = (fbm(vec2(p.x*5.0, 3.0), 2) - 0.5)*0.05;      // a brushed, not a cut, edge\n    float m = smoothstep(top + feather, top - feather*0.35, p.y + wob)\n            * smoothstep(bot - feather, bot + feather*0.35, p.y + wob)\n            * smoothstep(0.76, 0.62, abs(p.x));\n    vec3 fieldCol = mix(vec3(0.86,0.34,0.10), vec3(0.98,0.66,0.20), 0.30 + a.pres*0.85);\n    col = mix(col, stain(fieldCol, weave, cloud*0.5), m*0.94);\n  }\n  // Lower field: weight. The low band carries it, and it is nearly black.\n  {\n    float top = -0.06 + 0.010*cos(t*1.1), bot = -0.40 + 0.010*sin(t*0.9);\n    float wob = (fbm(vec2(p.x*5.0, 9.0), 2) - 0.5)*0.05;\n    float m = smoothstep(top + feather, top - feather*0.35, p.y + wob)\n            * smoothstep(bot - feather, bot + feather*0.35, p.y + wob)\n            * smoothstep(0.76, 0.62, abs(p.x));\n    vec3 fieldCol = mix(vec3(0.055,0.040,0.075), vec3(0.240,0.090,0.190), 0.20 + comp(a.low, 0.30)*0.80);\n    col = mix(col, stain(fieldCol, weave, cloud*0.4), m*0.96);\n  }\n\n  // Vocals open a third field between the two — thin, luminous, and the only\n  // thing in the picture that ever arrives.\n  {\n    float c = -0.02, h = 0.012 + a.voice*(0.052 + iParam2*0.05);\n    float m = smoothstep(h + feather*0.8, h*0.3, abs(p.y - c)) * smoothstep(0.74, 0.60, abs(p.x));\n    col = mix(col, mix(vec3(1.00,0.88,0.52), vec3(1.00,0.97,0.88), a.voice), m*a.voice*0.95);\n  }\n\n  // Sibilance is the tooth of the canvas catching the light.\n  col += weave*weave * a.sib * 0.09 * vec3(1.0,0.96,0.90);\n  // The edges of a stretched canvas fall off. It matters more than it sounds.\n  col *= 1.0 - smoothstep(0.50, 1.15, length(p*vec2(0.72, 1.0)))*0.30;\n  col *= 0.985 + 0.015*h21(floor(C*0.5));\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "shan-shui", n: 22, name: "Shan Shui",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "Mountain-and-water. Five ranges, each a hard silhouette at the ridge and dissolving downward as the brush unloads. The mist is not painted: it is paper left alone.",
    params: [{"name":"Drift","def":0.3},{"name":"Mist","def":0.4},{"name":"Late light","def":0.4}],
    reacts: [["sub","Raises the mist between the ranges"],["low","Walks the ranges past each other"],["pres","Ink density"],["sib","Axe-cut strokes on the near ranges"],["air","Snow, and not very much of it"],["voice","Warms the paper the way late light does, and sends one crane across"]],
    body: "// SHAN SHUI — mountain-and-water. Five ranges in ink, each a hard silhouette at\n// its ridge and dissolving downward the way a loaded brush unloads. The mist is\n// not painted: it is paper left alone between the ranges, which is the whole\n// convention this tradition is built on.\nfloat rangeLine(float x, float f, float seed, float amp, float base){\n  float r = fbm(vec2(x*f + seed, seed*0.6), 3);\n  float peak = pow(abs(fbm(vec2(x*f*2.3 + seed*3.0, 1.0), 2) - 0.5)*2.0, 0.55);\n  return base + amp*(r - 0.5)*1.35 + amp*0.85*peak;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.10 + iParam0*0.25);\n\n  vec3 PAPER = vec3(0.958,0.948,0.922);\n  vec3 col = PAPER - h21(floor(C*0.5))*0.020 - fbm(p*vec2(70.0,20.0), 2)*0.018;\n  col -= smoothstep(0.35, 1.05, length(p))*0.045;\n\n  float drift = comp(a.low, 0.30)*0.05 + t*0.012;\n  float mist  = 0.30 + comp(a.sub, 0.35)*0.45 + iParam1*0.25;\n\n  for (int i = 0; i < 5; i++){\n    float fi = float(i);\n    float near = fi/4.0;                                   // 0 far, 1 near\n    float y = rangeLine(p.x + drift*(0.3 + near), 1.2 + near*1.6, 5.0 + fi*13.0,\n                        0.075 + near*0.150, 0.235 - fi*0.130);\n    float m = smoothstep(y + 0.002, y - 0.002, p.y);\n    // The brush is loaded at the ridge and dry by the time it reaches the foot.\n    float unload = exp(-(y - p.y)*(3.2 - near*1.5));\n    vec3 ink = mix(vec3(0.400,0.450,0.490), vec3(0.085,0.105,0.135), near);\n    float density = (0.48 + near*0.40) * (0.72 + a.pres*0.50);\n    col = mix(col, ink, m * (0.20 + 0.80*unload) * density);\n    // The ridge itself takes the darkest line — that is where the brush lands.\n    col = mix(col, ink*0.55, m * smoothstep(0.030, 0.0, y - p.y) * (0.45 + near*0.45));\n    // Axe-cut strokes, only on the ranges you could reach.\n    if (i >= 3){\n      vec2 aq = rot(-0.62)*(p*vec2(52.0, 20.0));\n      float cut = smoothstep(0.60, 0.88, fbm(aq, 2)) * step(0.045, y - p.y) * step(y - p.y, 0.30);\n      col = mix(col, ink*0.60, m * cut * (0.30 + a.sib*0.60));\n    }\n    // Mist: paper left alone along the foot of each range.\n    // Mist sits at its own level and stays flat, the way air does. Following the\n    // ridge line is what made it read as a scalloped border.\n    float band = 0.150 - fi*0.130 + 0.018*fbm(vec2(p.x*1.8 + t*0.4, fi*3.0), 2);\n    col = mix(col, PAPER, m * smoothstep(0.055, 0.0, abs(p.y - band))\n                            * mist * (0.55 + 0.45*fbm(vec2(p.x*2.2 + t, fi), 2)));\n  }\n\n  // A pine on the near ridge. Two brush marks and a trunk, nothing more.\n  vec2 tq = p - vec2(-0.46, -0.245);\n  float trunk = smoothstep(0.0050, 0.0018, abs(tq.x + tq.y*0.16)) * step(0.0, tq.y) * step(tq.y, 0.125);\n  float bough = smoothstep(0.028, 0.005, abs(tq.y - 0.115 - abs(tq.x)*0.30)) * step(abs(tq.x), 0.090);\n  float bough2 = smoothstep(0.022, 0.004, abs(tq.y - 0.078 - abs(tq.x)*0.44)) * step(abs(tq.x), 0.066);\n  col = mix(col, vec3(0.055,0.068,0.085), clamp(trunk + bough + bough2, 0.0, 1.0)*0.92);\n\n  // Air is snow, and not very much of it.\n  vec2 sg = floor(p*54.0 - vec2(t*0.7, -t*1.5));\n  col = mix(col, vec3(1.0), step(0.988 - a.air*0.010, h21(sg))*(0.30 + a.air*0.60));\n\n  // Vocals warm the paper the way late light does, and send one crane across.\n  col = mix(col, vec3(0.965,0.905,0.745), a.voice*0.35*(0.5 + iParam2));\n  float ph = fract(t*0.09);\n  vec2 cq = p - vec2(mix(-0.85, 0.85, ph), 0.32 - 0.20*ph*ph);\n  cq.x = abs(cq.x);\n  col = mix(col, vec3(0.10,0.12,0.15),\n            smoothstep(0.0045, 0.0, abs(cq.y - cq.x*0.55)) * step(cq.x, 0.030) * a.voice*0.95);\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "aperture", n: 23, name: "Aperture",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "Light as the material rather than as a way of showing something else. One soft opening in a dark wall with no edge you can find — two gradients, a very slow mind, and almost no cost to draw.",
    params: [{"name":"Turn","def":0.35},{"name":"Opening","def":0.35},{"name":"Flood","def":0.45}],
    reacts: [["sub","The opening breathes — a few percent, no more"],["low","The warm third of the field, and the spill into the room"],["pres","The first hue, and how far the light reaches"],["sib","Grain on the wall, which is what stops the gradient banding"],["air","The second hue, and the softness of the edge"],["voice","Floods a second colour in from one side of the opening"]],
    body: "// APERTURE — light as the material, not as a way of showing something else. One\n// soft opening in a dark wall, filled with a field of colour that has no edge you\n// can find. The whole work is two gradients and a very slow mind; it costs almost\n// nothing to draw, which is why it is the one to reach for on a weak GPU.\nfloat roundBox(vec2 q, vec2 b, float r){\n  vec2 d = abs(q) - b + r;\n  return min(max(d.x, d.y), 0.0) + length(max(d, 0.0)) - r;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.05 + iParam0*0.12);\n\n  // The opening breathes on the sub, but only just — a few percent.\n  vec2 aperture = vec2(0.62, 0.30) * (1.0 + comp(a.sub, 0.40)*0.045 + iParam1*0.20);\n  float d = roundBox(p, aperture, 0.11);\n\n  // Interior field: two hues crossfading on a diagonal that turns once a minute.\n  vec2 ax = vec2(cos(t*0.5), sin(t*0.5));\n  float u = clamp(dot(p, ax)*0.9 + 0.5, 0.0, 1.0);\n  float vy = clamp(p.y/aperture.y*0.5 + 0.5, 0.0, 1.0);\n\n  vec3 cA = mix(vec3(0.10,0.30,0.95), vec3(0.95,0.20,0.55), 0.35 + a.pres*0.55);\n  vec3 cB = mix(vec3(0.10,0.85,0.80), vec3(0.55,0.25,1.00), 0.30 + a.air*0.65);\n  vec3 cC = mix(vec3(1.00,0.42,0.12), vec3(0.98,0.85,0.35), 0.25 + comp(a.low, 0.30)*0.60);\n  vec3 field = mix(mix(cA, cB, u), cC, smoothstep(0.62, 1.0, vy)*0.55);\n  // The field is not flat: a very slow atmosphere moves inside it.\n  field *= 0.86 + 0.20*fbm(p*1.05 + t*0.5, 3);\n\n  // Two falloffs: a hard-ish one for the plane of the wall, and a wide one that\n  // is the light actually spilling into the room.\n  float inside = smoothstep(0.055 + a.air*0.030, -0.045, d);\n  float spill  = exp(-max(d, 0.0)*(3.4 - a.pres*0.9));\n\n  vec3 wall = vec3(0.030,0.031,0.038) * (0.55 + 0.55*smoothstep(0.7, -0.5, p.y));\n  vec3 col = wall + field*inside*(0.85 + a.pres*0.55) + field*spill*(0.16 + comp(a.low,0.30)*0.20);\n\n  // Vocals flood a second colour in from the left edge of the opening.\n  float wash = smoothstep(-aperture.x - 0.1, aperture.x*0.6, p.x);\n  vec3 vcol = mix(vec3(1.00,0.86,0.42), vec3(1.00,0.97,0.90), a.voice);\n  col = mix(col, mix(col, vcol, 0.80), a.voice*(1.0 - wash)*inside*(0.6 + iParam2*0.5));\n  col += a.voice*0.30*vcol*spill;\n\n  // Sibilance is the only thing with an edge in the whole picture: a fine grain\n  // on the wall, which is what stops the gradients banding on a big screen.\n  col += (h21(floor(C) + floor(iTime*60.0)) - 0.5) * (0.012 + a.sib*0.020);\n  col *= 1.0 - smoothstep(0.75, 1.35, length(p))*0.45;\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "temple", n: 24, name: "Temple",
    series: "II", set: "vespers", setTitle: "Vespers",
    kit3d: false,
    line: "The esoteric diagram: flat pastel geometry in thin graphite on warm paper, arranged as though it meant something specific. Nothing is shaded and nothing is lit; the audio changes which parts are present.",
    params: [{"name":"Drift","def":0.3},{"name":"Line weight","def":0.35},{"name":"Vesica","def":0.4}],
    reacts: [["sub","The outermost disc"],["low","The disc inside it, and the spiral winding"],["pres","The third disc"],["sib","How many radiating spokes are inked"],["air","The innermost disc"],["voice","Opens the vesica at the centre — the one gold in the diagram"]],
    body: "// TEMPLE — the esoteric diagram: flat pastel geometry drawn in thin graphite on\n// warm paper, arranged as if it meant something specific. Nothing here is shaded\n// and nothing is lit; the audio changes which parts of the diagram are present.\nvec3 pastel(int k){\n  if (k == 0) return vec3(0.906,0.647,0.612);   // rose\n  if (k == 1) return vec3(0.596,0.714,0.827);   // powder blue\n  if (k == 2) return vec3(0.922,0.808,0.478);   // soft yellow\n  if (k == 3) return vec3(0.706,0.627,0.776);   // lilac\n  return             vec3(0.478,0.671,0.588);   // sage\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.06 + iParam0*0.16);\n  float aa = 2.0/iResolution.y;\n  float LINE = 0.0016 + 0.0010*iParam1;\n\n  vec3 col = vec3(0.949,0.925,0.866) - h21(floor(C*0.5))*0.020 - fbm(p*5.0, 3)*0.020;\n  vec3 GRAPHITE = vec3(0.235,0.235,0.275);\n\n  float r = length(p), th = atan(p.y, p.x);\n\n  // Four concentric discs, each a channel, each drawn flat and outlined.\n  float E[4];\n  E[0] = comp(a.sub, 0.32); E[1] = comp(a.low, 0.30); E[2] = a.pres; E[3] = a.air;\n  for (int i = 0; i < 4; i++){\n    float fi = float(i);\n    float rad = 0.40 - fi*0.085;\n    float m = smoothstep(rad, rad - aa*1.5, r) * (1.0 - smoothstep(rad - 0.062, rad - 0.062 - aa*1.5, r));\n    col = mix(col, pastel(i), m * (0.22 + E[i]*0.72));\n    col = mix(col, GRAPHITE, smoothstep(LINE*1.6, 0.0, abs(r - rad)) * 0.75);\n    col = mix(col, GRAPHITE, smoothstep(LINE*1.6, 0.0, abs(r - rad + 0.062)) * 0.55);\n  }\n\n  // The radiating segments — sibilance decides how many of them are inked.\n  float segs = 24.0;\n  float sa = (th/6.28318 + 0.5)*segs;\n  float si = floor(sa);\n  float spoke = smoothstep(LINE*2.2, 0.0, abs(fract(sa) - 0.5)/segs*6.28318*max(r,0.02));\n  col = mix(col, GRAPHITE, spoke * step(h21(vec2(si, 1.0)), 0.30 + a.sib*0.62) * smoothstep(0.13, 0.42, r) * 0.7);\n\n  // The spiral. It winds on the low band and it is the only line that moves.\n  float spiral = abs(fract((log(max(r, 0.02))*2.1 + th/6.28318)*1.0 + t*0.15) - 0.5);\n  col = mix(col, pastel(3)*0.85, smoothstep(0.030, 0.006, spiral) * smoothstep(0.06, 0.20, r)\n                                  * (0.10 + comp(a.low, 0.30)*0.55));\n\n  // Small satellites on a ring, each holding one bin of the spectrum.\n  for (int i = 0; i < 9; i++){\n    float fi = float(i);\n    float ang = fi/9.0*6.28318 + t*0.10;\n    vec2 c = vec2(cos(ang), sin(ang))*0.60;\n    float e = SPEC(0.006 + fi*0.055);\n    float rad = 0.026 + e*0.030;\n    float dd = length(p - c) - rad;\n    col = mix(col, pastel(int(mod(fi, 5.0))), smoothstep(aa, -aa, dd)*(0.25 + e*0.70));\n    col = mix(col, GRAPHITE, smoothstep(LINE*1.6, 0.0, abs(dd))*0.65);\n  }\n\n  // The vesica: two circles crossing. Vocals open it, and it is the one gold.\n  {\n    float off = 0.115;\n    float d1 = length(p - vec2(-off, 0.0)) - 0.20;\n    float d2 = length(p - vec2( off, 0.0)) - 0.20;\n    float ves = smoothstep(aa, -aa, max(d1, d2));\n    col = mix(col, vec3(0.851,0.694,0.290), ves * a.voice * (0.75 + iParam2*0.25));\n    col = mix(col, GRAPHITE, (smoothstep(LINE*1.5, 0.0, abs(d1)) + smoothstep(LINE*1.5, 0.0, abs(d2)))\n                              * (0.20 + a.voice*0.60) * 0.7);\n    col += ves * a.voice * 0.25 * vec3(1.00,0.92,0.62);\n  }\n\n  // A ruled baseline, because these diagrams were always drawn on one.\n  col = mix(col, GRAPHITE, smoothstep(LINE*1.4, 0.0, abs(p.y + 0.435)) * 0.35);\n\n  col *= 1.0 - smoothstep(0.72, 1.15, length(p))*0.16;\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "kluver", n: 25, name: "Klüver",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "The four form constants — tunnels, spirals, lattices, cobwebs. The retina maps to the cortex in log-polar, so all four are the same stripe field at different angles. This does one log-polar transform and draws four very simple patterns in it.",
    params: [{"name":"Rate","def":0.35},{"name":"Symmetry","def":0.4},{"name":"Rosette","def":0.45}],
    reacts: [["sub","Weight of the tunnel form"],["low","Weight of the spiral form, and the stripe rate"],["pres","Weight of the lattice form"],["sib","Weight of the cobweb form"],["air","The fine grain inside whichever form is winning"],["voice","Opens a fifth form: a rosette that exists only while someone sings"],["hit","A flash out of the centre"]],
    body: "// KLÜVER — the four form constants: tunnels, spirals, lattices and cobwebs. They\n// are what people actually report seeing, and there is a reason all four look\n// related: the retina maps to the visual cortex in log-polar coordinates, so a\n// plain set of stripes on the cortex becomes a tunnel, a spiral or a lattice\n// depending only on its angle. So this does one log-polar transform and then\n// draws four very simple patterns in it.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.20 + iParam0*0.50);\n\n  float r = max(length(p), 1e-3), th = atan(p.y, p.x);\n  // Cortical coordinates. x is log-radius, y is angle: the plane the cortex works in.\n  vec2 cx = vec2(log(r)*2.4 - t*0.55, th*(3.0 + floor(iParam1*5.0)));\n\n  float k = 3.4 + comp(a.low, 0.30)*3.2;\n  // The four constants, each a different orientation of the same stripe field.\n  float tunnel = sin(cx.x*k);\n  float spiral = sin(cx.x*k*0.8 + cx.y*2.6);\n  float lattis = sin(cx.x*k*0.9)*sin(cx.y*3.4);\n  float cobweb = max(sin(cx.y*7.0), sin(cx.x*k*1.6)*0.85);\n\n  // Which constant dominates is on the mix, and the crossfade is slow enough\n  // that you watch one form become another rather than cut between them.\n  float ph = t*0.11;\n  vec4 w = vec4(0.5 + 0.5*sin(ph),        0.5 + 0.5*sin(ph + 1.57),\n                0.5 + 0.5*sin(ph + 3.14), 0.5 + 0.5*sin(ph + 4.71));\n  w *= vec4(0.35 + comp(a.sub,0.32)*1.3, 0.35 + comp(a.low,0.30)*1.3,\n            0.35 + a.pres*1.3,           0.35 + a.sib*1.3);\n  w /= dot(w, vec4(1.0)) + 1e-3;\n  float f = tunnel*w.x + spiral*w.y + lattis*w.z + cobweb*w.w;\n\n  // Saturated, and the hue rotates with radius so the tunnel has depth in it.\n  vec3 col = ramp(vec3(0.06,0.02,0.28), vec3(0.95,0.15,0.55), vec3(1.00,0.85,0.25),\n                  fract(f*0.5 + 0.5 + log(r)*0.35 + t*0.05));\n  col *= 0.30 + 0.85*smoothstep(-0.15, 0.55, f);\n  // A second, thinner pass at three times the rate: the fine grain inside the form.\n  col += (0.05 + a.air*0.60) * pow(0.5 + 0.5*sin(cx.x*k*3.0 + cx.y*5.0), 8.0) * vec3(0.75,0.95,1.00);\n\n  // Everything converges on the centre, and the centre is where the light is.\n  col *= 1.0 - smoothstep(0.10, 0.95, r)*0.55;\n  col += exp(-r*(7.0 - a.voice*3.0)) * (0.10 + a.voice*1.30) * vec3(1.00,0.92,0.70);\n\n  // Vocals open a fifth form: a rosette that only exists while someone sings.\n  float rosette = pow(0.5 + 0.5*cos(th*12.0 + t*0.4), 6.0) * exp(-abs(r - 0.30 - a.voice*0.16)*9.0);\n  col += rosette * a.voice * (1.6 + iParam2*1.4) * vec3(0.55,1.00,0.85);\n\n  col += pu*0.30*vec3(1.0)*smoothstep(0.65, 0.05, r);\n  col = max(col, 0.0); col = col/(1.0 + col*0.60);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "apollonian", n: 26, name: "Apollonian",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "Circle inversion, eight times over. Every fold packs the plane into a smaller copy of itself, so the frame holds an infinite nest of tangent circles and there is no scale at which it stops.",
    params: [{"name":"Rate","def":0.3},{"name":"Zoom","def":0.4},{"name":"Gild","def":0.45}],
    reacts: [["sub","Tightens the packing"],["low","How hard each fold contracts"],["pres","Weight of the ring trap"],["sib","Softness of the ring trap"],["air","Sharpness of the line trap — how fine the gasket draws"],["voice","Lights the whole gasket from inside and gilds one trap"],["hit","Strikes the tangent points white"]],
    body: "// APOLLONIAN — circle inversion, eight times over. Every fold packs the plane\n// into a smaller copy of itself, so the picture contains an infinite nest of\n// tangent circles and there is no scale at which it stops. Depth of the fold is\n// what the audio moves, which means the mix changes how far down you can see.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y * (1.55 - iParam1*0.55);\n  float t = iTime*(0.14 + iParam0*0.32);\n\n  p = rot(t*0.13) * p;\n  float s = 1.10 + comp(a.low, 0.30)*0.28 + comp(a.sub, 0.35)*0.10;   // packing tightness\n  float scale = 1.0;\n  float trapA = 1e9, trapB = 1e9;\n  float depth = 0.0;\n\n  for (int i = 0; i < 8; i++){\n    p = -1.0 + 2.0*fract(0.5*p + 0.5);          // fold into the unit cell\n    float r2 = dot(p, p) + 1e-4;\n    float kk = s/r2;\n    p *= kk; scale *= kk;\n    // Orbit traps: what the fold happened to pass near on the way down. This is\n    // what puts structure inside the gasket instead of flat colour.\n    trapA = min(trapA, abs(p.x + p.y*0.4)/scale);\n    trapB = min(trapB, (abs(length(p) - 0.90))/scale);\n    depth += 1.0/scale;\n  }\n\n  float d1 = abs(p.y)/scale;\n  float d2 = abs(p.x)/scale;\n  float d  = min(d1, d2);\n  float w  = 2.0/iResolution.y;\n\n  // The gasket itself: thin bright curves where the fold lands on the trap.\n  float lineA = exp(-trapA*(110.0 - a.air*45.0));\n  float lineB = exp(-trapB*(70.0  - a.sib*28.0));\n  float edge  = smoothstep(w*3.5, 0.0, d);\n\n  float hue = fract(log(scale)*0.16 + t*0.04 + a.pres*0.20);\n  vec3 c1 = ramp(vec3(0.10,0.05,0.55), vec3(0.10,0.85,0.90), vec3(1.00,0.30,0.60), hue);\n  vec3 c2 = ramp(vec3(1.00,0.55,0.10), vec3(0.95,0.15,0.65), vec3(0.45,0.20,1.00), fract(hue + 0.35));\n\n  vec3 col = vec3(0.014,0.012,0.030);\n  col += lineA * c1 * (0.35 + SPEC(0.01 + fract(hue)*0.55)*2.2);\n  col += lineB * c2 * (0.25 + a.pres*1.4);\n  col += edge  * vec3(1.0,0.96,0.90) * (0.10 + pu*0.9);\n  // A glow that follows how deep the fold went, so the nest reads as receding.\n  col += c1 * exp(-depth*0.55) * 0.16;\n\n  // Vocals light the whole gasket from inside and pick out one trap in gold.\n  col += a.voice * (1.1 + iParam2*1.1) * exp(-trapA*38.0) * vec3(1.00,0.86,0.40);\n  col *= 1.0 - smoothstep(0.55, 1.15, length((C - 0.5*iResolution.xy)/iResolution.y))*0.45;\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.55);\n  o = vec4(pow(col, vec3(0.86)), 1.0);\n}",
  },
  {
    id: "droste", n: 27, name: "Droste",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "The picture contains itself, rotated. Log-space is tiled with a sheared period — a whole number of turns per scale step — so the seam closes exactly and the recursion never shows a join.",
    params: [{"name":"Fall rate","def":0.3},{"name":"Turns","def":0.35},{"name":"Gild","def":0.45}],
    reacts: [["low","Weight of the frame around each copy"],["pres","How many pieces of ornament each copy carries"],["sib","The inner rules"],["air","The fine weave over the ornament"],["voice","Gilds exactly one turn of the staircase, and you watch it travel down"],["hit","A pulse out of the vanishing point"]],
    body: "// DROSTE — the picture contains itself, rotated. In log-polar coordinates a\n// scale step is a translation, so making the picture self-similar is only a\n// matter of tiling log-space; making it *twist* as it recurses is a matter of\n// tiling it with a sheared period instead of a square one. Shear by exactly one\n// full turn per scale step and the seam closes perfectly, which is the whole\n// trick and the reason it can recurse forever without a visible join.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.10 + iParam0*0.26);\n\n  float r = max(length(p), 1e-4);\n  float th = atan(p.y, p.x);\n\n  float turns = 1.0 + floor(iParam1*2.99);      // whole turns per scale step\n  float ratio = 2.15;                           // and how much bigger each step is\n  float L = log(ratio);\n\n  float x = log(r) - t*0.60;                    // the endless fall inward\n  float period = floor(x/L);                    // which copy we are standing in\n  float u = fract(x/L);                         // where inside that copy\n  // The shear. Because it is a whole number of turns per step, the angle wraps\n  // to itself at every boundary and there is no join.\n  float ang = mod(th - x*(TAU*turns/L), TAU);\n\n  // One drawing, made once, repeated by the tiling: a heavy frame, an inner\n  // rule, a band of ornament and a fine weave over it.\n  float border = smoothstep(0.085, 0.015, u) + smoothstep(0.915, 0.985, u);\n  float rule   = smoothstep(0.026, 0.0, abs(u - 0.20)) + smoothstep(0.026, 0.0, abs(u - 0.80));\n  float ornN   = 8.0 + floor(a.pres*10.0);\n  float orn    = pow(0.5 + 0.5*cos(ang*ornN + t*0.30), 5.0)\n               * smoothstep(0.24, 0.33, u) * smoothstep(0.76, 0.67, u);\n  float weave  = pow(0.5 + 0.5*sin(ang*ornN*2.0 + u*TAU*3.0), 8.0)\n               * smoothstep(0.18, 0.28, u) * smoothstep(0.82, 0.72, u);\n\n  // Each copy takes its own bin, so the recursion has rungs you can hear.\n  float band = fract(period*0.137 + 0.5);\n  float e = SPEC(0.006 + band*0.58);\n  vec3 tint = ramp(vec3(0.05,0.06,0.42), vec3(0.10,0.80,0.90), vec3(1.00,0.36,0.62),\n                   fract(band + a.pres*0.15 + t*0.02));\n  vec3 warm = ramp(vec3(1.00,0.60,0.12), vec3(1.00,0.90,0.45), vec3(0.55,1.00,0.85),\n                   fract(period*0.211 + 0.4));\n\n  vec3 col = vec3(0.010,0.011,0.026);\n  col += tint * (0.05 + pow(e, 1.2)*1.7) * (0.20 + 0.80*smoothstep(0.12, 0.88, u));\n  col += border * warm * (0.30 + comp(a.low, 0.30)*1.35);\n  col += rule   * vec3(1.00,0.96,0.88) * (0.18 + a.sib*1.30);\n  col += orn    * tint * (0.30 + a.pres*1.60) * 1.5;\n  col += weave  * vec3(0.75,0.92,1.00) * (0.05 + a.air*1.00);\n\n  // Everything drains toward the middle, and the middle is the light.\n  col *= 1.0 - smoothstep(0.04, 0.92, r)*0.42;\n  col += exp(-r*(8.0 - a.voice*3.5)) * (0.08 + a.voice*1.35) * vec3(1.00,0.90,0.66);\n\n  // Vocals gild exactly one turn of the staircase, and you watch it travel down.\n  float lit = smoothstep(0.40, 0.0, abs(fract(period*0.137 - t*0.07) - 0.5));\n  col += a.voice * (1.0 + iParam2*1.1) * lit * (border*1.7 + orn*1.3) * vec3(1.00,0.85,0.38);\n  col += pu * 0.26 * smoothstep(0.75, 0.05, r);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.60);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "julia-bloom", n: 28, name: "Julia Bloom",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "One Julia set, walked slowly around the cardioid so the flower opens and closes without repeating. Smooth iteration count for continuous contours, and two orbit traps to put filigree inside the bloom instead of flat fill.",
    params: [{"name":"Walk rate","def":0.3},{"name":"Zoom","def":0.4},{"name":"Gild","def":0.45}],
    reacts: [["sub","Pulls the parameter in — the flower closes"],["low","Pushes it out — the flower opens"],["pres","Rotates the contour palette; the ring trap inside the set"],["sib","The escape edge, and the filigree across both sides"],["air","The line trap inside the set"],["voice","Gilds one trap and raises a bloom at the centre"],["hit","Rings the trap white"]],
    body: "// JULIA BLOOM — one Julia set, walked slowly around the cardioid so the flower\n// opens and closes without ever repeating. Coloured by smooth iteration count so\n// the bands are continuous rather than stepped, and traced by two orbit traps,\n// which is what puts filigree inside the bloom instead of flat fill.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.055 + iParam0*0.13);\n\n  // The parameter path. Radius near 0.79 is where the interesting shapes live;\n  // the low end nudges it in and out, which opens and closes the whole flower.\n  float rad = 0.7885 + comp(a.low, 0.30)*0.020 - comp(a.sub, 0.35)*0.012;\n  vec2 c = rad*vec2(cos(t), sin(t));\n\n  vec2 z = rot(t*0.07) * p * (1.55 - iParam1*0.55);\n  float trapR = 1e9, trapL = 1e9;\n  float it = 0.0, z2 = 0.0;\n  int MAXI = iResolution.y > 1250.0 ? 44 : 64;\n\n  for (int i = 0; i < 64; i++){\n    if (i >= MAXI) break;\n    z = vec2(z.x*z.x - z.y*z.y, 2.0*z.x*z.y) + c;\n    z2 = dot(z, z);\n    trapR = min(trapR, abs(length(z) - 0.62));       // a ring trap\n    trapL = min(trapL, abs(z.x*0.7 + z.y*0.7));      // a line trap\n    if (z2 > 64.0) break;\n    it += 1.0;\n  }\n\n  vec3 col;\n  if (z2 > 64.0){\n    // Outside: smooth iteration, so the contours are continuous.\n    float mu = it - log2(max(log2(z2), 1.0)) + 4.0;\n    float u = fract(mu*0.075 - t*0.10 + a.pres*0.18);\n    col = ramp(vec3(0.010,0.015,0.130), vec3(0.92,0.16,0.60), vec3(1.00,0.90,0.42), u);\n    // Hard contour banding, so the escape structure is legible rather than a wash.\n    float bandf = fract(mu*0.75);\n    col *= 0.16 + 1.20*pow(abs(sin(mu*1.35)), 3.2);\n    col += smoothstep(0.06, 0.0, min(bandf, 1.0 - bandf)) * 0.35 * vec3(1.0,0.95,0.85);\n    // Each contour band carries its own slice of the spectrum.\n    col *= 0.30 + SPEC(0.008 + fract(mu*0.075)*0.58)*2.4;\n    // The escape edge, picked out thin and hot.\n    col += exp(-abs(mu - 3.0)*0.9) * (0.35 + a.sib*1.8) * vec3(1.00,0.95,0.86);\n  } else {\n    // Inside: the traps are the only structure there is, so they carry it.\n    col = vec3(0.012,0.008,0.030);\n    col += exp(-trapL*(26.0 - a.air*9.0)) * (0.25 + a.air*1.5) * vec3(0.30,0.95,1.00);\n    col += exp(-trapR*(20.0 - a.pres*7.0)) * (0.20 + a.pres*1.3) * vec3(1.00,0.35,0.75);\n  }\n\n  // Filigree runs across both sides, which is what stitches inside to outside.\n  col += exp(-trapR*(46.0 - a.sib*16.0)) * (0.18 + a.sib*1.3) * vec3(0.85,0.90,1.00);\n\n  // Vocals gild one trap and raise a bloom at the centre of the set.\n  col += a.voice * (1.0 + iParam2*1.1) * exp(-trapL*30.0) * vec3(1.00,0.84,0.36);\n  col += a.voice * 0.55 * exp(-length(p)*3.2) * vec3(1.00,0.90,0.62);\n  col += pu * 0.24 * exp(-trapR*14.0) * vec3(1.0);\n\n  col *= 1.0 - smoothstep(0.60, 1.20, length(p))*0.40;\n  col = max(col, 0.0); col = col/(1.0 + col*0.58);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "oil-wheel", n: 29, name: "Oil Wheel",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "The liquid light show: coloured oil and water pressed between two clock glasses on an overhead projector, boiling from the lamp heat. The light is behind, so the colours multiply rather than add.",
    params: [{"name":"Boil","def":0.35},{"name":"Blob size","def":0.4},{"name":"Lamp","def":0.45}],
    reacts: [["sub","Heat under the dish — the lamp swells"],["low","How hard the dish churns"],["pres","The gate around the projected circle"],["sib","The meniscus at every blob edge, and water beading between them"],["air","Thin-film colour on the very edge of the film"],["voice","Pushes a bloom of clear light through from behind the glass"],["hit","A surge in the lamp"]],
    body: "// OIL WHEEL — the liquid light show: coloured oil and water pressed between two\n// clock glasses on an overhead projector, boiling from the lamp heat. The light\n// is behind, so the colours multiply rather than add, and the meniscus of each\n// blob is a hard bright ring. Nothing here is additive glow; it is stained light.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.16 + iParam0*0.42);\n\n  // The lamp behind the glass: hot in the middle, falling off hard at the rim.\n  float lampR = length(p*vec2(0.92, 1.0));\n  vec3 lamp = mix(vec3(1.00,0.97,0.88), vec3(1.00,0.80,0.52), smoothstep(0.0, 0.9, lampR));\n  float hot = exp(-lampR*1.35) * (1.10 + comp(a.sub, 0.35)*0.55);\n  vec3 col = lamp * hot;\n\n  // Convection: the whole dish churns, and the low end drives the boil.\n  vec2 churn = vec2(fbm(p*1.6 + vec2(t*0.30, 0.0), OCT), fbm(p*1.6 + vec2(0.0, 41.0 - t*0.26), OCT)) - 0.5;\n  vec2 q = p + churn*(0.16 + comp(a.low, 0.30)*0.22);\n\n  // Nine blobs of dye. Each is transparent, so each multiplies the light through it.\n  for (int i = 0; i < 9; i++){\n    float fi = float(i);\n    float hh = h21(vec2(fi, 5.0));\n    float e  = SPEC(0.008 + fi*0.055);\n    vec2 c = 0.44*vec2(cos(t*(0.21 + hh*0.30) + fi*2.4), sin(t*(0.18 + hh*0.26) + fi*1.7));\n    // The blob is not round: surface tension plus boiling gives it lobes.\n    vec2 d = q - c;\n    float ang = atan(d.y, d.x);\n    float rad = (0.115 + hh*0.085 + e*(0.045 + iParam1*0.06))\n              * (1.0 + 0.22*sin(ang*(2.0 + floor(hh*4.0)) + t*0.7 + fi));\n    float dd = length(d);\n    // The film is thick in the middle of a blob and thin at its edge, so the\n    // dye is denser in the middle. Flat opacity is what made them cut-outs.\n    float inside = smoothstep(rad, rad - 0.014, dd) * (0.45 + 0.55*smoothstep(rad, rad*0.25, dd));\n\n    // Dye colour: the transmission spectrum of the blob, not a light colour.\n    vec3 dye = hh < 0.20 ? vec3(0.20,0.35,1.00)\n             : hh < 0.40 ? vec3(1.00,0.20,0.42)\n             : hh < 0.58 ? vec3(0.15,0.90,0.55)\n             : hh < 0.76 ? vec3(1.00,0.72,0.10)\n                         : vec3(0.72,0.25,1.00);\n    // Multiply: two blobs overlapping go darker and more saturated, as real dye does.\n    col *= mix(vec3(1.0), dye, inside*(0.72 + e*0.26));\n    // The meniscus: a hard bright ring where the film thins at the edge.\n    float rim = smoothstep(0.016, 0.0, abs(dd - rad));\n    col += rim * (0.25 + e*1.6 + a.sib*0.7) * mix(vec3(1.0), dye, 0.35);\n    // Thin-film colour on the very edge, which is where the interference is.\n    col += rim * (0.08 + a.air*0.55) * (0.5 + 0.5*cos(dd*160.0 + fi + vec3(0.0,2.1,4.2)));\n  }\n\n  // Water beading between the dye: fine bright cells all over the dish.\n  float beads = smoothstep(0.62, 0.90, fbm(q*11.0 + t*0.4, 2));\n  col += beads * (0.04 + a.sib*0.45) * vec3(1.00,0.96,0.90);\n\n  // Vocals push a bloom of clear light through from behind the glass.\n  col += a.voice * (0.9 + iParam2*1.0) * exp(-lampR*2.0) * vec3(1.00,0.92,0.72);\n  col *= 1.0 + pu*0.35;\n\n  // The clock glass has an edge, and the projector has a soft round gate.\n  col *= 1.0 - smoothstep(0.62, 0.98, lampR)*0.92;\n  col += smoothstep(0.020, 0.0, abs(lampR - 0.72)) * (0.10 + a.pres*0.45) * vec3(1.0,0.95,0.85);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.42);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "melt", n: 30, name: "Melt",
    series: "III", set: "dilate", setTitle: "Dilate",
    kit3d: false,
    line: "The frame dissolving. Four rounds of domain warping, each feeding the next, with a downward bias so the picture runs rather than swirls — and the three channels warped by different amounts, which is what separates the colour at the edges.",
    params: [{"name":"Rate","def":0.3},{"name":"Warp","def":0.35},{"name":"Pull","def":0.4}],
    reacts: [["sub","Gravity on the paint — how fast it runs"],["low","Depth of the warp"],["pres","How far the three channels come apart"],["sib","Rotation between warp rounds; the contours through the melt"],["air","Glow along the separation edges"],["voice","Pulls the melt back up into a bright cell and gilds its rim"],["hit","A white flash through the whole field"]],
    body: "// MELT — the frame dissolving. Four rounds of domain warping, each one feeding\n// the next, with a downward bias so the picture runs rather than swirls. The\n// three channels are warped by slightly different amounts, which is what makes\n// the colour separate at the edges the way it does when you stare too long.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.10 + iParam0*0.26);\n\n  float amt = 0.28 + comp(a.low, 0.30)*0.30 + iParam1*0.25;\n  float sag = 0.10 + comp(a.sub, 0.35)*0.26;          // gravity on the paint\n\n  // One warp chain, sampled at three slightly different depths for the channels.\n  vec3 v = vec3(0.0);\n  for (int ch = 0; ch < 3; ch++){\n    float sep = 1.0 + float(ch)*(0.035 + a.pres*0.075);\n    vec2 q = p*1.25;\n    for (int i = 0; i < 4; i++){\n      if (i >= OCT) break;\n      float fi = float(i);\n      vec2 w = vec2(fbm(q*1.35 + vec2(t*0.30 + fi, 11.0), 3),\n                    fbm(q*1.35 + vec2(41.0, -t*0.24 - fi), 3)) - 0.5;\n      q += w*amt*sep;\n      q.y -= sag*(0.5 + w.x)*0.28;                    // it runs downhill\n      q = rot(0.22 + a.sib*0.35)*q;\n    }\n    v[ch] = clamp((fbm(q*1.10 + t*0.06, OCT) - 0.34)*2.35, 0.0, 1.0);\n  }\n\n  // Saturated, and deliberately not a rainbow: three inks bleeding into each other.\n  // One ink carries the picture; the other two only bleed into its edges.\n  vec3 col = ramp(vec3(0.030,0.010,0.180), vec3(0.98,0.14,0.46), vec3(1.00,0.88,0.28), v.r);\n  col = mix(col, ramp(vec3(0.01,0.20,0.42), vec3(0.10,0.95,0.72), vec3(0.95,1.00,0.58), v.g),\n            smoothstep(0.30, 0.85, abs(v.g - v.r))*0.75);\n  col = mix(col, ramp(vec3(0.16,0.01,0.32), vec3(0.58,0.24,1.00), vec3(1.00,0.58,0.92), v.b),\n            smoothstep(0.32, 0.90, abs(v.b - v.g))*0.55);\n  col *= 0.35 + 0.90*smoothstep(0.05, 0.75, v.r);\n\n  // The separation itself, drawn: where the channels disagree, the edge glows.\n  float split = abs(v.r - v.b);\n  col += smoothstep(0.10, 0.45, split) * (0.20 + a.air*1.5) * vec3(0.55,0.95,1.00);\n  // Thin contours through the melt, so it has some structure to lose.\n  float f = v.r*18.0;\n  col += smoothstep(fwidth(f)*1.6, 0.0, abs(fract(f) - 0.5)) * (0.12 + a.sib*1.1) * vec3(1.0,0.95,0.85);\n\n  // Vocals pull the melt back up into a bright cell and gild its rim.\n  float cellR = length(p*vec2(1.0, 1.15));\n  float pull = a.voice * exp(-cellR*(3.2 - a.voice*1.2));\n  col = mix(col, mix(col, vec3(1.00,0.90,0.55), 0.75), pull*(0.55 + iParam2*0.45));\n  col += a.voice * 0.6 * smoothstep(0.10, 0.0, abs(cellR - 0.20 - a.voice*0.14)) * vec3(1.00,0.86,0.44);\n\n  col += pu*0.30*vec3(1.0)*smoothstep(0.9, 0.1, cellR);\n  col *= 1.0 - smoothstep(0.55, 1.20, length(p))*0.42;\n  col = max(col, 0.0); col = col/(1.0 + col*0.52);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "cymatic", n: 31, name: "Cymatic",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "A Chladni plate: sand on a disc driven at a frequency, collecting wherever the plate is not moving. Mode numbers come straight off the spectrum and are blended between neighbouring integers, so the pattern flows from one mode shape into the next instead of flickering.",
    params: [{"name":"Rate","def":0.3},{"name":"Plate size","def":0.4},{"name":"Backlight","def":0.4}],
    reacts: [["sub","Wavenumber of the first mode; the driver at the centre"],["low","Mode number of the first mode"],["pres","The second mode, and the rim of the plate"],["sib","The third mode"],["air","Grain size of the sand"],["voice","Drives a fourth, much higher mode and lights the plate from beneath"],["hit","Sand that has not settled yet hops"]],
    body: "// CYMATIC — a Chladni plate: sand on a disc driven at a frequency, collecting\n// wherever the plate is not moving. The mode numbers come straight off the\n// spectrum, and they are blended between neighbouring integers rather than\n// snapped, so the pattern flows from one mode shape into the next instead of\n// flickering. It is the one work in the library that is literally a picture of\n// the sound rather than a reaction to it.\nfloat besselish(float x, float m){\n  x = max(x, 0.75);\n  return cos(x - m*1.5708 - 0.7854)/sqrt(x);        // asymptotic J_m, exact enough for nodes\n}\nfloat plateMode(float r, float th, float m, float k){\n  return besselish(k*r, m)*cos(m*th);\n}\nfloat blendMode(float r, float th, float mf, float k){\n  float m0 = floor(mf);\n  return mix(plateMode(r, th, m0, k), plateMode(r, th, m0 + 1.0, k), fract(mf));\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.10 + iParam0*0.22);\n\n  float R = 0.40 + iParam1*0.08;\n  float r = length(p)/R, th = atan(p.y, p.x);\n\n  // Three driven modes. Mode number from the band, wavenumber from its energy.\n  float m1 = 2.0 + comp(a.low, 0.30)*6.0  + sin(t*0.21)*0.9;\n  float m2 = 5.0 + a.pres*9.0             + sin(t*0.13 + 2.0)*1.2;\n  float m3 = 9.0 + a.sib*11.0             + sin(t*0.09 + 4.0)*1.4;\n  float k1 = 7.5 + comp(a.sub, 0.35)*5.0;\n  float k2 = 13.0 + a.pres*7.0;\n  float k3 = 20.0 + a.air*10.0;\n\n  float u = blendMode(r, th, m1, k1)*(0.45 + comp(a.low,0.30)*0.85)\n          + blendMode(r, th, m2, k2)*(0.30 + a.pres*0.80)\n          + blendMode(r, th, m3, k3)*(0.18 + a.sib*0.65);\n  // Vocals drive a fourth, much higher mode on top of the others.\n  u += blendMode(r, th, 14.0 + a.voice*10.0, 30.0 + a.voice*14.0) * a.voice * 1.3;\n\n  // The plate: brushed metal, lit from one side.\n  float brush = 0.85 + 0.30*fbm(p*vec2(140.0, 5.0), 2);\n  vec3 plate = mix(vec3(0.055,0.058,0.070), vec3(0.155,0.160,0.185), brush);\n  plate *= 0.72 + 0.55*smoothstep(0.9, -0.4, p.y - p.x*0.4);\n  vec3 col = plate;\n\n  // Sand collects on the nodes. Its width is set by the derivative, so the lines\n  // stay one grain wide however tight the mode gets.\n  float w = fwidth(u)*1.35 + 1e-4;\n  float sand = smoothstep(w*3.4, w*0.6, abs(u));\n  // Grain: the sand is granular, not a drawn line.\n  float grain = 0.55 + 0.75*h21(floor(p*(340.0 + a.air*140.0)));\n  vec3 sandCol = mix(vec3(0.86,0.80,0.66), vec3(1.00,0.96,0.86), grain*0.6);\n  sandCol = mix(sandCol, vec3(1.00,0.86,0.42), a.voice*0.75);\n  col = mix(col, sandCol*grain, sand*0.96);\n  // Sand that has not settled yet, hopping on transients.\n  col += sand*(1.0 - sand)*3.0 * pu * 0.55 * vec3(1.00,0.96,0.86);\n\n  // The rim of the plate, and the table it sits on.\n  float disc = smoothstep(1.005, 0.995, r);\n  col = mix(vec3(0.014,0.014,0.020), col, disc);\n  col += smoothstep(0.012, 0.0, abs(r - 1.0)) * (0.20 + a.pres*0.85) * vec3(0.80,0.84,0.95);\n  // The driver at the centre.\n  col += exp(-r*R*22.0) * (0.10 + comp(a.sub,0.35)*0.90) * vec3(0.70,0.85,1.00) * disc;\n\n  // Vocals light the plate from beneath, through the sand.\n  col += a.voice * (0.55 + iParam2*0.75) * disc * smoothstep(1.0, 0.0, r) * vec3(1.00,0.82,0.38) * (0.25 + sand*0.9);\n\n  col *= 1.0 - smoothstep(0.55, 1.25, length(p))*0.45;\n  col = max(col, 0.0); col = col/(1.0 + col*0.45);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "hypertile", n: 32, name: "Hypertile",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "A tiling of the hyperbolic plane in the Poincaré disk. Every tile is the same size in its own geometry; it is only the flat screen that makes them shrink toward the rim, so the regress at the edge is exact rather than faked.",
    params: [{"name":"Drift","def":0.3},{"name":"Disk size","def":0.35},{"name":"Tiling","def":0.4}],
    reacts: [["sub","The light at the centre of the disk"],["low","The geodesics each tile was reflected across"],["pres","Rotates the palette; the circle at infinity"],["sib","The tile edges"],["air","A rosette at the centre of every tile"],["voice","Gilds one ring of the regress and opens the centre"],["hit","A pulse through the whole disk"]],
    body: "// HYPERTILE — a {p,q} tiling of the hyperbolic plane, drawn in the Poincaré\n// disk. Every tile is the same size in its own geometry; it is only our flat\n// screen that makes them shrink toward the rim, so the regress at the edge is\n// infinite and exact rather than faked. Built by folding each pixel back to the\n// fundamental triangle through one rotation and one circle inversion, and\n// counting how many folds it took.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.06 + iParam0*0.16);\n\n  float D = 0.46 + iParam1*0.08;\n  vec2 z = p/D;\n  float rr = length(z);\n  vec3 col = vec3(0.010,0.012,0.026);\n\n  if (rr < 1.0){\n    // A slow hyperbolic translation, so the tiling drifts without ever changing.\n    float sh = tanh(t*0.16);\n    z = (z + vec2(sh, 0.0)) / (1.0 + sh*z.x);\n    z = rot(t*0.05) * z;\n\n    // {P,Q}. Hyperbolic exactly when 1/P + 1/Q < 1/2, and this is the circle\n    // that makes it so.\n    float P = 5.0 + floor(iParam2*3.0);\n    float Q = 4.0;\n    float cx = cos(3.14159265/P)/sin(3.14159265/Q);\n    float cr = sqrt(max(cx*cx - 1.0, 1e-4));\n\n    float folds = 0.0;\n    float sect = TAU/P;\n    for (int i = 0; i < 22; i++){\n      // Fold into one wedge of the P-fold rotation.\n      float ang = atan(z.y, z.x);\n      ang = mod(ang + sect*0.5, sect) - sect*0.5;\n      z = length(z)*vec2(cos(ang), sin(ang));\n      // Invert in the mirror circle, if we are inside it.\n      vec2 d = z - vec2(cx, 0.0);\n      float dd = dot(d, d);\n      if (dd > cr*cr) break;\n      z = vec2(cx, 0.0) + d*(cr*cr/dd);\n      folds += 1.0;\n    }\n\n    // Inside the fundamental domain: draw the tile.\n    float tr = length(z - vec2(cx, 0.0));\n    float edge = abs(tr - cr);\n    float spoke = abs(z.y);\n    float w = fwidth(edge)*1.4 + 1e-4;\n\n    // Every ring of tiles takes its own slice of the spectrum.\n    float band = fract(folds/7.0);\n    float e = SPEC(0.006 + band*0.58);\n    vec3 tint = ramp(vec3(0.06,0.10,0.55), vec3(0.10,0.80,0.85), vec3(1.00,0.42,0.68),\n                     fract(band + a.pres*0.16 + t*0.03));\n\n    // The tile face, its ribs, and the geodesic it was reflected across.\n    col = tint * (0.05 + pow(e, 1.25)*1.5) * (0.55 + 0.45*sin(folds*1.7));\n    col += smoothstep(w*3.0, 0.0, edge) * (0.30 + a.sib*1.5) * vec3(1.00,0.96,0.90);\n    col += smoothstep(w*2.2, 0.0, spoke) * (0.10 + comp(a.low,0.30)*0.90) * tint * 1.4;\n    // A rosette at the centre of every tile.\n    float rose = pow(0.5 + 0.5*cos(atan(z.y, z.x - cx)*P + t*0.4), 7.0);\n    col += rose * smoothstep(cr*0.85, cr*0.15, tr) * (0.10 + a.air*1.1) * vec3(0.85,0.95,1.00);\n\n    // The regress at the rim: it should go dark and infinitely fine, not alias.\n    col *= smoothstep(1.0, 0.86, rr);\n    col *= 0.35 + 0.85*exp(-folds*0.10);\n    // The light at the centre of the disk.\n    col += exp(-rr*rr*5.0) * (0.06 + comp(a.sub, 0.35)*0.35) * vec3(0.75,0.88,1.00);\n\n    // Vocals gild one ring of the regress and open the centre.\n    col += a.voice * (0.9 + iParam2*0.6) * smoothstep(0.55, 0.95, fract(folds/4.0 - t*0.10))\n           * smoothstep(w*4.0, 0.0, edge) * vec3(1.00,0.86,0.40) * 2.0;\n    col += a.voice * 0.55 * exp(-rr*rr*3.0) * vec3(1.00,0.90,0.64);\n    col += pu * 0.22 * smoothstep(1.0, 0.2, rr);\n  }\n  // The rim of the disk itself: the circle at infinity.\n  col += smoothstep(0.010, 0.0, abs(rr - 1.0)) * (0.10 + a.pres*0.55) * vec3(0.60,0.80,1.00);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.55);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "yantra", n: 33, name: "Yantra",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "Nine interlocking triangles about a point — four rising, five descending — with two rings of lotus petals, three circles and a square enclosure with four gates. Line work on a dark ground, lit from behind.",
    params: [{"name":"Drift","def":0.3},{"name":"Line weight","def":0.35},{"name":"Backlight","def":0.4}],
    reacts: [["sub","The light behind the whole figure"],["low","The eight-petal ring, and the glow at the bindu"],["pres","The three circles, and the bindu itself"],["sib","The sixteen-petal ring"],["air","The enclosure and its gates"],["voice","Raises the light behind the figure and gilds the outer petals"],["hit","A pulse out of the bindu"]],
    body: "// YANTRA — nine interlocking triangles about a point, two rings of lotus petals,\n// three circles and a square enclosure with four gates. Four triangles rise and\n// five descend; where they cross they cut the small triangles the figure is\n// actually counted in. Drawn as line work on a dark ground with the light coming\n// through from behind, so it reads as a diagram lit rather than a diagram\n// coloured, and each family of the figure answers to a different band.\nfloat triSDF(vec2 q, float R, float yo, float flip){\n  q.y = (q.y - yo)*flip;\n  // Equilateral, apex up in the folded frame. R is the circumradius.\n  float k = R*0.5;                                   // inradius\n  return max(max(dot(q, vec2( 0.86603, 0.5)), dot(q, vec2(-0.86603, 0.5))), -q.y) - k;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.05 + iParam0*0.13);\n  float r = length(p), th = atan(p.y, p.x);\n  float LW = 0.0020 + iParam1*0.0020;\n\n  vec3 col = mix(vec3(0.038,0.032,0.080), vec3(0.008,0.007,0.024), smoothstep(0.05, 0.95, r));\n  col += exp(-r*3.0) * (0.10 + comp(a.sub, 0.35)*0.40) * vec3(0.45,0.35,0.85);\n\n  vec3 GOLD = vec3(1.00,0.82,0.36), ROSE = vec3(1.00,0.45,0.42), PALE = vec3(0.80,0.90,1.00);\n\n  // Four rising triangles, spanning most of the figure.\n  for (int i = 0; i < 4; i++){\n    float fi = float(i);\n    float R  = 0.335 - fi*0.062;\n    float yo = -0.075 + fi*0.036;\n    float e  = SPEC(0.008 + fi*0.070);\n    float d  = triSDF(p, R*(1.0 + e*0.025), yo, 1.0);\n    col += smoothstep(LW*1.8, 0.0, abs(d)) * GOLD * (0.25 + e*1.8);\n    col += smoothstep(0.0, -0.06, d) * GOLD * (0.008 + e*0.045);\n  }\n  // Five descending triangles, offset the other way so the crossings interlock.\n  for (int i = 0; i < 5; i++){\n    float fi = float(i);\n    float R  = 0.350 - fi*0.058;\n    float yo = 0.082 - fi*0.034;\n    float e  = SPEC(0.012 + fi*0.062);\n    float d  = triSDF(p, R*(1.0 + e*0.025), yo, -1.0);\n    col += smoothstep(LW*1.8, 0.0, abs(d)) * ROSE * (0.25 + e*1.8);\n    col += smoothstep(0.0, -0.06, d) * ROSE * (0.008 + e*0.045);\n  }\n\n  // Two rings of lotus petals: eight, then sixteen.\n  for (int k = 0; k < 2; k++){\n    float N   = (k == 0) ? 8.0 : 16.0;\n    float R0  = (k == 0) ? 0.400 : 0.470;\n    float amp = (k == 0) ? 0.048 : 0.040;\n    float off = (k == 0) ? 0.0 : 0.196;\n    float e   = (k == 0) ? comp(a.low, 0.30) : a.sib;\n    float petal = R0 + amp*pow(abs(cos(th*N*0.5 + off)), 0.65);\n    col += smoothstep(LW*1.7, 0.0, abs(r - petal)) * mix(ROSE, GOLD, float(k)) * (0.20 + e*1.6);\n  }\n\n  // Three circles.\n  for (int k = 0; k < 3; k++){\n    col += smoothstep(LW*1.5, 0.0, abs(r - (0.355 + float(k)*0.017))) * PALE * (0.12 + a.pres*0.85);\n  }\n\n  // The enclosure: three squares with a gate on each side.\n  vec2 q = abs(p);\n  float sq = max(q.x, q.y);\n  for (int k = 0; k < 3; k++){\n    float S = 0.560 + float(k)*0.028;\n    float gate = smoothstep(0.10, 0.13, min(q.x, q.y));\n    col += smoothstep(LW*1.5, 0.0, abs(sq - S)) * mix(gate, 1.0, 0.35) * GOLD * (0.10 + a.air*0.85);\n  }\n\n  // The bindu, and the point is that it never moves.\n  col += smoothstep(0.011, 0.005, r) * (0.60 + a.pres*1.4) * vec3(1.00,0.94,0.80);\n  col += exp(-r*15.0) * (0.10 + comp(a.low, 0.30)*0.55) * GOLD;\n\n  // Vocals raise the light behind the whole figure and gild the outer petals.\n  col += a.voice * (0.75 + iParam2*0.85) * exp(-r*1.9) * vec3(1.00,0.86,0.50);\n  col += a.voice * 1.2 * smoothstep(LW*3.2, 0.0,\n           abs(r - 0.470 - 0.040*pow(abs(cos(th*8.0 + 0.196)), 0.65))) * vec3(1.00,0.90,0.55);\n  col += pu * 0.20 * exp(-r*4.0) * vec3(1.0);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "phosphene", n: 34, name: "Phosphene",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "What you see with your eyes closed facing the sun: a red-orange field with your own retinal vessels printed across it, floaters drifting down through it, and a bloom in the middle that answers to your pulse.",
    params: [{"name":"Drift","def":0.3},{"name":"Bloom","def":0.4},{"name":"Corona","def":0.4}],
    reacts: [["sub","The pulse behind the field"],["low","How dark the vessels print"],["pres","How far the light reaches out from the fovea"],["sib","Light catching on the vessel walls"],["air","The floaters, and their cold edges"],["voice","The core goes white and a cold corona rings it"],["hit","Blue-green afterimage speckle — the complementary colour"]],
    body: "// PHOSPHENE — what you see with your eyes closed facing the sun: a red-orange\n// field with your own retinal vessels printed across it, floaters drifting\n// downward through it, and a slow bloom in the middle that answers to your\n// pulse. The one work in the library that is a picture of the inside of the eye\n// rather than of anything outside it.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.09 + iParam0*0.20);\n  float r = length(p*vec2(0.94, 1.0));\n\n  // The field: blood-red at the edge, orange through the middle, white at the\n  // fovea. The sub-bass is the pulse behind it.\n  float pulse = 0.90 + comp(a.sub, 0.35)*0.30;\n  float v = clamp(1.25 - r*(1.55 - a.pres*0.35), 0.0, 1.0)*pulse;\n  v *= 0.88 + 0.24*fbm(p*1.4 + t*0.15, OCT);\n  vec3 col = ramp(vec3(0.135,0.006,0.012), vec3(0.860,0.130,0.020), vec3(1.000,0.780,0.400), v);\n  col = mix(col, vec3(1.00,0.96,0.88), smoothstep(0.72, 1.0, v)*0.75);\n\n  // Retinal vessels: a ridged field, branching, and they sit ON the light rather\n  // than in it — you see them as shadows, which is why they subtract.\n  vec2 vp = p*2.3 + vec2(0.10*sin(t*0.5), 0.07*cos(t*0.43));   // they drift with the eye\n  float n1 = fbm(vp*1.6, OCT), n2 = fbm(vp*3.3 + 9.0, 3);\n  float ridge = 1.0 - abs(n1*2.0 - 1.0);\n  float ridge2 = 1.0 - abs(n2*2.0 - 1.0);\n  float vessels = pow(max(ridge, 0.0), 5.0)*0.75 + pow(max(ridge2, 0.0), 7.0)*0.25;\n  vessels *= smoothstep(0.06, 0.75, r);                        // faint at the fovea\n  col *= 1.0 - vessels*(0.42 + comp(a.low, 0.30)*0.35);\n  col += vessels*vessels*(0.05 + a.sib*0.35)*vec3(1.00,0.60,0.35);\n\n  // Floaters: translucent, they lag the eye and then settle.\n  for (int i = 0; i < 5; i++){\n    float fi = float(i);\n    float hh = h21(vec2(fi, 7.0));\n    vec2 c = vec2(-0.55 + hh*1.10 + 0.10*sin(t*0.25 + fi),\n                  fract(hh + t*0.045)*1.30 - 0.65);\n    vec2 d = (p - c)*rot(fi*1.7);\n    // A comma, not a disc: floaters are strands of collapsed vitreous.\n    float body = smoothstep(0.055 + hh*0.030, 0.0,\n                            length(d*vec2(1.0, 3.4 + hh*2.0)) + 0.030*sin(d.x*30.0));\n    col *= 1.0 - body*(0.18 + a.air*0.20);\n    col += body*(0.03 + a.air*0.30)*vec3(0.55,0.75,1.00);      // the cold edge on a floater\n  }\n\n  // The bloom at the fovea.\n  col += exp(-r*(5.2 - a.pres*1.6)) * (0.12 + a.pres*0.85) * vec3(1.00,0.88,0.60);\n\n  // Transients print blue-green afterimage speckle — the complementary colour,\n  // which is what actually happens when the red channel saturates.\n  float sp = step(0.9955 - pu*0.010, h21(floor(p*90.0) + floor(iTime*12.0)));\n  col += sp * (0.30 + pu*1.6) * vec3(0.20,0.95,0.70) * smoothstep(1.0, 0.1, r);\n\n  // Vocals open the eye: the core goes white and a cold corona rings it.\n  col = mix(col, vec3(1.00,0.98,0.94), a.voice * exp(-r*(3.4 - a.voice*1.2)) * (0.55 + iParam1*0.45));\n  col += a.voice * (0.55 + iParam2*0.55) * smoothstep(0.10, 0.0, abs(r - 0.26 - a.voice*0.16))\n         * vec3(0.35,0.90,0.95);\n\n  // The lid closes the frame, so there is no rectangle in it.\n  col *= 1.0 - smoothstep(0.62, 1.15, r)*0.95;\n  col *= 0.985 + 0.015*h21(floor(C) + floor(iTime*40.0));       // retinal noise\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "deep-field", n: 35, name: "Deep Field",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "A long exposure of nothing in particular. Three nebula layers in three domains so the gas has depth, dust lanes that subtract rather than add, stars in three densities, and one spiral galaxy far enough away to be small.",
    params: [{"name":"Drift","def":0.3},{"name":"Supernova","def":0.4}],
    reacts: [["sub","The third nebula layer, and the deepest gas"],["low","The blue layer"],["pres","The magenta layer, and the galaxy core"],["sib","Thickness of the dust lanes"],["air","Star magnitude and twinkle"],["voice","A supernova, with the four-point cross a real one shows on a scope"],["hit","A cosmic ray hit on the sensor"]],
    body: "// DEEP FIELD — a long exposure of nothing in particular. Three nebula layers in\n// three different domains so the gas has depth rather than being one cloud, dust\n// lanes that subtract rather than add, a star field in three densities, and one\n// spiral galaxy far enough away to be small.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.030 + iParam0*0.070);\n\n  vec3 col = vec3(0.006,0.007,0.016);\n\n  // Nebula. Each layer drifts at its own rate, which is what gives parallax to\n  // something that has no geometry at all.\n  vec3 tint1 = vec3(0.85,0.20,0.55), tint2 = vec3(0.15,0.45,1.00), tint3 = vec3(1.00,0.55,0.18);\n  vec2 q1 = p*1.15 + vec2(t*0.20, t*0.05);\n  vec2 q2 = p*1.85 - vec2(t*0.14, t*0.03) + 17.0;\n  vec2 q3 = p*0.80 + vec2(t*0.09, -t*0.06) + 41.0;\n  float w1 = fbm(q1 + fbm(q1*0.7, 3)*1.3, OCT);\n  float w2 = fbm(q2 + fbm(q2*0.6, 3)*1.1, OCT);\n  float w3 = fbm(q3, 3);\n  col += tint1 * pow(smoothstep(0.42, 0.92, w1), 1.6) * (0.18 + a.pres*1.10);\n  col += tint2 * pow(smoothstep(0.46, 0.95, w2), 1.8) * (0.14 + comp(a.low, 0.30)*0.95);\n  col += tint3 * pow(smoothstep(0.52, 0.98, w3), 2.2) * (0.10 + comp(a.sub, 0.35)*0.70);\n  // Dust: the dark lanes are what make a nebula read as matter and not as fog.\n  col *= 1.0 - smoothstep(0.40, 0.72, fbm(p*2.4 + 71.0, OCT))*(0.55 + a.sib*0.20);\n\n  // A spiral galaxy, small, off to one side.\n  vec2 gp = (p - vec2(-0.52, 0.22))*vec2(1.0, 2.1);\n  gp = rot(0.6)*gp;\n  float gr = length(gp), ga = atan(gp.y, gp.x);\n  float arms = 0.5 + 0.5*sin(ga*2.0 - log(max(gr, 0.01))*5.0 + t*0.9);\n  float gal = exp(-gr*13.0)*1.4 + pow(arms, 2.6)*exp(-gr*7.0)*0.85;\n  col += gal * mix(vec3(1.00,0.90,0.72), vec3(0.65,0.78,1.00), smoothstep(0.0, 0.12, gr))\n             * (0.30 + a.pres*1.0);\n\n  // Stars, three densities. The brightest get diffraction spikes, which is the\n  // detail that makes the frame read as a photograph.\n  for (int i = 0; i < 3; i++){\n    float fi = float(i);\n    float sc = 46.0 + fi*72.0;\n    vec2 sg = p*sc; vec2 si = floor(sg), sf = fract(sg) - 0.5;\n    float hh = h21(si + fi*13.0);\n    if (hh < 0.978 - fi*0.008) continue;\n    vec2 jit = (vec2(h21(si + 3.0), h21(si + 9.0)) - 0.5)*0.7;\n    float d = length(sf - jit);\n    float tw = 0.55 + 0.65*h21(si + floor(iTime*2.2 + hh*10.0));\n    float mag = (1.0 - fi*0.30) * (0.35 + a.air*1.5*tw);\n    col += smoothstep(0.055, 0.0, d) * mag * mix(vec3(1.0,0.92,0.80), vec3(0.78,0.86,1.00), hh);\n    if (i == 0){\n      vec2 e = (sf - jit)/max(0.001, 1.0);\n      float spike = exp(-abs(e.x)*90.0)*exp(-abs(e.y)*7.0) + exp(-abs(e.y)*90.0)*exp(-abs(e.x)*7.0);\n      col += spike * mag * 0.45 * vec3(0.92,0.95,1.00);\n    }\n  }\n\n  // Vocals: a supernova, with the four-point cross a real one shows on a scope.\n  vec2 sn = vec2(0.34, -0.16);\n  vec2 sd = p - sn;\n  float sr = length(sd);\n  float cross = exp(-abs(sd.x)*(120.0 - a.voice*60.0))*exp(-abs(sd.y)*3.0)\n              + exp(-abs(sd.y)*(120.0 - a.voice*60.0))*exp(-abs(sd.x)*3.0);\n  col += a.voice * (1.5 + iParam1*1.5) * (exp(-sr*(9.0 - a.voice*4.0)) + cross*0.75)\n       * vec3(1.00,0.94,0.80);\n  col += a.voice * 0.45 * smoothstep(0.14, 0.0, abs(sr - 0.10 - a.voice*0.12)) * vec3(0.60,0.85,1.00);\n\n  // A transient is a cosmic ray hit on the sensor, which is exactly what it\n  // looks like on a real long exposure.\n  vec2 cg = floor(p*130.0 + floor(iTime*24.0));\n  col += step(0.9985 - pu*0.006, h21(cg)) * pu * 2.2 * vec3(1.0);\n\n  col *= 1.0 - smoothstep(0.60, 1.30, length(p))*0.35;\n  col = max(col, 0.0); col = col/(1.0 + col*0.42);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "abyssal", n: 36, name: "Abyssal",
    series: "III", set: "numen", setTitle: "Numen",
    kit3d: false,
    line: "Below the last light. Everything in frame makes its own: three medusae pulsing on the low end, a siphonophore whose chain of lights runs on the spectrum, plankton that fire on transients, and marine snow falling through all of it.",
    params: [{"name":"Drift","def":0.3},{"name":"Bell size","def":0.35},{"name":"Chain","def":0.4}],
    reacts: [["sub","The bell pulse — the animal swimming"],["low","Sway in the tentacles, and the water itself"],["pres","The glow through each bell"],["sib","The tentacles"],["air","Marine snow"],["voice","The whole chain runs bright and a great bell opens beneath it"],["hit","Plankton fire"]],
    body: "// ABYSSAL — below the last light. Everything in frame makes its own light, so\n// the ground is genuinely black and the picture is built only out of what is\n// glowing: three medusae pulsing on the low end, a siphonophore whose chain of\n// lights runs on the spectrum, plankton that fire on transients, and marine snow\n// falling through all of it.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.09 + iParam0*0.20);\n\n  vec3 col = vec3(0.004,0.008,0.014);\n  // The faintest suggestion of water, so the black is water and not a void.\n  col += vec3(0.010,0.030,0.048) * fbm(p*1.3 + vec2(t*0.05, -t*0.03), 3) * (0.25 + comp(a.low,0.30)*0.55);\n\n  // Marine snow, falling and slowly turning.\n  for (int k = 0; k < 2; k++){\n    float fk = float(k);\n    float sc = 40.0 + fk*46.0;\n    vec2 sg = p*sc + vec2(sin(p.y*2.0 + t)*0.6, t*(1.4 + fk*1.1));\n    vec2 si = floor(sg), sf = fract(sg) - 0.5;\n    float hh = h21(si + fk*17.0);\n    if (hh > 0.965){\n      float d = length(sf - (vec2(h21(si+3.0), h21(si+7.0)) - 0.5)*0.6);\n      col += smoothstep(0.10, 0.0, d) * (0.06 + a.air*0.55) * vec3(0.62,0.80,0.92) * (1.0 - fk*0.4);\n    }\n  }\n\n  // Three medusae. The bell pulses on the sub — that is the animal swimming.\n  for (int i = 0; i < 3; i++){\n    float fi = float(i);\n    float hh = h21(vec2(fi, 11.0));\n    float ph = t*(0.55 + hh*0.30) + fi*2.1;\n    float pump = 0.5 + 0.5*sin(ph);                       // the contraction\n    pump = mix(pump, pump*pump, 0.5);\n    float sq = 1.0 + (pump - 0.5)*(0.42 + comp(a.sub, 0.35)*0.75);\n\n    vec2 c = vec2(-0.62 + fi*0.58 + 0.09*sin(t*0.31 + fi*2.0),\n                  -0.34 + 0.30*fract(hh + t*0.035)*1.6 - 0.15);\n    vec2 d = p - c;\n    float R = (0.085 + hh*0.055) * (1.0 + iParam1*0.5);\n\n    // Bell: a squashed half-disc, open at the bottom.\n    vec2 bq = vec2(d.x/(R*sq), d.y/(R/sq*0.82));\n    float br = length(bq);\n    float bell = smoothstep(1.0, 0.86, br) * smoothstep(-0.15, 0.10, d.y + R*0.10);\n    vec3 glow = mix(vec3(0.20,0.75,1.00), vec3(0.75,0.35,1.00), fract(hh*3.0));\n    col += bell * (0.05 + a.pres*0.42) * glow * 0.55;\n    // The rim of the bell is where the light organs are.\n    col += smoothstep(0.10, 0.0, abs(br - 0.96)) * smoothstep(-0.12, 0.12, d.y)\n         * (0.25 + a.pres*1.7) * glow;\n\n    // Tentacles: they trail, and they lag the pulse.\n    for (int j = 0; j < 5; j++){\n      float fj = float(j);\n      float xo = (fj - 2.0)*0.30*R;\n      float sway = sin(d.y*9.0 - ph*1.6 + fj)*0.035*(1.0 + comp(a.low,0.30));\n      float tx = abs(d.x - xo - sway);\n      float below = smoothstep(0.0, -0.02, d.y);\n      float len = R*(2.6 + hh*2.4);\n      float strand = smoothstep(0.006, 0.0, tx) * below * smoothstep(-len, -len*0.15, d.y);\n      col += strand * (0.10 + a.sib*0.95) * glow * 0.85;\n    }\n  }\n\n  // The siphonophore: one long chain, and each light on it is one bin of the\n  // spectrum, so the animal is lit end to end by the mix.\n  for (int i = 0; i < 22; i++){\n    float fi = float(i);\n    float u = fi/21.0;\n    vec2 c = vec2(mix(-0.95, 0.95, u) , 0.30 + 0.13*sin(u*7.0 + t*0.5) - u*0.10);\n    float e = SPEC(0.006 + u*0.56);\n    float d = length(p - c);\n    col += smoothstep(0.030 + e*0.020, 0.0, d) * (0.10 + pow(e, 1.2)*2.2)\n         * mix(vec3(0.30,1.00,0.80), vec3(0.35,0.60,1.00), u);\n    col += exp(-d*22.0) * (0.02 + e*0.35) * vec3(0.25,0.85,0.85);\n  }\n\n  // Plankton fire when something strikes the water.\n  vec2 pg = floor(p*(70.0 + pu*20.0) + floor(iTime*9.0));\n  col += step(0.9955 - pu*0.014, h21(pg)) * (0.25 + pu*2.4) * vec3(0.45,0.95,1.00);\n\n  // Vocals: the whole chain runs bright, and one great bell opens beneath it.\n  col += a.voice * (0.7 + iParam2*0.8) * exp(-abs(p.y - 0.30 + 0.10*p.x)*7.0) * vec3(0.45,1.00,0.85);\n  float gr = length((p - vec2(0.10, -0.30))*vec2(1.0, 1.45));\n  col += a.voice * 1.1 * smoothstep(0.34, 0.30, gr) * smoothstep(0.24, 0.33, gr) * vec3(0.85,0.55,1.00);\n  col += a.voice * 0.35 * exp(-gr*3.4) * vec3(0.55,0.40,0.95);\n\n  col *= 1.0 - smoothstep(0.55, 1.25, length(p))*0.45;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "cascade", n: 37, name: "Cascade",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "A Rube Goldberg machine drawn as a patent elevation. Seven stages on one loop: a ball rolls, dominoes topple, a lever throws, a bucket falls, a hammer swings, a bell rings, a lamp lights. The arc and the pendulum are exact; the chain is choreographed, which is what a Rube Goldberg machine is.",
    params: [{"name":"Rate","def":0.35},{"name":"Line weight","def":0.35}],
    reacts: [["sub","Size of the rolling ball"],["low","Brightness of everything in motion"],["pres","The standing frame"],["sib","Dominoes light as they go over"],["voice","Lights the lamp at the end of the chain"],["hit","Rattles the whole apparatus"]],
    body: "// CASCADE — a Rube Goldberg machine drawn as a patent elevation. The whole\n// apparatus is on screen at all times, standing on its own frame; only the\n// moving parts move. Seven stages on one loop, left to right: a hopper releases\n// a ball, it rolls, it topples a row of dominoes, the last one trips a lever,\n// the lever throws a second ball on a ballistic arc, the ball drops into a\n// bucket, the bucket falls and releases a hammer, the hammer strikes the bell,\n// and the bell lights the lamp.\n//\n// The arc and the hammer are exact — projectile motion and a damped pendulum,\n// both closed form. The chain is choreographed, which is what a Rube Goldberg\n// machine is: nobody simulates one, they adjust the timing until it works.\nfloat seg(vec2 p, vec2 a, vec2 b){\n  vec2 pa = p - a, ba = b - a;\n  return length(pa - ba*clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0));\n}\nfloat boxd(vec2 p, vec2 b){ vec2 d = abs(p) - b; return min(max(d.x,d.y),0.0) + length(max(d,0.0)); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.55);\n\n  float LW = 0.0030 + iParam1*0.0024;\n  float aa = 1.6/iResolution.y;\n  vec3 BG = vec3(0.030,0.048,0.100), INK = vec3(0.66,0.82,1.00),\n       HOT = vec3(1.00,0.78,0.35), BALL = vec3(0.85,0.94,1.00);\n\n  // Blueprint: a graded ground and a drawn grid.\n  vec3 col = BG*(0.80 + 0.40*smoothstep(0.9, -0.7, p.y));\n  vec2 g = fract(p*12.0) - 0.5;\n  col += vec3(0.055,0.085,0.150)*smoothstep(0.028, 0.008, min(abs(g.x), abs(g.y)))*0.6;\n  vec2 g2 = fract(p*3.0) - 0.5;\n  col += vec3(0.070,0.105,0.180)*smoothstep(0.012, 0.004, min(abs(g2.x), abs(g2.y)))*0.6;\n\n  float T = 7.0, ph = fract(t/T)*T;\n  float frame = 0.0, work = 0.0, glow = 0.0, hotL = 0.0;\n\n  // ── The frame the machine is bolted to. Always there. ────────────────────\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, vec2(-0.95,-0.50), vec2(0.95,-0.50)) - LW*0.5));\n  for (int i = 0; i < 4; i++){\n    float x = -0.86 + float(i)*0.58;\n    frame = max(frame, smoothstep(LW*0.8, 0.0, seg(p, vec2(x,-0.50), vec2(x, 0.40)) - LW*0.2)*0.55);\n  }\n  frame = max(frame, smoothstep(LW*0.8, 0.0, seg(p, vec2(-0.95,0.40), vec2(0.95,0.40)) - LW*0.2)*0.45);\n\n  // ── 1. Hopper and the ramp ───────────────────────────────────────────────\n  vec2 hop = vec2(-0.86, 0.28);\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, hop + vec2(-0.06,0.08), hop + vec2(-0.018,-0.02)) - LW*0.4));\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, hop + vec2( 0.06,0.08), hop + vec2( 0.018,-0.02)) - LW*0.4));\n  vec2 rA = vec2(-0.84, 0.24), rB = vec2(-0.40, 0.06);\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, rA, rB) - LW*0.5));\n\n  float s1 = clamp(ph/1.30, 0.0, 1.0);\n  vec2 b1 = mix(rA, rB, s1*s1) + vec2(0.0, 0.020);\n  float ballR = 0.020 + comp(a.sub, 0.32)*0.007;\n  glow = max(glow, smoothstep(ballR, ballR - aa*2.0, length(p - b1))*step(ph, 1.34));\n\n  // ── 2. Dominoes on a shelf ───────────────────────────────────────────────\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, vec2(-0.42,0.04), vec2(-0.02,0.04)) - LW*0.5));\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    float x = -0.385 + fi*0.050;\n    float fall = clamp((ph - 1.32 - fi*0.070)/0.15, 0.0, 1.0);\n    vec2 q = rot(-fall*1.40)*(p - vec2(x, 0.04));      // they fall to the right\n    q.y -= 0.042;\n    float d = boxd(q, vec2(0.0055, 0.042));\n    work = max(work, smoothstep(LW, 0.0, abs(d) - LW*0.3));\n    glow = max(glow, smoothstep(0.0, -0.008, d)*fall*(0.20 + a.sib*0.7));\n  }\n\n  // ── 3. The lever ─────────────────────────────────────────────────────────\n  float lev = clamp((ph - 1.90)/0.22, 0.0, 1.0);\n  vec2 piv = vec2(0.05, 0.02);\n  float lang = mix(-0.20, 0.60, lev);\n  work = max(work, smoothstep(LW, 0.0, seg(p, piv + rot(lang)*vec2(-0.11,0.0), piv + rot(lang)*vec2(0.14,0.0)) - LW*0.55));\n  frame = max(frame, smoothstep(LW, 0.0, abs(length(p - piv) - 0.011) - LW*0.3));\n  frame = max(frame, smoothstep(LW*0.8, 0.0, seg(p, piv, vec2(0.05,-0.50)) - LW*0.2)*0.5);\n\n  // ── 4. Exact ballistics ──────────────────────────────────────────────────\n  float ft = max(ph - 2.12, 0.0);\n  vec2 launch = piv + rot(0.60)*vec2(0.14,0.0);\n  vec2 vel = vec2(0.34, 0.60);\n  float gAcc = 1.00;\n  vec2 b2 = launch + vel*ft - vec2(0.0, 0.5*gAcc*ft*ft);\n  float flying = step(2.12, ph)*step(ph, 3.40);\n  // The trajectory, drawn behind the ball: the machine shows its working.\n  for (int k = 0; k < 16; k++){\n    float u = float(k)/15.0*1.30;\n    vec2 q = launch + vel*u - vec2(0.0, 0.5*gAcc*u*u);\n    frame = max(frame, smoothstep(LW*0.7, 0.0, length(p - q) - LW*0.10)*0.32);\n  }\n  glow = max(glow, smoothstep(ballR, ballR - aa*2.0, length(p - b2))*flying);\n\n  // ── 5. Bucket on a pulley ────────────────────────────────────────────────\n  float landed = smoothstep(3.34, 3.52, ph);\n  vec2 buk = vec2(0.50, 0.20 - landed*0.34);\n  work = max(work, smoothstep(LW, 0.0, seg(p, buk + vec2(-0.050,0.038), buk + vec2(-0.034,-0.034)) - LW*0.4));\n  work = max(work, smoothstep(LW, 0.0, seg(p, buk + vec2( 0.050,0.038), buk + vec2( 0.034,-0.034)) - LW*0.4));\n  work = max(work, smoothstep(LW, 0.0, seg(p, buk + vec2(-0.034,-0.034), buk + vec2(0.034,-0.034)) - LW*0.4));\n  work = max(work, smoothstep(LW*0.7, 0.0, seg(p, vec2(0.50,0.375), buk + vec2(0.0,0.038)) - LW*0.15));\n  frame = max(frame, smoothstep(LW, 0.0, abs(length(p - vec2(0.50,0.40)) - 0.024) - LW*0.4));\n  // The cord over the pulley to the latch.\n  work = max(work, smoothstep(LW*0.7, 0.0, seg(p, vec2(0.524,0.40), vec2(0.72,0.40)) - LW*0.15));\n  work = max(work, smoothstep(LW*0.7, 0.0, seg(p, vec2(0.72,0.40), vec2(0.72,0.34 - landed*0.05)) - LW*0.15));\n\n  // ── 6. A real pendulum, damped, exactly solved ───────────────────────────\n  float rel = smoothstep(3.86, 4.02, ph);\n  float pt = max(ph - 3.92, 0.0);\n  float swing = rel*0.90*cos(3.4*pt)*exp(-pt*0.50);\n  vec2 hp = vec2(0.72, 0.32);\n  vec2 hend = hp + rot(swing - 1.5708)*vec2(0.28, 0.0);\n  work = max(work, smoothstep(LW, 0.0, seg(p, hp, hend) - LW*0.45));\n  glow = max(glow, smoothstep(0.024, 0.024 - aa*2.0, length(p - hend)));\n  frame = max(frame, smoothstep(LW, 0.0, abs(length(p - hp) - 0.009) - LW*0.3));\n\n  // ── 7. Bell, then lamp ───────────────────────────────────────────────────\n  vec2 bell = vec2(0.72, -0.02);\n  float strike = exp(-abs(swing)*8.0)*rel*smoothstep(3.96, 4.10, ph);\n  frame = max(frame, smoothstep(LW, 0.0, abs(length((p - bell)*vec2(1.0,1.15)) - 0.042) - LW*0.4));\n  frame = max(frame, smoothstep(LW, 0.0, seg(p, bell + vec2(0.0,0.042), bell + vec2(0.0,0.075)) - LW*0.25));\n  for (int k = 0; k < 3; k++){\n    float rr = 0.055 + float(k)*0.032 + fract(ph*0.9)*0.045;\n    hotL = max(hotL, smoothstep(0.005, 0.0, abs(length(p - bell) - rr))*strike*(1.0 - float(k)*0.30));\n  }\n\n  vec2 lamp = vec2(0.72, -0.32);\n  float lit = smoothstep(4.08, 4.28, ph)*smoothstep(6.92, 6.44, ph);\n  frame = max(frame, smoothstep(LW, 0.0, abs(length(p - lamp) - 0.050) - LW*0.4));\n  frame = max(frame, smoothstep(LW*0.8, 0.0, seg(p, lamp + vec2(0.0,-0.050), vec2(0.72,-0.50)) - LW*0.2));\n  float on = lit*(0.30 + a.voice*1.7 + strike*0.5);\n  hotL = max(hotL, smoothstep(0.047, 0.0, length(p - lamp))*on);\n  col += HOT*on*exp(-length(p - lamp)*4.8)*1.1;\n\n  col += INK*frame*(0.30 + a.pres*0.55);\n  col += INK*work*(0.65 + a.pres*0.95);\n  col += BALL*glow*(0.75 + comp(a.low,0.30)*1.2);\n  col += HOT*hotL*1.8;\n  col += vec3(1.00,0.45,0.40)*pu*0.22*(frame + work);\n\n  col *= 1.0 - smoothstep(0.72, 1.32, length(p))*0.42;\n  col = max(col, 0.0); col = col/(1.0 + col*0.45);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "break", n: 38, name: "Break",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "The shot. Position under linear drag is closed form and a cushion bounce is a triangle-wave fold of the coordinate — both exact, however many times it rebounds. Ball-on-ball after the break is not resolved; with no memory there is no way to order the events.",
    params: [{"name":"Rate","def":0.35},{"name":"Ball size","def":0.4},{"name":"Table light","def":0.4}],
    reacts: [["sub","The cue ball, and its weight"],["low","The crack of the contact"],["pres","The rail catching light"],["voice","Puts the whole table under a light"],["hit","A flash on the break"]],
    body: "// BREAK — the shot. Fifteen bodies racked in a triangle, struck, and scattering\n// under linear drag with elastic wall bounces. Two parts of this are exact and\n// one is not: position under linear drag is x0 + v(1-e^-kt)/k in closed form,\n// and a wall bounce is a triangle-wave fold of the coordinate, which is exactly\n// correct however many times it bounces. Ball-on-ball after the break is not\n// resolved — with no memory there is no way to order the events — so the balls\n// pass through each other once the rack has opened. The break itself is real.\nfloat fold(float x, float lo, float hi){\n  // Elastic reflection off two walls, closed form. Exact for any number of hits.\n  float w = hi - lo, u = abs(fract((x - lo)/(2.0*w))*2.0 - 1.0);\n  return lo + u*w;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.60);\n\n  float T = 6.5;\n  float ph = fract(t/T)*T;\n  float k = 0.55;                                   // drag coefficient\n  float BX = 0.86, BY = 0.44;                       // the cushions\n\n  vec3 col = vec3(0.010,0.013,0.020);\n  // The table: felt-dark, with a rail catching a little light.\n  col += vec3(0.020,0.045,0.038) * (1.0 - smoothstep(0.0, 1.1, length(p*vec2(0.8,1.0))));\n  float rail = max(abs(p.x) - BX, abs(p.y) - BY);\n  col += vec3(0.10,0.13,0.18) * smoothstep(0.012, 0.0, abs(rail)) * (0.35 + a.pres*0.7);\n\n  // The cue travels in, strikes at t=1.4, and the rack opens.\n  float strikeT = 1.40;\n  float after = max(ph - strikeT, 0.0);\n  float decay = (1.0 - exp(-k*after))/k;            // the closed-form drag term\n  float pre = clamp(ph/strikeT, 0.0, 1.0);\n\n  vec2 cue = mix(vec2(-0.72, 0.0), vec2(-0.20, 0.0), pre*pre);\n  if (ph > strikeT) cue = vec2(-0.20, 0.0) + vec2(0.35, 0.0)*decay*0.30;\n  float R = 0.030 + iParam1*0.012;\n\n  // Fifteen in a triangle, and the break pattern is fixed: a real break is\n  // deterministic to the first collision and chaotic after it, so a designed\n  // spread is more honest than a fake simulation.\n  for (int i = 0; i < 15; i++){\n    float fi = float(i);\n    // Rack out the triangle: row r, position s within the row.\n    float r = floor((sqrt(8.0*fi + 1.0) - 1.0)*0.5 + 1e-4);\n    float s = fi - r*(r + 1.0)*0.5;\n    vec2 home = vec2(-0.10 + r*R*1.74, (s - r*0.5)*R*2.02);\n\n    float hh = h21(vec2(fi, 3.0));\n    float ang = (hh - 0.5)*2.4 + (home.y > 0.0 ? 0.55 : -0.55) - r*0.12;\n    float spd = (1.55 - r*0.16) * (0.6 + hh*0.8);\n    vec2 v = vec2(cos(ang), sin(ang))*spd;\n\n    vec2 pos = home + v*decay;\n    pos.x = fold(pos.x, -BX + R, BX - R);\n    pos.y = fold(pos.y, -BY + R, BY - R);\n\n    float e = SPEC(0.008 + fi*0.038);\n    vec3 hue = ramp(vec3(1.00,0.75,0.10), vec3(0.95,0.20,0.35), vec3(0.25,0.55,1.00), fract(hh*1.7));\n    float d = length(p - pos);\n\n    // Motion blur, smeared along the velocity — a long exposure, not a sprite.\n    float speed = length(v)*exp(-k*after)*step(strikeT, ph);\n    vec2 dir = normalize(v + 1e-5);\n    vec2 q = p - pos;\n    float along = dot(q, dir), across = length(q - dir*along);\n    float trail = smoothstep(R*0.85, 0.0, across) *\n                  smoothstep(0.0, -speed*0.22, along) * smoothstep(-speed*0.30, -speed*0.24, along);\n\n    col += hue * smoothstep(R, R - 0.006, d) * (0.30 + pow(e, 1.2)*2.2);\n    col += hue * trail * (0.10 + e*0.75) * 0.7;\n    col += hue * exp(-d*26.0) * (0.05 + e*0.45);\n    // The specular pip every ball carries.\n    col += vec3(1.0) * smoothstep(R*0.34, 0.0, length(q - dir*0.0 - vec2(-R*0.30, R*0.32))) * 0.55 * smoothstep(R, R*0.9, d);\n  }\n\n  // The cue ball.\n  col += vec3(0.95,0.95,0.92) * smoothstep(R, R - 0.006, length(p - cue)) * (0.55 + comp(a.sub,0.32)*0.9);\n  col += vec3(0.9,0.95,1.0) * exp(-length(p - cue)*22.0) * 0.25;\n\n  // The moment of contact: the low end is the crack, and it is one frame long.\n  float impact = exp(-abs(ph - strikeT)*26.0);\n  col += vec3(1.00,0.94,0.80) * impact * (0.55 + comp(a.low,0.30)*1.6)\n       * exp(-length(p - vec2(-0.13, 0.0))*(9.0 - impact*4.0));\n  col += vec3(1.0) * pu * 0.18;\n\n  // Vocals put the whole table under a light.\n  col += a.voice * (0.35 + iParam2*0.55) * vec3(1.00,0.90,0.70)\n       * exp(-length(p*vec2(0.7,1.0))*1.9) * 0.55;\n\n  col *= 1.0 - smoothstep(0.70, 1.30, length(p))*0.50;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "linkage", n: 39, name: "Linkage",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "Three crank-sliders and a gear train off one shaft, so everything is in a fixed phase with everything else. Exact kinematics — the slider is r·cosθ + √(L² − r²sin²θ) and the gears mesh at the reciprocal of their tooth counts. The shaft speed is deliberately not on the audio.",
    params: [{"name":"Shaft rate","def":0.35},{"name":"Line weight","def":0.35},{"name":"Work lamp","def":0.4}],
    reacts: [["sub","Heat at the end of each stroke"],["low","Gear faces, and the brass"],["pres","The drawn line on every part"],["sib","Oil catching along the moving edges"],["voice","Puts the work lamp on over the bench"],["hit","The stroke landing"]],
    body: "// LINKAGE — a machine. Three crank-sliders and a gear train, all driven off one\n// shaft, so everything in frame is in a fixed phase relationship with everything\n// else. The kinematics are exact: a crank-slider is r·cosθ + sqrt(L² − r²sin²θ)\n// and the gears mesh at the reciprocal of their tooth counts.\n//\n// The crank speed is deliberately NOT on the audio. Multiplying elapsed time by\n// a rate that moves makes the angle jump every time the rate changes, and a\n// machine that stutters reads as broken rather than as driven. So the shaft runs\n// steady and the audio drives the load: the lamp, the sparks, the heat.\nfloat seg(vec2 p, vec2 a, vec2 b){\n  vec2 pa = p - a, ba = b - a;\n  return length(pa - ba*clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0));\n}\nfloat gearSDF(vec2 q, float R, float teeth, float phase){\n  float ang = atan(q.y, q.x);\n  float tooth = R*(1.0 + 0.085*smoothstep(-0.35, 0.35, cos(ang*teeth + phase)));\n  return length(q) - tooth;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.35 + iParam0*0.55);\n  float th = t*1.35;                                 // the shaft: steady, always\n\n  float LW = 0.0032 + iParam1*0.0026;\n  float aa = 1.6/iResolution.y;\n  vec3 STEEL = vec3(0.62,0.68,0.78), BRASS = vec3(0.85,0.66,0.30),\n       HOT = vec3(1.00,0.62,0.20);\n\n  vec3 col = vec3(0.020,0.021,0.028);\n  col += vec3(0.030,0.032,0.044) * (1.0 - smoothstep(0.1, 1.2, length(p)));\n\n  float line = 0.0, fill = 0.0, brassL = 0.0, spark = 0.0;\n\n  // ── Gear train: three gears, meshed, ratios exact ─────────────────────────\n  float Ns[3]; Ns[0] = 18.0; Ns[1] = 12.0; Ns[2] = 26.0;\n  vec2 gc[3]; gc[0] = vec2(-0.62,-0.20); gc[1] = vec2(-0.335,-0.20); gc[2] = vec2(0.055,-0.20);\n  float Rs[3]; Rs[0] = 0.150; Rs[1] = 0.100; Rs[2] = 0.217;\n  float dir = 1.0, phase = 0.0;\n  for (int i = 0; i < 3; i++){\n    float N = Ns[i];\n    // Each gear turns at the reciprocal of its teeth, and reverses at each mesh.\n    float spin = th * (18.0/N) * dir;\n    vec2 q = p - gc[i];\n    float d = gearSDF(q, Rs[i], N, spin*N);\n    fill = max(fill, smoothstep(0.0, -0.008, d)*0.30);\n    line = max(line, smoothstep(LW, 0.0, abs(d) - LW*0.3));\n    // Hub, and the spokes that let you see it turn.\n    line = max(line, smoothstep(LW, 0.0, abs(length(q) - Rs[i]*0.22) - LW*0.3));\n    for (int s = 0; s < 4; s++){\n      float sa = spin + float(s)*1.5708;\n      line = max(line, smoothstep(LW*0.8, 0.0,\n                 seg(q, rot(sa)*vec2(Rs[i]*0.22,0.0), rot(sa)*vec2(Rs[i]*0.80,0.0)) - LW*0.25));\n    }\n    dir = -dir;\n  }\n\n  // ── Three crank-sliders off the big gear ─────────────────────────────────\n  for (int i = 0; i < 3; i++){\n    float fi = float(i);\n    float crankA = th*(18.0/26.0) + fi*2.0944;       // 120 degrees apart\n    float r = 0.085, L = 0.30;\n    vec2 hub = gc[2];\n    vec2 pin = hub + rot(crankA)*vec2(r, 0.0);\n    // Exact slider position: the closed-form crank-slider solution.\n    float sx = r*cos(crankA) + sqrt(max(L*L - r*r*sin(crankA)*sin(crankA), 0.0));\n    float yLane = hub.y + (fi - 1.0)*0.185;\n    vec2 slide = vec2(hub.x + sx, yLane);\n    // The rod runs from the pin to the slider.\n    line = max(line, smoothstep(LW, 0.0, seg(p, pin, slide) - LW*0.5));\n    line = max(line, smoothstep(LW, 0.0, seg(p, hub, pin) - LW*0.6));\n    // The lane it runs in, and the piston head.\n    line = max(line, smoothstep(LW*0.7, 0.0, seg(p, vec2(hub.x + 0.10, yLane), vec2(0.94, yLane)) - LW*0.15)*0.45);\n    vec2 hq = p - slide;\n    float head = max(abs(hq.x) - 0.030, abs(hq.y) - 0.026);\n    brassL = max(brassL, smoothstep(0.0, -0.006, head));\n    line = max(line, smoothstep(LW, 0.0, abs(head) - LW*0.3));\n    // Compression: the closer the head gets to the stop, the hotter it runs.\n    float squeeze = smoothstep(0.86, 0.94, slide.x);\n    spark = max(spark, squeeze * exp(-length(p - vec2(0.95, yLane))*20.0) * (0.25 + comp(a.sub,0.32)*1.5));\n    // The transient is the stroke landing.\n    spark = max(spark, squeeze * pu * exp(-length(p - vec2(0.95, yLane))*9.0) * 1.2);\n  }\n\n  // ── A flywheel on the shaft, because a machine needs inertia in it ────────\n  vec2 fq = p - vec2(-0.62, -0.20);\n  line = max(line, smoothstep(LW*0.8, 0.0, abs(length(fq) - 0.215) - LW*0.2)*0.5);\n\n  col += STEEL * line * (0.45 + a.pres*0.95);\n  col += STEEL * fill * (0.25 + comp(a.low,0.30)*0.55);\n  col += BRASS * brassL * (0.40 + comp(a.low,0.30)*0.85);\n  col += HOT * spark * 1.5;\n  // Oil catching the light along every moving edge.\n  col += vec3(0.35,0.55,0.85) * line * (0.05 + a.sib*0.65) * 0.5;\n  // Sub-bass is the shop floor shaking.\n  col += vec3(0.10,0.12,0.20) * comp(a.sub, 0.32) * 0.25 * smoothstep(0.9, -0.5, p.y);\n\n  // Vocals put the work lamp on over the bench.\n  col += a.voice * (0.55 + iParam2*0.65) * vec3(1.00,0.88,0.62)\n       * exp(-length((p - vec2(0.15, 0.42))*vec2(0.8,1.4))*2.2);\n\n  col *= 1.0 - smoothstep(0.70, 1.30, length(p))*0.45;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "rebound", n: 40, name: "Rebound",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "A struck drop, and this one is real physics. A liquid drop held by surface tension rings in the modes Rayleigh derived in 1879, each at its own frequency and decaying at its own rate — so the surface is literally that sum. Higher modes ring faster and die sooner, which is why a struck drop looks the way it does.",
    params: [{"name":"Rate","def":0.4},{"name":"Drop size","def":0.4},{"name":"Gather","def":0.4}],
    reacts: [["sub","Drops it onto the floor; the contact shadow"],["low","Body colour and weight"],["pres","Which modes are driven; the wet rim"],["air","The specular on the surface"],["voice","Gathers the three into one surface tension"],["hit","The strike, and the shock through the body"]],
    body: "// REBOUND — soft body, and this one is real physics rather than a shape that\n// wobbles. A liquid drop held by surface tension oscillates in spherical\n// harmonic modes at frequencies Rayleigh derived in 1879: w_n proportional to\n// sqrt(n(n-1)(n+1)). Strike it and every mode rings at once, each decaying at\n// its own rate, and the sum of them is the wobble. So the surface here is\n// literally that sum: r(theta) = R(1 + SUM A_n cos(n theta + phase) e^-t/tau\n// cos(w_n t)). Higher modes ring faster and die sooner, which is why a struck\n// drop looks the way it does.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.55 + iParam0*0.75);\n\n  vec3 col = vec3(0.012,0.014,0.024);\n  col += vec3(0.030,0.034,0.058) * (1.0 - smoothstep(0.0, 1.2, length(p)));\n\n  // Three drops, struck on staggered clocks so something is always ringing.\n  for (int d = 0; d < 3; d++){\n    float fd = float(d);\n    float period = 1.30 + fd*0.37;\n    float since = fract((t + fd*0.51)/period)*period;   // time since this one was struck\n    float hh = h21(vec2(fd, 5.0));\n\n    vec2 ctr = vec2(-0.46 + fd*0.46, -0.06 + 0.05*sin(t*0.4 + fd*2.0));\n    float R0 = (0.135 + hh*0.045) * (1.0 + iParam1*0.45);\n\n    vec2 q = p - ctr;\n    float r = length(q), th = atan(q.y, q.x);\n\n    // Rayleigh's modes. n = 2 is the slow oval wobble, n = 5 is the fast ripple.\n    float def = 0.0, ring = 0.0;\n    for (int n = 2; n <= 6; n++){\n      float fn = float(n);\n      float wn = 3.1*sqrt(fn*(fn - 1.0)*(fn + 1.0)/6.0);   // Rayleigh scaling\n      float tau = 0.95/(fn*fn*0.28);                       // viscosity kills high n first\n      // Which modes get excited is on the mix: low end drives the slow wobble,\n      // the top of the spectrum drives the fast ripple.\n      float drive = SPEC(0.006 + (fn - 2.0)*0.085);\n      float amp = (0.06 + drive*0.42 + pu*0.30) * (0.55 + hh*0.55);\n      float phase = fn*1.7 + fd*2.3;\n      float m = amp*exp(-since/tau)*cos(wn*since);\n      def  += m*cos(fn*th + phase);\n      ring += abs(m)*(fn/6.0);\n    }\n    float surf = R0*(1.0 + def);\n\n    // Shading: a drop is a lens, so it is dark in the middle and bright at the\n    // rim where the surface turns away.\n    float inside = smoothstep(surf, surf - 0.006, r);\n    float rim = smoothstep(0.055, 0.0, abs(r - surf));\n    float nz = clamp(1.0 - r/max(surf, 1e-3), 0.0, 1.0);\n    // A drop is a lens: dark and saturated in the middle, and the hue shifts\n    // through it the way a thick liquid does.\n    vec3 body = mix(vec3(0.030,0.075,0.230), vec3(0.10,0.62,0.92), pow(nz, 0.7));\n    body = mix(body, vec3(0.95,0.30,0.62), fract(hh*2.3)*0.45 + a.pres*0.25);\n    body = mix(body, vec3(1.00,0.72,0.25), smoothstep(0.75, 1.0, nz)*0.45);\n\n    col += inside * body * (0.55 + comp(a.low, 0.30)*0.85);\n    col += rim * inside * (0.45 + a.pres*1.50) * vec3(0.60,0.90,1.00);\n    col += rim * (1.0 - inside) * (0.15 + ring*1.6) * vec3(0.35,0.70,1.00);\n    // The specular a wet surface always has, and it moves as the drop deforms.\n    vec2 sp = q - vec2(-0.30, 0.34)*R0;\n    col += inside * smoothstep(R0*0.22, 0.0, length(sp)) * (0.45 + a.air*0.9) * vec3(1.0,0.98,0.95);\n    // The strike itself, visible as a shock through the body.\n    col += inside * exp(-since*9.0) * (0.20 + pu*1.2) * vec3(0.85,0.95,1.00) * (0.4 + ring);\n\n    // Sub-bass drops it onto the floor: a contact shadow that squashes with it.\n    float floorY = -0.34;\n    float squash = 1.0 + def*1.4;\n    col += vec3(0.10,0.16,0.30) * exp(-abs(p.y - floorY)*30.0)\n         * exp(-abs(p.x - ctr.x)*(7.0/squash)) * (0.15 + comp(a.sub, 0.32)*0.55);\n  }\n\n  // Vocals gather the three into one bright surface tension, and gild the rims.\n  col += a.voice * (0.35 + iParam2*0.55) * vec3(1.00,0.86,0.50)\n       * exp(-abs(p.y + 0.06)*3.4) * 0.55;\n\n  col *= 1.0 - smoothstep(0.65, 1.25, length(p))*0.45;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "ignition", n: 41, name: "Ignition",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "Fire, coloured off the Planck locus rather than a hand-picked orange ramp — deep red near 1000K through orange to near-white by 2500K. The plume rises, shears in the draught and breaks into vortices, and its edge is eaten by the turbulence rather than faded, because a flame has a torn edge.",
    params: [{"name":"Rate","def":0.4},{"name":"Plume width","def":0.35},{"name":"Flare","def":0.45}],
    reacts: [["sub","Width of the column; the blue premixed base"],["low","Temperature, and how far it licks"],["pres","Turbulence in the sheet"],["air","Embers lifting off the fire"],["voice","Flares it hotter and further"],["hit","A surge through the plume"]],
    body: "// IGNITION — fire, coloured properly. The temperature field is buoyant: it\n// rises, shears in the draught, breaks into vortices as it goes and cools with\n// height, and the colour at every point comes off an approximation of the Planck\n// locus rather than a hand-picked orange ramp. That is the difference between\n// fire and a warm gradient — real flame runs deep red near 1000K through orange\n// and yellow to near-white by 2500K, and the eye knows the sequence even when it\n// cannot name it. The edge of the plume is eaten by the turbulence rather than\n// faded out, because a flame has a torn edge, not a soft one.\nvec3 blackbody(float K){\n  K = clamp(K, 1000.0, 6500.0)/100.0;\n  float r = K <= 66.0 ? 255.0 : 329.7*pow(K - 60.0, -0.1332);\n  float g = K <= 66.0 ? 99.47*log(K) - 161.12 : 288.12*pow(K - 60.0, -0.0755);\n  float b = K >= 66.0 ? 255.0 : (K <= 19.0 ? 0.0 : 138.52*log(K - 10.0) - 305.04);\n  return clamp(vec3(r, g, b)/255.0, 0.0, 1.0);\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.60 + iParam0*0.90);\n\n  vec3 col = vec3(0.007,0.006,0.009);\n\n  float h = p.y + 0.46;                                   // height above the fuel\n  float lick = 1.0 + comp(a.low, 0.30)*0.85 + pu*0.50;\n  float draught = 0.20*sin(t*0.29) + 0.12*sin(t*0.17 + 2.0);\n  float x = p.x - draught*h*h*1.25;\n\n  // Advect: sample the field lower down and later, which is the stateless way to\n  // carry heat up a column. Stretched vertically, because rising gas is.\n  vec2 q = vec2(x*3.1, h*1.05 - t*(1.55 + comp(a.low,0.30)*0.85));\n  float warp = fbm(q*0.60 + vec2(t*0.09, 0.0), 3);\n  float n1 = fbm(q*1.20 + warp*(1.35 + a.pres*0.90), OCT);\n  // Finer vortices higher up, where the sheet has broken.\n  float n2 = fbm(q*3.30 + warp*2.40, 3);\n  float n = mix(n1, n1*0.55 + n2*0.45, smoothstep(0.10, 0.85, h));\n\n  // The plume edge is torn by the noise, not faded — this is the whole look.\n  float w0 = (0.20 + iParam1*0.20)*(0.45 + comp(a.sub, 0.32)*0.95);\n  float edge = w0*(1.0 - h*0.44)*(0.30 + n*1.60);\n  float dens = smoothstep(edge, edge*0.18, abs(x));\n  dens *= smoothstep(1.55*lick, 0.05, h)*step(-0.02, h);\n  // Tongues: the flame separates into licks near the top.\n  float tongue = smoothstep(0.42, 0.68, n)*smoothstep(0.20, 0.70, h);\n  dens = max(dens, tongue*smoothstep(w0*2.6, 0.0, abs(x))*smoothstep(1.5*lick, 0.2, h));\n  dens = clamp(dens, 0.0, 1.0);\n\n  // Temperature: hottest low and on axis, falling with height and with n.\n  float T = (980.0 + 900.0*comp(a.low, 0.30) + 480.0*a.pres)*(1.15 - h*0.42)\n          + 620.0*smoothstep(0.20, 0.80, n);\n  T = mix(T, T*1.22, pu*0.50);\n  float emit = pow(dens, 1.25)*(0.55 + n*0.75);\n  col += blackbody(T + 420.0)*emit*(1.25 + a.pres*0.80);\n\n  // The blue base, where the fuel is still premixed and burning hottest.\n  float base = smoothstep(w0*1.5, 0.0, abs(x))*smoothstep(0.26, 0.0, h)*step(-0.02, h);\n  col += vec3(0.30,0.58,1.00)*base*(0.35 + comp(a.sub, 0.32)*0.95)*smoothstep(0.25, 0.60, n);\n\n  // Soot: the plume is opaque where it has cooled, which is what gives fire a top.\n  float soot = smoothstep(0.60, 0.22, dens)*smoothstep(0.60, 1.5, h)*smoothstep(0.32, 0.58, n);\n  col = mix(col, col*0.20 + vec3(0.030,0.026,0.026), soot*0.70);\n\n  // Embers, cooling as they rise. Air is the draught that lifts them.\n  for (int i = 0; i < 2; i++){\n    float fi = float(i);\n    vec2 eg = vec2(x*(24.0 + fi*18.0), h*(15.0 + fi*12.0) - t*(3.6 + fi*2.2));\n    vec2 ei = floor(eg), ef = fract(eg) - 0.5;\n    float hh = h21(ei + fi*23.0);\n    if (hh > 0.950 - a.air*0.012){\n      float life = fract(hh*7.0 + t*0.11);\n      float d = length(ef - (vec2(h21(ei+3.0), h21(ei+9.0)) - 0.5)*0.6);\n      col += smoothstep(0.16, 0.0, d)*blackbody(2700.0 - life*1600.0)\n           * (0.40 + a.air*1.5)*(1.0 - life)*smoothstep(w0*4.0, 0.0, abs(x));\n    }\n  }\n\n  // The fuel bed, and the light it throws into the room.\n  col += blackbody(1350.0)*exp(-abs(p.y + 0.46)*22.0)*exp(-abs(p.x)*3.0)\n       * (0.40 + comp(a.sub, 0.32)*0.95);\n  col += blackbody(2100.0)*0.085*exp(-length(p - vec2(0.0,-0.32))*1.6)*(0.35 + comp(a.low,0.30));\n\n  // Vocals flare it: more air, hotter, reaching further.\n  col += blackbody(3100.0)*a.voice*(0.60 + iParam2*0.80)*emit*1.6;\n  col += vec3(1.00,0.93,0.78)*a.voice*0.32*base;\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.40);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "tokamak", n: 42, name: "Tokamak",
    series: "IV", set: "impetus", setTitle: "Impetus",
    kit3d: false,
    line: "A poloidal cross-section of confined plasma. Plasma moves freely along a field line and not across one, so everything is smeared along the flux surfaces and nothing crosses them. Emission follows the pressure profile; the edge goes unstable and throws filaments, which nobody has fully tamed in a real machine either.",
    params: [{"name":"Rate","def":0.4},{"name":"Plasma size","def":0.35},{"name":"Heating","def":0.4}],
    reacts: [["sub","Shafranov shift; the field coils"],["low","Core emission"],["pres","Flux surfaces, and the vessel wall"],["sib","Edge-localised modes, and the strike point"],["air","Light scattered off neutral gas at the wall"],["voice","Ramps the heating — the core lights"],["hit","An edge mode letting go"]],
    body: "// TOKAMAK — a poloidal cross-section of magnetically confined plasma. The nested\n// D-shaped curves are flux surfaces: the plasma is free to move along a field\n// line but not across one, so everything in the picture is smeared along them\n// and nothing crosses. Emission follows the pressure profile, which peaks on\n// axis and falls to nothing at the last closed surface. At the edge it goes\n// unstable and throws filaments out into the vessel, which is the part of this\n// that nobody has ever fully tamed in a real machine either.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.45 + iParam0*0.75);\n\n  // The magnetic axis sits outboard of the vessel centre, as it does in a real\n  // machine — the Shafranov shift.\n  vec2 ax = vec2(0.06 + comp(a.sub, 0.32)*0.02, 0.0);\n  vec2 q = p - ax;\n\n  // A D-shaped flux coordinate: elongated, with triangularity.\n  float kappa = 1.62;                                   // elongation\n  float delta = 0.32;                                   // triangularity\n  float th = atan(q.y/kappa, q.x);\n  float rr = length(vec2(q.x - delta*0.18*sin(th)*sin(th), q.y/kappa));\n  float edge = 0.36 + iParam1*0.08;\n  float psi = rr/edge;                                  // 0 on axis, 1 at the edge\n\n  vec3 col = vec3(0.010,0.010,0.018);\n\n  if (psi < 1.35){\n    // Pressure profile: peaked on axis, zero at the last closed surface.\n    float pres = pow(max(1.0 - psi*psi, 0.0), 1.6);\n    // Emission is hottest and bluest on axis, cooler and pinker at the edge.\n    vec3 core = mix(vec3(0.85,0.92,1.00), vec3(0.55,0.35,1.00), smoothstep(0.0, 0.8, psi));\n    core = mix(core, vec3(1.00,0.35,0.70), smoothstep(0.6, 1.0, psi));\n    col += core * pres * (0.40 + comp(a.low, 0.30)*0.95);\n\n    // Flux surfaces, drawn. The spacing tightens toward the edge because the\n    // gradient does.\n    float surf = sin(psi*psi*(11.0 + a.pres*8.0) - t*0.9);\n    float w = fwidth(surf)*1.3 + 1e-4;\n    col += smoothstep(w*2.4, 0.0, abs(surf)) * (0.06 + a.pres*0.55) * core * 1.4;\n\n    // Filaments: plasma smeared along the field, so they run around the surface\n    // and never across it. Each takes one bin of the spectrum.\n    float fl = 0.0;\n    for (int i = 0; i < 4; i++){\n      float fi = float(i);\n      float n = 4.0 + fi*3.0;\n      float e = SPEC(0.008 + fi*0.10);\n      fl += pow(0.5 + 0.5*sin(th*n - psi*7.0 + t*(1.1 + fi*0.4)), 7.0) * (0.10 + e*1.5);\n    }\n    col += fl * pres * mix(vec3(0.45,0.85,1.00), vec3(0.95,0.55,1.00), psi) * 0.60;\n\n    // Edge-localised modes: the boundary lets go, periodically and violently.\n    float elm = smoothstep(0.72, 1.0, psi) * pow(0.5 + 0.5*sin(th*9.0 + t*3.1), 5.0);\n    col += elm * (0.15 + a.sib*1.6 + pu*2.2) * vec3(1.00,0.75,0.95);\n    // And what escapes crosses the scrape-off layer to the wall.\n    float sol = smoothstep(1.0, 1.30, psi) * smoothstep(1.36, 1.0, psi);\n    col += sol * (0.05 + a.sib*0.75 + pu*1.1) * vec3(0.70,0.45,1.00)\n         * pow(0.5 + 0.5*sin(th*9.0 - t*2.2), 4.0);\n  }\n\n  // The vessel: a first wall and a divertor at the bottom, where the exhaust goes.\n  float vessel = length(vec2(q.x, q.y/1.72)) - 0.52;\n  col += vec3(0.30,0.34,0.44) * smoothstep(0.010, 0.0, abs(vessel)) * (0.30 + a.pres*0.55);\n  float div = smoothstep(0.030, 0.0, abs(p.y + 0.60)) * smoothstep(0.42, 0.10, abs(p.x - 0.06));\n  col += vec3(0.35,0.38,0.46) * div * 0.5;\n  col += vec3(1.00,0.55,0.85) * div * (0.10 + a.sib*1.2) * 0.8;      // strike point\n\n  // Toroidal field coils, seen edge-on.\n  for (int i = 0; i < 2; i++){\n    float sx = (float(i)*2.0 - 1.0)*0.62;\n    vec2 cq = abs(p - vec2(sx, 0.0)) - vec2(0.035, 0.30);\n    float cd = min(max(cq.x, cq.y), 0.0) + length(max(cq, 0.0));\n    col += vec3(0.22,0.26,0.36) * smoothstep(0.008, 0.0, abs(cd));\n    col += vec3(0.35,0.60,1.00) * smoothstep(0.0, -0.02, cd) * (0.06 + comp(a.sub,0.32)*0.45);\n  }\n\n  // Air is the diagnostic light scattered off the neutral gas near the wall.\n  col += vec3(0.35,0.75,1.00) * a.air * 0.30 * smoothstep(0.95, 1.30, psi) * smoothstep(1.5, 1.1, psi);\n\n  // Vocals ramp the heating: the core brightens and the whole column lights.\n  col += a.voice * (0.55 + iParam2*0.65) * vec3(0.85,0.90,1.00)\n       * pow(max(1.0 - psi*psi, 0.0), 2.4) * 1.0;\n  col += a.voice * 0.35 * vec3(1.00,0.85,0.95) * exp(-abs(psi - 1.0)*7.0);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.48);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "karman", n: 43, name: "Kármán",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "Flow past a cylinder and the vortex street it sheds. Real fluid dynamics: for inviscid flow the whole field comes out of one complex potential, so these streamlines are contours of an exact stream function and adding a vortex to the flow is just addition. The shedding cadence is set, not solved.",
    params: [{"name":"Rate","def":0.3},{"name":"Zoom","def":0.4},{"name":"Reynolds","def":0.45}],
    reacts: [["sub","Core size of each vortex; the stagnation point"],["low","Circulation — how hard each vortex spins"],["pres","Vorticity colour; the cylinder edge"],["sib","The streamlines themselves"],["air","Dye stretched along the flow"],["voice","Raises the Reynolds number — the street tightens and lights"]],
    body: "// KÁRMÁN — flow past a cylinder, and the vortex street it sheds. This is real\n// fluid dynamics rather than an impression of it: for inviscid incompressible\n// flow the whole field comes out of one complex potential, so the streamlines\n// here are contours of an exact stream function — U·y·(1 − a²/r²) for the\n// cylinder, plus (Γ/2π)·ln r for every shed vortex. Superposition is exact, so\n// adding vortices to the flow is just addition. What is not simulated is the\n// shedding itself: the vortices are released on a fixed Strouhal cadence and\n// advect downstream, which is what they measurably do.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.55);\n\n  vec2 z = (p + vec2(0.55, 0.0)) * (1.55 - iParam1*0.45);\n  float A = 0.19;                                     // cylinder radius\n  float U = 1.0;\n\n  // Cylinder in a uniform stream. The classic result, unchanged since 1850.\n  float r2 = max(dot(z, z), 1e-4);\n  float psi = U*z.y*(1.0 - A*A/r2);\n  float vort = 0.0;\n  vec2 vel = vec2(0.0);\n\n  // Twelve vortices, alternating sign, on a Strouhal cadence.\n  float span = 3.4, spacing = span/6.0;\n  for (int i = 0; i < 12; i++){\n    float fi = float(i);\n    float sgn = mod(fi, 2.0) < 1.0 ? 1.0 : -1.0;\n    float u = fract((t*0.42 + fi*spacing*0.5)/span);   // where along the wake\n    float xk = A*1.35 + u*span;\n    float yk = sgn*(A*0.62 + u*0.22);                  // the street widens as it goes\n    // Fade in as it is shed and out as it dissipates, so nothing pops.\n    float life = smoothstep(0.0, 0.10, u)*smoothstep(1.0, 0.72, u);\n    float G = sgn*0.30*life*(0.55 + comp(a.low, 0.30)*0.85);\n\n    vec2 d = z - vec2(xk, yk);\n    float dd = max(dot(d, d), 4e-4);\n    psi += G*0.5*log(dd);\n    // A finite core, or the vorticity is a delta and you cannot draw it.\n    float sig = 0.055 + comp(a.sub, 0.32)*0.020;\n    vort += G*exp(-dd/(sig*sig))*3.4;\n    vel += G*vec2(-d.y, d.x)/dd*0.12;\n  }\n  vel += vec2(U, 0.0);\n\n  vec3 col = vec3(0.012,0.016,0.028);\n\n  // Vorticity is what you actually see in a wake: two colours, two senses of spin.\n  vec3 warm = vec3(1.00,0.42,0.28), cool = vec3(0.25,0.62,1.00);\n  col += (vort > 0.0 ? warm : cool) * pow(abs(vort), 0.85) * (0.45 + a.pres*1.10);\n\n  // Streamlines: contours of the stream function, one width everywhere.\n  float s = psi*(11.0 + a.pres*7.0);\n  float w = fwidth(s)*1.25 + 1e-4;\n  float lines = smoothstep(w*2.2, 0.0, abs(fract(s) - 0.5));\n  col += lines * (0.10 + a.sib*0.85) * vec3(0.80,0.90,1.00);\n\n  // Dye injected upstream, carried along the streamlines and stretched by them.\n  float dye = smoothstep(0.55, 0.95, fbm(vec2(psi*4.5, z.x*1.5 - t*1.1), 3));\n  col += dye * (0.05 + a.air*0.65) * vec3(0.65,0.95,1.00) * smoothstep(-0.5, 0.6, z.x);\n\n  // The cylinder itself, and the stagnation point on its nose.\n  float cyl = length(z) - A;\n  col = mix(col, vec3(0.045,0.050,0.065), smoothstep(0.004, -0.004, cyl));\n  col += vec3(0.55,0.70,0.95) * smoothstep(0.006, 0.0, abs(cyl)) * (0.30 + a.pres*0.65);\n  col += vec3(1.00,0.90,0.75) * exp(-length(z - vec2(-A, 0.0))*26.0) * (0.15 + comp(a.sub,0.32)*0.75);\n\n  // Speed, faintly, over the shoulders where the flow accelerates past the body.\n  col += vec3(0.30,0.45,0.70) * smoothstep(1.2, 2.4, length(vel)) * 0.35;\n\n  // Vocals raise the Reynolds number: the street tightens and lights up.\n  col += a.voice * (0.55 + iParam2*0.75) * abs(vort) * vec3(1.00,0.86,0.45) * 1.2;\n  col += pu * 0.16 * lines;\n\n  col *= 1.0 - smoothstep(0.70, 1.30, length(p))*0.42;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "lodestone", n: 44, name: "Lodestone",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "A bar magnet under paper with filings on top. The field lines are exact: for two poles the flux function is cos θ₁ − cos θ₂, and a field line is precisely a contour of it — so every pixel knows which line it is on, which is why they close properly and never drift.",
    params: [{"name":"Rate","def":0.3},{"name":"Line weight","def":0.35},{"name":"Gild","def":0.4}],
    reacts: [["sub","Pole strength, and the glow around each end"],["low","Separation of the poles, and field strength"],["pres","Spacing of the drawn field lines"],["sib","Filing density"],["air","Light on the upper edge of each filing"],["voice","Magnetises the plate — the lines gild and the filings stand up"],["hit","Taps the plate; the filings resettle"]],
    body: "// LODESTONE — a bar magnet under paper, with filings on top. The field lines\n// here are exact: for two poles the flux function is cos θ₁ − cos θ₂ measured\n// from each pole, and a field line is precisely a contour of it. So the lines\n// are not traced or integrated — every pixel knows which line it is on, which is\n// why they close properly and never drift or double back.\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.09 + iParam0*0.22);\n\n  // The magnet breathes apart and together, slowly. Pole strength is on the low\n  // end, which is the one thing here that reads as weight.\n  float sep = 0.30 + 0.045*sin(t*0.55) + comp(a.low, 0.30)*0.045;\n  float qm = 1.0 + comp(a.low, 0.30)*0.55;\n  vec2 pN = vec2( sep, 0.0), pS = vec2(-sep, 0.0);\n\n  vec2 dN = p - pN, dS = p - pS;\n  float rN = max(length(dN), 0.012), rS = max(length(dS), 0.012);\n  // Flux function. Contours of this are the field lines.\n  float psi = qm*(dN.x/rN - dS.x/rS);\n  // The field itself, for the filings to lie along.\n  vec2 B = qm*(dN/(rN*rN*rN) - dS/(rS*rS*rS));\n  float Bm = length(B);\n  vec2 Bd = B/max(Bm, 1e-4);\n\n  // Paper: warm, tooth, and a little darker at the edges.\n  vec3 col = vec3(0.075,0.078,0.092);\n  col += vec3(0.030,0.030,0.038)*fbm(p*40.0, 2);\n  col *= 1.0 - smoothstep(0.35, 1.15, length(p))*0.45;\n\n  // Field lines, one width everywhere thanks to the derivative.\n  float s = psi*(5.5 + a.pres*4.0);\n  float w = fwidth(s)*1.3 + 1e-4;\n  float lines = smoothstep(w*2.4, 0.0, abs(fract(s) - 0.5));\n  col += lines * (0.10 + a.pres*0.75) * vec3(0.42,0.62,1.00) * 1.2;\n\n  // Filings: short dashes on a jittered grid, each lying along B, and packed\n  // more densely where the field is strong — which is what makes the pattern.\n  for (int k = 0; k < 2; k++){\n    float fk = float(k);\n    float sc = 46.0 + fk*31.0;\n    vec2 g = p*sc;\n    vec2 gi = floor(g), gf = fract(g) - 0.5;\n    float hh = h21(gi + fk*19.0);\n    vec2 jit = (vec2(h21(gi + 3.0), h21(gi + 7.0)) - 0.5)*0.7;\n    vec2 q = gf - jit;\n    // Align to the field at the cell centre, not at the pixel, or the dash bends.\n    vec2 cc = (gi + 0.5 + jit)/sc;\n    vec2 cN = cc - pN, cS = cc - pS;\n    float cRN = max(length(cN), 0.012), cRS = max(length(cS), 0.012);\n    vec2 cB = qm*(cN/(cRN*cRN*cRN) - cS/(cRS*cRS*cRS));\n    vec2 dir = cB/max(length(cB), 1e-4);\n    float along = dot(q, dir), across = dot(q, vec2(-dir.y, dir.x));\n    float len = 0.32 + hh*0.14;\n    float dash = smoothstep(0.075, 0.020, abs(across)) * smoothstep(len, len*0.55, abs(along));\n    float dens = smoothstep(0.6, 14.0, length(cB));\n    col += dash * dens * (0.30 + a.sib*0.85) * vec3(0.62,0.66,0.76) * (1.0 - fk*0.35);\n    // Filings catch the light on their upper edge.\n    col += dash * dens * smoothstep(0.0, 0.06, across) * (0.10 + a.air*0.85) * vec3(0.90,0.94,1.00);\n  }\n\n  // The magnet: two blocks, and the convention is red north, blue south.\n  vec2 mq = abs(p) - vec2(sep + 0.055, 0.055);\n  float mag = min(max(mq.x, mq.y), 0.0) + length(max(mq, 0.0));\n  vec3 body = p.x > 0.0 ? vec3(0.72,0.16,0.16) : vec3(0.14,0.26,0.66);\n  col = mix(col, body*(0.55 + comp(a.sub, 0.32)*0.55), smoothstep(0.004, -0.004, mag));\n  col += vec3(1.0) * smoothstep(0.005, 0.0, abs(mag)) * 0.25;\n  col += body * exp(-max(mag, 0.0)*14.0) * (0.10 + comp(a.sub, 0.32)*0.75);\n\n  // Vocals magnetise the whole plate: the lines gild and the filings stand up.\n  col += a.voice * (0.75 + iParam2*0.85) * lines * vec3(1.00,0.84,0.40) * 1.6;\n  col += a.voice * 0.30 * smoothstep(0.5, 9.0, Bm) * vec3(1.00,0.90,0.62);\n  // A transient taps the plate and the filings resettle.\n  col += pu * 0.22 * lines * vec3(1.0);\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.45);\n  o = vec4(pow(col, vec3(0.92)), 1.0);\n}",
  },
  {
    id: "turning", n: 45, name: "Turning",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "One tree, one year. The season runs on a slow clock and the day on a fast one, and neither is on the audio, because a year that speeds up for the chorus is not a year. What the audio does is weather.",
    params: [{"name":"Rate","def":0.35},{"name":"Late light","def":0.4}],
    reacts: [["sub","Part of the gust that leans the crown"],["low","The gust itself"],["air","Grass and leaf catching the wind; what is falling"],["voice","Turns the light golden and sends a flight across"]],
    body: "// TURNING — one tree, one year. The season runs on a slow clock and the day runs\n// on a fast one, and neither is on the audio, because a year that speeds up when\n// the chorus arrives is not a year. What the audio does is weather: the wind\n// through the crown, the light on the field, what is falling.\n#define TAU 6.28318531\nfloat seg(vec2 p, vec2 a, vec2 b, float w){\n  vec2 pa = p - a, ba = b - a;\n  return length(pa - ba*clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0)) - w;\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio();\n  int OCT = plajahOct();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.65);\n\n  float year = fract(t*0.028);                       // spring 0, summer ¼, autumn ½, winter ¾\n  float day  = fract(t*0.115);\n  // Long days: the sun is up about two thirds of the cycle, so the piece is\n  // mostly daylight and night is the punctuation rather than the default.\n  float sun  = sin(day*TAU)*0.62 + 0.30;\n  float gust = comp(a.low, 0.30)*0.7 + comp(a.sub, 0.35)*0.3;\n\n  // Seasonal light, and the day laid over it.\n  vec3 springSky = vec3(0.62,0.80,0.93), summerSky = vec3(0.35,0.66,0.95),\n       autumnSky = vec3(0.86,0.72,0.55), winterSky = vec3(0.72,0.76,0.84);\n  vec3 sky = year < 0.25 ? mix(springSky, summerSky, year/0.25)\n           : year < 0.50 ? mix(summerSky, autumnSky, (year-0.25)/0.25)\n           : year < 0.75 ? mix(autumnSky, winterSky, (year-0.50)/0.25)\n                         : mix(winterSky, springSky, (year-0.75)/0.25);\n  vec3 night = vec3(0.035,0.055,0.115);\n  float dayAmt = smoothstep(-0.25, 0.30, sun);\n  vec3 zenith = mix(night, sky*0.72, dayAmt);\n  vec3 horizon = mix(night*1.6, mix(sky, vec3(1.00,0.80,0.58), 0.45), dayAmt);\n  // Dusk and dawn burn along the horizon.\n  horizon = mix(horizon, vec3(1.00,0.55,0.32), smoothstep(0.35, 0.0, abs(sun))*0.75);\n  vec3 col = mix(horizon, zenith, smoothstep(-0.15, 0.60, p.y));\n\n  // Sun and moon on the same arc, half a year of phase apart.\n  vec2 sunP = vec2(cos(day*TAU - 1.5708)*0.72, sin(day*TAU)*0.42 + 0.02);\n  col += vec3(1.00,0.95,0.80)*smoothstep(0.055, 0.030, length(p - sunP))*max(sun, 0.0)*1.5;\n  col += vec3(1.00,0.90,0.70)*exp(-length(p - sunP)*4.0)*max(sun, 0.0)*0.55;\n  vec2 moonP = -sunP;\n  float phase = fract(t*0.043);\n  float md = length(p - moonP);\n  float moon = smoothstep(0.042, 0.034, md)\n             * smoothstep(0.0, 0.012, abs(p.x - moonP.x + (phase - 0.5)*0.09) + (phase < 0.5 ? 0.0 : -0.02));\n  col += vec3(0.95,0.95,0.88)*moon*max(-sun, 0.0)*1.2;\n  // Stars, only at night.\n  vec2 sg = floor(p*76.0);\n  col += step(0.995, h21(sg))*(0.35 + a.air*1.4*h21(sg + floor(t*3.0)))\n       * max(-sun, 0.0)*smoothstep(0.0, 0.4, p.y)*vec3(0.9,0.93,1.0);\n\n  // The field. Green through spring and summer, straw in autumn, snow in winter.\n  float hy = -0.24 + 0.030*fbm(vec2(p.x*2.2, 3.0), 3);\n  float ground = smoothstep(hy + 0.004, hy - 0.004, p.y);\n  vec3 fieldC = year < 0.25 ? mix(vec3(0.30,0.52,0.22), vec3(0.24,0.46,0.18), year/0.25)\n              : year < 0.50 ? mix(vec3(0.24,0.46,0.18), vec3(0.62,0.50,0.22), (year-0.25)/0.25)\n              : year < 0.75 ? mix(vec3(0.62,0.50,0.22), vec3(0.86,0.88,0.92), (year-0.50)/0.25)\n                            : mix(vec3(0.86,0.88,0.92), vec3(0.30,0.52,0.22), (year-0.75)/0.25);\n  col = mix(col, fieldC*(0.35 + dayAmt*0.75), ground);\n  // Grass catching the wind, on the air band.\n  float blade = sin(p.x*180.0 + fbm(p*7.0 + t*0.5, 2)*10.0 + t*1.4 + gust*3.0)*0.5 + 0.5;\n  col += ground*pow(blade, 7.0)*a.air*0.55*fieldC*2.2*dayAmt;\n\n  // The tree. Trunk and eight boughs, fixed — a tree does not change its shape\n  // between verses. The crown leans with the gust.\n  vec2 root = vec2(-0.06, hy);\n  float lean = gust*0.045 + 0.012*sin(t*0.9);\n  float wood = seg(p, root, root + vec2(lean*0.4, 0.20), 0.020);\n  for (int i = 0; i < 8; i++){\n    float fi = float(i);\n    float ang = 1.5708 + (fi - 3.5)*0.30 + 0.10*sin(fi*2.1);\n    float len = 0.20 + 0.055*sin(fi*1.7);\n    vec2 b0 = root + vec2(lean*0.4, 0.20);\n    vec2 b1 = b0 + vec2(cos(ang), sin(ang))*len + vec2(lean, 0.0);\n    wood = min(wood, seg(p, b0, b1, 0.009 - fi*0.0002));\n    float ang2 = ang + (h21(vec2(fi,1.0)) - 0.5)*0.9;\n    vec2 b2 = b1 + vec2(cos(ang2), sin(ang2))*len*0.55 + vec2(lean*1.4, 0.0);\n    wood = min(wood, seg(p, b1, b2, 0.005));\n  }\n  col = mix(col, vec3(0.14,0.10,0.085)*(0.45 + dayAmt*0.65), smoothstep(0.003, -0.003, wood));\n\n  // The crown: full in summer, turning in autumn, gone in winter.\n  vec2 cq = (p - (root + vec2(lean*1.6, 0.44)))*vec2(1.0, 1.30);\n  float crown = fbm(cq*3.6 + vec2(gust*0.5 + t*0.20, 0.0), OCT);\n  // A crown with body: a broad mass with a bitten edge, not a soft blob.\n  float foliage = smoothstep(0.045, -0.020, length(cq) - 0.30 + (crown - 0.5)*0.30);\n  float leafAmt = year < 0.10 ? smoothstep(0.0, 0.10, year)\n                : year < 0.55 ? 1.0\n                : year < 0.72 ? smoothstep(0.72, 0.55, year) : 0.0;\n  vec3 leafC = year < 0.30 ? mix(vec3(0.52,0.72,0.28), vec3(0.20,0.45,0.18), year/0.30)\n             : year < 0.62 ? mix(vec3(0.20,0.45,0.18), vec3(0.86,0.48,0.12), (year-0.30)/0.32)\n                           : vec3(0.72,0.32,0.10);\n  // Clumps: a crown is a few hundred bunches of leaf, not one silhouette.\n  float clump = fbm(cq*13.0 + vec2(gust*0.8 + t*0.35, 0.0), 3);\n  vec3 leafLit = leafC*(0.35 + dayAmt*0.85)*(0.55 + clump*0.95);\n  // Light comes through the top of the crown and is blocked underneath.\n  leafLit *= 0.70 + 0.55*smoothstep(-0.22, 0.22, cq.y*-1.0 + clump*0.30);\n  col = mix(col, leafLit, foliage*leafAmt*0.95);\n  col += foliage*leafAmt*smoothstep(0.62, 0.90, clump)*a.air*0.55*vec3(0.85,1.00,0.55);\n  // Blossom, and only in the first weeks of spring.\n  col = mix(col, vec3(1.00,0.86,0.90), foliage*smoothstep(0.12, 0.02, year)*smoothstep(0.0, 0.03, year)*0.75);\n\n  // What is falling depends on the month: petals, then leaves, then snow.\n  vec2 fg = p*34.0 + vec2(sin(p.y*3.0 + t)*1.4 + t*(0.6 + gust*1.4), t*(1.6 + gust));\n  vec2 fi2 = floor(fg), ff = fract(fg) - 0.5;\n  float fh = h21(fi2);\n  float fallAmt = max(smoothstep(0.52, 0.68, year)*smoothstep(0.80, 0.68, year),\n                      smoothstep(0.70, 0.82, year))*0.9\n                + smoothstep(0.12, 0.02, year)*0.5;\n  vec3 fallC = year < 0.20 ? vec3(1.00,0.88,0.92)\n             : year < 0.72 ? vec3(0.88,0.52,0.16) : vec3(1.00,1.00,1.00);\n  if (fh > 0.965 - a.air*0.010)\n    col = mix(col, fallC*(0.45 + dayAmt*0.75),\n              smoothstep(0.16, 0.0, length(ff))*fallAmt*(0.5 + a.air*0.6));\n\n  // Vocals turn the light: the whole year goes golden for as long as it lasts.\n  col = mix(col, mix(col, vec3(1.00,0.84,0.52), 0.65), a.voice*(0.35 + iParam1*0.45)*dayAmt);\n  float ph2 = fract(t*0.05);\n  for (int b = 0; b < 4; b++){\n    float fb = float(b);\n    vec2 bp = vec2(mix(-1.0, 1.0, fract(ph2 + fb*0.06))*1.0, 0.34 + fb*0.016 + 0.04*sin(t*0.6 + fb));\n    vec2 q = p - bp; q.x = abs(q.x);\n    col = mix(col, vec3(0.12,0.12,0.15),\n              smoothstep(0.004, 0.0, abs(q.y - q.x*(0.42 + 0.3*sin(t*7.0 + fb))))*step(q.x, 0.017)*a.voice*0.95);\n  }\n\n  o = vec4(clamp(col, 0.0, 1.0), 1.0);\n}",
  },
  {
    id: "slit", n: 46, name: "Slit",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "The double slit. The field on the right is the exact superposition of two cylindrical waves and the pattern on the screen is the exact two-slit intensity, fringes inside a single-slit envelope, arriving as individual detections. The lead vocal is the which-path measurement: as it comes in, fringe visibility falls and the pattern collapses to two humps.",
    params: [{"name":"Rate","def":0.4},{"name":"Slit spacing","def":0.35},{"name":"Marker","def":0.4}],
    reacts: [["sub","The source"],["low","Brightness of the field either side of the barrier"],["pres","Wavelength — how tight the fringes run"],["sib","The nodal lines where the two waves cancel"],["voice","Measures which path — the interference collapses"],["hit","A burst of detections on the screen"]],
    body: "// SLIT — the double slit, with the one detail that makes it the experiment it is\n// rather than a wave picture. The field on the right is the exact superposition\n// of two cylindrical waves; the pattern on the screen is the exact two-slit\n// intensity, fringes inside a single-slit envelope; and the detections arriving\n// on it are individual, which is what you see if you run the thing one particle\n// at a time. The lead vocal is the which-path measurement: as it comes in, the\n// fringe visibility falls and the pattern collapses to two humps. That is not a\n// metaphor — visibility really is what you lose when you learn the path.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.55 + iParam0*0.85);\n\n  float k = 46.0 + a.pres*26.0;                    // wavenumber\n  float w = 5.5;                                   // and its frequency\n  float d = 0.115 + iParam1*0.06;                  // slit separation\n  float aw = 0.030;                                // slit width\n  float barX = -0.42, screenX = 0.68;\n\n  // Knowing the path destroys the fringes. Nothing else here changes.\n  float visib = clamp(1.0 - a.voice*1.35, 0.0, 1.0);\n\n  vec3 col = vec3(0.010,0.012,0.024);\n\n  vec2 s1 = vec2(barX,  d*0.5), s2 = vec2(barX, -d*0.5);\n\n  if (p.x < barX){\n    // Incoming plane wave, from the left.\n    float ph = k*(p.x + 1.0) - w*t;\n    float amp = cos(ph);\n    col += mix(vec3(0.10,0.20,0.55), vec3(0.35,0.70,1.00), amp*0.5 + 0.5) * 0.30;\n    col += vec3(0.55,0.80,1.00) * pow(max(amp, 0.0), 6.0) * (0.10 + comp(a.low,0.30)*0.55);\n  } else {\n    // Two cylindrical waves, superposed. This is the whole of the physics.\n    float r1 = length(p - s1), r2 = length(p - s2);\n    float a1 = cos(k*r1 - w*t)/sqrt(max(r1, 0.06));\n    float a2 = cos(k*r2 - w*t)/sqrt(max(r2, 0.06));\n    // With which-path information the two no longer add coherently.\n    float amp = a1 + a2*visib;\n    float inten = amp*amp;\n    col += mix(vec3(0.06,0.10,0.30), vec3(0.30,0.72,1.00), clamp(inten*0.55, 0.0, 1.0))\n         * (0.35 + comp(a.low, 0.30)*0.55);\n    col += vec3(0.60,0.88,1.00) * pow(max(amp, 0.0), 5.0) * (0.15 + a.pres*0.95) * 0.55;\n    // Nodal lines: the places the two waves cancel, which is where the dark\n    // fringes on the screen come from.\n    float nod = abs(r1 - r2)*k/TAU;\n    col += smoothstep(fwidth(nod)*1.6, 0.0, abs(fract(nod) - 0.5)) * (0.04 + a.sib*0.55)\n         * vec3(0.85,0.95,1.00) * visib;\n  }\n\n  // The barrier, with two gaps in it.\n  float slitMask = smoothstep(aw, aw*0.6, abs(abs(p.y) - d*0.5));\n  float bar = smoothstep(0.010, 0.006, abs(p.x - barX)) * (1.0 - slitMask);\n  col = mix(col, vec3(0.055,0.060,0.075), bar);\n  col += vec3(0.35,0.45,0.65) * smoothstep(0.012, 0.008, abs(p.x - barX)) * (1.0 - slitMask) * 0.35;\n\n  // The screen, and the exact two-slit intensity written onto it.\n  float L = screenX - barX;\n  float sinth = p.y/sqrt(p.y*p.y + L*L);\n  float fringe = cos(k*d*sinth*0.5);\n  float env = aw*k*sinth*0.5;\n  float sinc = abs(env) < 1e-3 ? 1.0 : sin(env)/env;\n  float I = (1.0 + visib*(2.0*fringe*fringe - 1.0)) * sinc*sinc;   // visibility folded in\n  I = clamp(I*0.5, 0.0, 1.0);\n\n  float onScreen = smoothstep(0.016, 0.004, abs(p.x - screenX));\n  col += onScreen * vec3(0.12,0.14,0.20);\n  // The accumulated pattern, drawn as a profile growing off the screen.\n  float prof = smoothstep(screenX, screenX + I*0.26, p.x) * smoothstep(screenX + I*0.26 + 0.006, screenX + I*0.26, p.x);\n  col += smoothstep(screenX - 0.004, screenX, p.x) * smoothstep(screenX + I*0.26, screenX + I*0.26 - 0.004, p.x)\n       * mix(vec3(0.20,0.35,0.75), vec3(0.55,0.85,1.00), I) * (0.45 + a.pres*0.75);\n\n  // Individual detections. They land where the intensity says they should, one\n  // at a time, and they are the reason this is an experiment and not a diagram.\n  vec2 dg = floor(vec2(p.x*220.0, p.y*220.0)) + floor(t*9.0)*vec2(1.0, 7.0);\n  float hh = h21(dg);\n  float hit = step(1.0 - I*0.030 - pu*0.010, hh) * onScreen;\n  col += hit * vec3(1.00,0.96,0.86) * (1.2 + pu*1.5);\n\n  // The source.\n  col += vec3(0.55,0.80,1.00) * exp(-length(p - vec2(-0.92, 0.0))*12.0) * (0.25 + comp(a.sub,0.32)*0.85);\n\n  // Vocals: the measurement itself, marked at the slits where it is made.\n  col += a.voice * (0.65 + iParam2*0.75) * vec3(1.00,0.82,0.40)\n       * (exp(-length(p - s1)*17.0) + exp(-length(p - s2)*17.0));\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.48);\n  o = vec4(pow(col, vec3(0.90)), 1.0);\n}",
  },
  {
    id: "entangled", n: 47, name: "Entangled",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "A pair prepared together and separated. Neither has a definite state until one is measured; when it is, both settle at once and the outcomes are always opposite. The curve below is the real correlation, E = −cos θ, against the best any local explanation manages. The band between them is one shared field, not a signal — nothing crosses that gap.",
    params: [{"name":"Rate","def":0.35},{"name":"Preparation","def":0.45}],
    reacts: [["sub","Radius of each shell"],["low","The glow around each particle"],["pres","Lobe brightness; the correlation curve"],["sib","The measured axis, once it is drawn"],["air","The shared band between them"],["voice","Prepares a fresh pair — the apparatus lights"],["hit","A measurement: both flash at the same instant"]],
    body: "// ENTANGLED — a pair prepared together and then separated. Neither has a\n// definite state until one is measured, and when it is, both are settled at\n// once: the outcomes are always opposite. The curve along the bottom is the\n// real correlation, E = −cos θ, drawn against the straight line that is the\n// best any local explanation can manage — the gap between them is the whole\n// argument.\n//\n// The band between the two is drawn as one shared field that brightens at both\n// ends simultaneously, deliberately, because nothing travels between them. No\n// signal crosses that gap. What you can see afterwards is that the answers agree.\n#define TAU 6.28318531\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.35 + iParam0*0.60);\n\n  vec3 col = vec3(0.010,0.011,0.024);\n  col += vec3(0.020,0.024,0.048)*(1.0 - smoothstep(0.0, 1.2, length(p)));\n\n  // Measurement events land on a fixed clock; how hard they land is the transient.\n  float mPeriod = 1.55;\n  float since = fract(t/mPeriod)*mPeriod;\n  float n = floor(t/mPeriod);\n  float flash = exp(-since*7.0)*(0.35 + pu*1.6);\n  // The analyser angle changes every run, and the outcome follows from it.\n  float theta = fract(n*0.618)*TAU;                  // golden step, so it never repeats\n  float outcome = h21(vec2(n, 3.0)) < 0.5 ? 1.0 : -1.0;\n\n  vec2 cL = vec2(-0.52, 0.10), cR = vec2(0.52, 0.10);\n  float settled = smoothstep(0.0, 0.22, since);      // before this, it is undecided\n\n  for (int i = 0; i < 2; i++){\n    float side = float(i)*2.0 - 1.0;                  // -1 left, +1 right\n    vec2 c = (i == 0) ? cL : cR;\n    vec2 q = p - c;\n    float r = length(q), ph = atan(q.y, q.x);\n    // Opposite outcomes: the two are always anti-correlated.\n    float axis = theta + (i == 0 ? 0.0 : 3.14159265) * 1.0;\n    float spin = outcome*side;\n\n    // Unmeasured, it is a smooth superposition — no orientation you can name.\n    // Undecided: a smooth ring with no orientation you could name.\n    float sup = pow(0.5 + 0.5*cos(ph*2.0 - t*1.3), 2.0)*0.35 + 0.65;\n    // Measured, it is a definite axis.\n    float def = pow(abs(cos(ph - axis)), 14.0);\n    float lobe = mix(sup, def, settled);\n\n    float shell = exp(-abs(r - (0.115 + comp(a.sub,0.32)*0.020))*30.0);\n    vec3 hue = spin > 0.0 ? vec3(1.00,0.42,0.55) : vec3(0.35,0.70,1.00);\n    hue = mix(vec3(0.65,0.60,0.95), hue, settled);     // colour only once decided\n\n    col += shell*lobe*hue*(0.85 + a.pres*1.60);\n    // A second, inner shell so it reads as a body rather than a halo.\n    col += exp(-abs(r - 0.055)*40.0)*hue*(0.30 + a.pres*0.80)*mix(0.5, 1.0, lobe);\n    col += exp(-r*(9.0 - a.voice*3.0))*hue*(0.10 + comp(a.low,0.30)*0.55);\n    // The axis, drawn once the measurement has been made.\n    float ax = smoothstep(0.006, 0.0, abs(dot(q, vec2(-sin(axis), cos(axis)))))\n             * smoothstep(0.20, 0.16, r);\n    col += ax*settled*(0.35 + a.sib*1.1)*vec3(1.00,0.95,0.85);\n    // Both flash at the same instant. That is the point.\n    col += flash*exp(-r*7.0)*vec3(1.00,0.94,0.80)*1.3;\n  }\n\n  // The shared field: one band, brightening at both ends together.\n  float mid = smoothstep(0.075, 0.0, abs(p.y - 0.10))*smoothstep(0.52, 0.14, abs(p.x));\n  float weave = pow(0.5 + 0.5*sin(p.x*24.0 + t*2.0)*cos(p.y*18.0 - t*1.4), 4.0);\n  col += mid*weave*(0.10 + a.air*0.95 + flash*1.20)*vec3(0.60,0.85,1.00);\n\n  // The correlation curve, and the bound a local theory cannot get past.\n  float gy = -0.36;\n  float u = clamp((p.x + 0.72)/1.44, 0.0, 1.0);\n  float ang = u*TAU;\n  float quantum = -cos(ang);\n  float classical = -1.0 + 2.0*abs(fract(ang/3.14159265 + 0.5) - 0.5)*2.0;\n  float inBox = smoothstep(0.74, 0.72, abs(p.x))*smoothstep(0.20, 0.18, abs(p.y - gy));\n  col += inBox*smoothstep(0.006, 0.0, abs(p.y - (gy + quantum*0.13)))\n       * (0.35 + a.pres*1.1)*vec3(0.55,0.95,1.00);\n  col += inBox*smoothstep(0.004, 0.0, abs(p.y - (gy + classical*0.13)))\n       * 0.30*vec3(0.55,0.55,0.65);\n  // Where we are on the curve right now.\n  float here = clamp(theta/TAU, 0.0, 1.0)*1.44 - 0.72;\n  col += inBox*smoothstep(0.020, 0.0, length(p - vec2(here, gy - cos(theta)*0.13)))\n       * (0.55 + flash*1.5)*vec3(1.00,0.88,0.55);\n  col += inBox*smoothstep(0.0015, 0.0, abs(p.y - gy))*0.12*vec3(0.5,0.55,0.7);\n\n  // Vocals prepare a fresh pair: the whole apparatus lights and the band swells.\n  col += a.voice*(0.55 + iParam1*0.75)*mid*vec3(1.00,0.86,0.52)*1.3;\n  col += a.voice*0.35*(exp(-length(p - cL)*5.0) + exp(-length(p - cR)*5.0))*vec3(1.00,0.90,0.66);\n\n  col *= 1.0 - smoothstep(0.75, 1.30, length(p))*0.40;\n  col = max(col, 0.0); col = col/(1.0 + col*0.50);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "tesseract", n: 48, name: "Tesseract",
    series: "IV", set: "equilibrium", setTitle: "Equilibrium",
    kit3d: false,
    line: "A four-dimensional cube turned in two planes and projected twice. It seems to turn inside out because both projections are perspective: the cell nearer in w looks larger, so as the solid rotates through w the near and far cells trade places. Nothing is distorted — it is an honest shadow of a solid that will not fit in the room.",
    params: [{"name":"Rate","def":0.35},{"name":"Size","def":0.4},{"name":"Edge weight","def":0.4}],
    reacts: [["sub","A wash through the whole solid"],["low","Pushes it through w — the inside-out moment"],["pres","Edge brightness, one bin per edge"],["sib","The vertices"],["air","The halo around each vertex"],["voice","Opens a third rotation plane — a second, gilded shadow"],["hit","A flash through the frame"]],
    body: "// TESSERACT — a four-dimensional cube, turned in two planes at once and then\n// projected twice: 4D to 3D, then 3D to 2D. The reason it seems to turn inside\n// out is that both projections are perspective, so the cell nearer in w looks\n// larger, and as the solid rotates through w the near and far cells trade\n// places. Nothing about it is distorted; you are watching an honest shadow of a\n// solid that will not fit in the room. Sixteen vertices, thirty-two edges, and\n// each edge takes one bin of the spectrum.\nfloat seg2(vec2 p, vec2 a, vec2 b){\n  vec2 pa = p - a, ba = b - a;\n  return length(pa - ba*clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0));\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 p = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.22 + iParam0*0.40);\n\n  // Two independent rotation planes. Rates are fixed — an angle built by\n  // multiplying elapsed time by an audio-driven rate jumps every time the rate\n  // moves, and a solid that stutters stops reading as a solid.\n  float aXW = t*0.62, aYZ = t*0.41, aXY = t*0.17;\n  float cw = cos(aXW), sw = sin(aXW);\n  float cz = cos(aYZ), sz = sin(aYZ);\n  float cxy = cos(aXY), sxy = sin(aXY);\n\n  float S = 0.62 + iParam1*0.30;\n\n  vec3 P[16];\n  float W[16];\n  for (int i = 0; i < 16; i++){\n    vec4 v = vec4(float(i & 1), float((i >> 1) & 1), float((i >> 2) & 1), float((i >> 3) & 1))*2.0 - 1.0;\n    // Rotate in the x–w plane, then y–z, then x–y.\n    vec4 r = v;\n    r = vec4(r.x*cw - r.w*sw, r.y, r.z, r.x*sw + r.w*cw);\n    r = vec4(r.x, r.y*cz - r.z*sz, r.y*sz + r.z*cz, r.w);\n    r = vec4(r.x*cxy - r.y*sxy, r.x*sxy + r.y*cxy, r.z, r.w);\n    // 4D to 3D: the cell nearer in w projects larger.\n    float kw = 2.6/(2.6 - r.w*0.85);\n    vec3 q = r.xyz*kw;\n    // 3D to 2D.\n    float kz = 3.1/(3.1 - q.z*0.75);\n    P[i] = vec3(q.xy*kz*S*0.42, kz);\n    W[i] = r.w;\n  }\n\n  vec3 col = vec3(0.008,0.010,0.022);\n  col += vec3(0.020,0.024,0.052)*(1.0 - smoothstep(0.0, 1.15, length(p)));\n\n  float aa = 1.6/iResolution.y;\n  float ei = 0.0;\n  for (int i = 0; i < 16; i++){\n    for (int b = 0; b < 4; b++){\n      if ((i & (1 << b)) != 0) continue;              // draw each edge once\n      int j = i | (1 << b);\n      vec3 A = P[i], B = P[j];\n      float d = seg2(p, A.xy, B.xy);\n      float depth = (A.z + B.z)*0.5;                  // nearer edges are brighter and fatter\n      float wm = (W[i] + W[j])*0.5;                   // and where they sit in the fourth axis\n\n      float e = SPEC(0.006 + fract(ei/32.0)*0.58);\n      float wdt = (0.0022 + 0.0016*(depth - 0.85) + e*0.0026)*(0.6 + iParam2*0.9);\n      wdt = max(wdt, 0.0010);\n\n      vec3 hue = ramp(vec3(0.20,0.35,1.00), vec3(0.35,0.95,0.95), vec3(1.00,0.40,0.75),\n                      clamp(wm*0.5 + 0.5, 0.0, 1.0));\n      float core = smoothstep(wdt, wdt - aa*1.5, d);\n      col += hue * core * (0.30 + pow(e, 1.15)*2.4) * (0.45 + depth*0.55);\n      col += hue * exp(-d*(90.0 - e*30.0)) * (0.03 + e*0.35) * (0.3 + depth*0.5);\n      ei += 1.0;\n    }\n  }\n\n  // The vertices, as points of light, sized by how near they are in w.\n  for (int i = 0; i < 16; i++){\n    float wm = clamp(W[i]*0.5 + 0.5, 0.0, 1.0);\n    float d = length(p - P[i].xy);\n    col += vec3(1.00,0.96,0.90)*smoothstep(0.010 + wm*0.006, 0.0, d)*(0.35 + a.sib*1.3);\n    col += vec3(0.55,0.80,1.00)*exp(-d*40.0)*(0.05 + a.air*0.55)*wm;\n  }\n\n  // The low end pushes the whole solid through w, so the inner cell swells and\n  // the outer one closes over it — the inside-out moment, on the bass.\n  col *= 0.75 + comp(a.low, 0.30)*0.75;\n  col += vec3(0.30,0.50,1.00)*comp(a.sub, 0.32)*0.10*(1.0 - smoothstep(0.0, 0.9, length(p)));\n\n  // Vocals open a third rotation plane: a second shadow, offset, gilded.\n  if (a.voice > 0.02){\n    float g = 0.0;\n    for (int i = 0; i < 16; i++){\n      for (int b = 0; b < 4; b++){\n        if ((i & (1 << b)) != 0) continue;\n        int j = i | (1 << b);\n        vec2 A = P[i].xy*(1.0 + a.voice*0.16), B = P[j].xy*(1.0 + a.voice*0.16);\n        g = max(g, smoothstep(0.0030, 0.0, seg2(p, A, B)));\n      }\n    }\n    col += g*a.voice*(0.85 + iParam2*0.6)*vec3(1.00,0.84,0.40);\n  }\n  col += pu*0.20*vec3(1.0)*smoothstep(0.9, 0.1, length(p));\n\n  col = max(col, 0.0); col = col/(1.0 + col*0.52);\n  o = vec4(pow(col, vec3(0.88)), 1.0);\n}",
  },
  {
    id: "hypergate-iii", n: 49, name: "Hypergate III",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "The first work in the library, rebuilt as architecture you fly through. Twenty panels around each ring, each still lit by its own slice of the spectrum — and no sun anywhere, because there is no sun inside a tunnel. Everything you see is lit by the panels.",
    params: [{"name":"Speed","def":0.4},{"name":"Field of view","def":0.35},{"name":"Helix","def":0.4}],
    reacts: [["sub","Inflates the barrel; haze in the corridor"],["low","Bounce light off the wall"],["pres","Panel hue; the specular on the alloy"],["sib","—"],["air","The struts between panels"],["voice","Corkscrews a band of light down the corridor"],["hit","Prismatic fringing, and a strike on the near rings"]],
    body: "// HYPERGATE III — the first work in the library, rebuilt as architecture you fly\n// through rather than a picture of a tunnel. The gate is a real ring-and-panel\n// structure repeated down a curving flight path; the camera follows that path\n// and banks by the actual lateral acceleration of it, so the roll leads the turn\n// the way it does in an aircraft and not after it. Twenty panels around each\n// ring, each still lit by its own slice of the spectrum.\n#define TAU 6.28318531\n\nvec3 path(float z){\n  // The corridor is not straight. Two slow sines of incommensurate period, so\n  // the flight never repeats and never quite settles.\n  return vec3(sin(z*0.11)*3.4 + sin(z*0.047)*2.1,\n              cos(z*0.083)*1.9 + sin(z*0.031)*1.2,\n              z);\n}\n\nvec2 map(vec3 p){\n  vec3 c = path(p.z);\n  vec2 q = p.xy - c.xy;                                 // into the corridor frame\n  float r = length(q);\n  float a = atan(q.y, q.x);\n\n  float RAD = 2.15;\n  // Ribs every 1.6 units down the corridor.\n  float zr = rep1(p.z, 1.6);\n  float rib = max(abs(r - RAD) - 0.22, abs(zr) - 0.16);\n  // Panels between the ribs, twenty around, with gaps for the struts.\n  float seg = TAU/20.0;\n  float sa = rep1(a, seg);\n  float wall = max(abs(r - RAD) - 0.10, abs(zr) - 0.62);\n  wall = max(wall, -(abs(sa) - seg*0.26));               // cut the panel gaps\n  // A rail running the full length at four points around.\n  float ra = rep1(a + seg*0.5, TAU/4.0);\n  float rail = max(abs(r - RAD + 0.26) - 0.05, abs(ra)*r - 0.05);\n\n  float d = min(min(rib, wall), rail);\n  float id = d == rib ? 1.0 : (d == wall ? 2.0 : 3.0);\n  return vec2(d*0.85, id);\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.55 + iParam0*0.85);\n\n  // Flight. Speed is constant — an audio-driven rate multiplied by elapsed time\n  // jumps every time the rate moves, and at this speed a jump is a teleport.\n  float z = t*4.6;\n  vec3 ro = path(z);\n  vec3 ta = path(z + 2.2);\n  float roll = autoBank(path(z - 1.1), ro, path(z + 1.1), 3.4) + sin(t*0.21)*0.09;\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.45 - iParam1*0.45));\n\n  vec2 h = raymarch(ro, rd, 46.0);\n  vec3 col = vec3(0.0);\n\n  // Fog colour is also the far colour, so the corridor recedes into it.\n  vec3 fogC = mix(vec3(0.020,0.030,0.075), vec3(0.10,0.04,0.14), 0.35 + a.pres*0.35);\n\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n\n    vec3 c = path(pos.z);\n    vec2 q = pos.xy - c.xy;\n    float ang = atan(q.y, q.x);\n    float bin = fract((ang/TAU + 0.5) + 0.02);\n    float e = SPEC(pow(bin, 2.0)*0.66 + 0.004);\n\n    // Full hue traverse around the ring, so the spectrum reads as colour and\n    // not as one tint at different brightnesses.\n    vec3 tint = ramp(vec3(0.06,0.16,1.00), vec3(0.10,0.95,0.85), vec3(1.00,0.18,0.52),\n                     fract(bin*1.6 + a.pres*0.14));\n    tint = mix(tint, vec3(1.00,0.72,0.22), smoothstep(0.55, 1.0, e)*0.5);\n\n    // Material: brushed dark alloy for the structure, emissive glass for panels.\n    float rough = h.y == 2.0 ? 0.30 : 0.55;\n    vec3 albedo = h.y == 2.0 ? tint*0.045 : vec3(0.014,0.016,0.024);\n    // Micro-detail, so nothing reads as a flat CG plane.\n    float grain = triNoise(pos*4.0, n, 3.0);\n    rough = clamp(rough + (grain - 0.5)*0.22, 0.06, 0.95);\n    albedo *= 0.80 + grain*0.45;\n\n    float ao = calcAO(pos, n);\n\n    // There is no sun inside a tunnel. The only light in here is the panels, so\n    // the room colour is the spectrum itself, gathered from four angles around\n    // the ring — and anything not facing a lit panel stays black. That falloff\n    // is the whole difference between a corridor and a lit cylinder.\n    vec3 room = vec3(0.0);\n    for (int i = 0; i < 4; i++){\n      float ba = fract(float(i)*0.25 + 0.125);\n      float be = SPEC(pow(ba, 2.0)*0.66 + 0.004);\n      room += ramp(vec3(0.06,0.16,1.00), vec3(0.10,0.95,0.85), vec3(1.00,0.18,0.52),\n                   fract(ba*1.6 + a.pres*0.14)) * pow(be, 1.7);\n    }\n    room *= 0.32;\n\n    vec3 inward = normalize(vec3(-q, 0.0) + 1e-5);\n    float wrap = max(dot(n, inward), 0.0)*0.75 + 0.25;      // wrap light off the wall\n\n    // Emission: the panels are the light in this room.\n    // Only the loud bins light. A panel that is always on is a wall, not a meter.\n    float emit = h.y == 2.0 ? (0.010 + pow(e, 2.0)*3.0) : (h.y == 3.0 ? (0.008 + a.air*0.8) : 0.003);\n    col += tint*emit;\n\n    col += albedo*room*wrap*ao*7.0;\n    col += albedo*vec3(0.10,0.14,0.26)*ao*(0.25 + comp(a.low, 0.30)*0.55);   // the faintest fill\n    // One specular, from the room, and it is the only sharp thing on the metal.\n    col += room*ggx(n, v, inward, rough)*(0.9 + a.pres*1.8)*ao;\n    col *= 0.30 + 0.70*ao;\n\n    // Vocals corkscrew a band of light down the corridor.\n    float helix = pow(0.5 + 0.5*cos(ang*3.0 - pos.z*0.9 + t*1.6), 8.0);\n    col += tint*helix*a.voice*(1.6 + iParam2*1.4);\n\n    col = mix(col, fogC, 1.0 - exp(-h.x*h.x*0.0042));\n  } else {\n    col = fogC*0.35;\n  }\n\n  // Volumetric haze in the corridor, sampled cheaply along the ray.\n  float haze = 0.0;\n  for (int i = 0; i < 8; i++){\n    float d = 2.0 + float(i)*4.2;\n    vec3 sp = ro + rd*d;\n    vec3 cc = path(sp.z);\n    float rr = length(sp.xy - cc.xy);\n    haze += exp(-rr*0.55)*exp(-d*0.045);\n  }\n  col += fogC*haze*(0.030 + comp(a.sub, 0.32)*0.055 + pu*0.030)*2.0;\n  col += vec3(1.00,0.92,0.75)*a.voice*0.10*haze;\n\n  col = aces(col*(0.78 + pu*0.20));\n  col *= 1.0 - smoothstep(0.55, 1.25, length(uv))*0.35;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "chrome-sea", n: 50, name: "Chrome Sea",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "Skimming a liquid metal ocean under a storm. The surface is a displaced heightfield marched directly and the normal comes from differences of the same height function, so the reflection is right down to the wavelet. Nearly everything you see is the sky, arriving second-hand.",
    params: [{"name":"Speed","def":0.4},{"name":"Field of view","def":0.35},{"name":"Shaft","def":0.45}],
    reacts: [["sub","Swell height"],["low","Swell, and the glint in the troughs"],["pres","Sky break behind the sun; the specular"],["sib","Roughness of the chop, and spray off the crests"],["voice","Opens a shaft through the cloud onto the water"]],
    body: "// CHROME SEA — skimming a liquid metal ocean under a storm. The surface is a\n// displaced heightfield marched directly rather than an SDF, which is both\n// cheaper and exact for a function of two variables; the normal comes from\n// finite differences of the same height function, so the reflection is right to\n// the wavelet. Nearly everything you see is the sky, arriving second-hand.\nvec2 map(vec3 p){ return vec2(1e9, -1.0); }      // unused: this one marches a heightfield\n\nfloat waveH(vec2 q, float t, float swell, int oct){\n  // Gerstner-ish stack: each octave shorter, steeper and turned a little, which\n  // is what stops an ocean reading as one sine.\n  float h = 0.0, amp = 0.30*swell, f = 0.55;\n  vec2 d = vec2(1.0, 0.35);\n  for (int i = 0; i < 6; i++){\n    if (i >= oct) break;\n    d = r2(0.72)*d;\n    float ph = dot(q, d)*f + t*(1.4 + float(i)*0.55);\n    h += amp*(sin(ph) - 0.28*sin(ph*2.0));       // sharpened crests\n    amp *= 0.52; f *= 1.92;\n  }\n  h += (fbm3(vec3(q*0.9, t*0.18), 3) - 0.5)*0.28*swell;\n  return h;\n}\n\nvec3 skyDome(vec3 rd, vec3 sun, float pres, float voice){\n  float up = clamp(rd.y*0.5 + 0.5, 0.0, 1.0);\n  vec3 sky = ramp(vec3(0.020,0.026,0.045), vec3(0.055,0.075,0.135), vec3(0.14,0.19,0.32), up);\n  // Storm cloud, thicker overhead.\n  float cl = fbm3(vec3(rd.xz/max(rd.y + 0.22, 0.10)*0.55, iTime*0.010), 4);\n  sky = mix(sky, vec3(0.030,0.033,0.045), smoothstep(0.42, 0.78, cl)*smoothstep(-0.02, 0.35, rd.y)*0.90);\n  // A break in the cloud where the sun is, which is the only warm thing here.\n  float sd = max(dot(rd, sun), 0.0);\n  sky += vec3(1.00,0.74,0.42)*pow(sd, 14.0)*(0.22 + pres*0.55);\n  sky += vec3(1.00,0.86,0.62)*pow(sd, 2600.0)*3.2;\n  sky = mix(sky, sky*vec3(1.20,1.02,0.86), voice*0.55);\n  return sky;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.45 + iParam0*0.65);\n\n  float swell = 0.55 + comp(a.sub, 0.32)*1.05 + comp(a.low, 0.30)*0.45;\n  int OCT = iResolution.y > 1300.0 ? 4 : 6;\n\n  // A long banking arc over the water. Height is fixed; the sea comes to it.\n  vec3 pth = vec3(sin(t*0.21)*9.0 + sin(t*0.083)*5.0, 2.6 + sin(t*0.13)*0.55, t*7.0);\n  vec3 pPrev = vec3(sin((t-0.4)*0.21)*9.0 + sin((t-0.4)*0.083)*5.0, 0.0, (t-0.4)*7.0);\n  vec3 pNext = vec3(sin((t+0.4)*0.21)*9.0 + sin((t+0.4)*0.083)*5.0, 0.0, (t+0.4)*7.0);\n  vec3 ro = pth;\n  vec3 ta = ro + normalize(pNext - pPrev)*6.0 + vec3(0.0, -0.55, 0.0);\n  float roll = autoBank(pPrev, vec3(pth.x, 0.0, pth.z), pNext, 1.05);\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.5 - iParam1*0.35));\n\n  vec3 sun = normalize(vec3(0.42, 0.13, 0.90));\n  vec3 col;\n\n  // March the heightfield: step until the ray passes below the surface, then\n  // bisect. Exact enough that the crests never alias into stair steps.\n  float tHit = -1.0;\n  if (rd.y < 0.0){\n    float tp = 0.0, hp = ro.y - waveH(ro.xz, t, swell, OCT);\n    for (int i = 0; i < 90; i++){\n      float d = 0.35 + float(i)*0.95;\n      vec3 sp = ro + rd*d;\n      float hh = sp.y - waveH(sp.xz, t, swell, OCT);\n      if (hh < 0.0){\n        tHit = mix(tp, d, hp/(hp - hh));         // one linear bisection is plenty\n        break;\n      }\n      tp = d; hp = hh;\n      if (d > 190.0) break;\n    }\n  }\n\n  if (tHit > 0.0){\n    vec3 pos = ro + rd*tHit;\n    float e = 0.035 + tHit*0.0035;                // widen the difference with distance\n    float h0 = waveH(pos.xz, t, swell, OCT);\n    vec3 n = normalize(vec3(h0 - waveH(pos.xz + vec2(e, 0.0), t, swell, OCT), e,\n                            h0 - waveH(pos.xz + vec2(0.0, e), t, swell, OCT)));\n    vec3 v = -rd;\n    vec3 rf = reflect(rd, n);\n    rf.y = abs(rf.y);\n\n    // Liquid metal: almost everything is reflection, tinted by the metal itself.\n    vec3 tint = mix(vec3(0.72,0.76,0.86), vec3(0.95,0.78,0.52), 0.20 + a.pres*0.45);\n    float fr = fresnel(max(dot(n, v), 0.0), 0.62);\n    col = skyDome(rf, sun, a.pres, a.voice)*tint*(0.20 + fr*0.85);\n    // The sun's own reflection, roughened by the chop.\n    float rough = 0.055 + a.sib*0.10;\n    col += vec3(1.00,0.88,0.66)*ggx(n, v, sun, rough)*(1.6 + a.pres*3.2)*tint;\n    // Sub-surface glint in the troughs, which is what gives mercury its weight.\n    col += tint*0.012*(0.35 + comp(a.low,0.30))*max(-h0*1.4, 0.0);\n    // Spray off the crests, on the sibilance.\n    float crest = smoothstep(0.22*swell, 0.42*swell, h0);\n    col += vec3(0.90,0.94,1.00)*crest*(0.05 + a.sib*0.85)\n         * step(0.965 - a.sib*0.02, h21(floor(pos.xz*(9.0 + pu*6.0) + vec2(0.0, t*3.0))));\n    col = mix(col, skyDome(rd, sun, a.pres, a.voice), 1.0 - exp(-tHit*0.0075));\n  } else {\n    col = skyDome(rd, sun, a.pres, a.voice);\n  }\n\n  // Vocals lift a shaft of light out of the cloud break onto the water.\n  float shaft = pow(max(dot(rd, sun), 0.0), 3.0)*smoothstep(0.35, -0.15, rd.y);\n  col += vec3(1.00,0.80,0.50)*shaft*a.voice*(0.45 + iParam2*0.55);\n  col += vec3(1.0)*pu*0.045;\n\n  col = aces(col*0.80);\n  col *= 1.0 - smoothstep(0.58, 1.28, length(uv))*0.32;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "nave", n: 51, name: "Nave",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "Flying the length of a gothic church. A pointed arch is the intersection of two circles struck from opposite springing points — which is how it was set out with a compass on the tracing floor, and how it is built here. Light enters only through the clerestory.",
    params: [{"name":"Speed","def":0.4},{"name":"Field of view","def":0.35},{"name":"Shafts","def":0.45}],
    reacts: [["low","Weight of the light, and the air it travels through"],["pres","Warmth of the stone; the fill off the vault"],["sib","Specular on the limestone"],["voice","Raises the whole interior and flares the shafts"],["hit","Dust turning over in the light"]],
    body: "// NAVE — flying the length of a gothic church. A pointed arch is the intersection\n// of two circles struck from opposite springing points, which is exactly how it\n// was set out with a compass on the tracing floor, and exactly how it is built\n// here. Clustered piers, a clerestory above the aisle roof, and light that only\n// enters through the windows — so the vaults are lit from the side, which is what\n// makes the interior of a church look the way it does.\n#define TAU 6.28318531\n\nfloat archD(vec3 p, float span, float rise, float thick){\n  // Two struck circles, intersected: the pointed arch.\n  float d1 = length(vec2(abs(p.x) + span*0.5, p.y)) - span;\n  float d = max(d1, -p.y - rise);\n  return abs(d) - thick;\n}\n\nvec2 map(vec3 p){\n  float bay = 5.2;\n  vec3 q = p; q.z = rep1(p.z, bay);\n\n  // Piers: a cluster of shafts, which is the gothic tell against a plain column.\n  float pier = 1e9;\n  for (int i = 0; i < 5; i++){\n    float fi = float(i);\n    float ang = fi*TAU/5.0;\n    vec2 off = vec2(cos(ang), sin(ang))*0.24;\n    pier = min(pier, sdCyl(vec3(abs(q.x) - 3.4 + off.x, q.y - 3.0, q.z + off.y), 0.13, 6.2));\n  }\n  pier = min(pier, sdCyl(vec3(abs(q.x) - 3.4, q.y - 3.0, q.z), 0.30, 6.0));\n\n  // The arcade: pointed arches between the piers, down both sides.\n  float arc = archD(vec3(q.z, q.y - 4.4, 0.0), 2.35, 0.0, 0.26);\n  arc = max(arc, abs(abs(q.x) - 3.4) - 0.34);\n\n  // The vault: two arcs crossing overhead, plus the ribs on the groin.\n  float vaultA = archD(vec3(p.x, p.y - 6.4, 0.0), 3.9, 0.0, 0.22);\n  vaultA = max(vaultA, abs(q.z) - 2.5);\n  float vaultB = archD(vec3(q.z, p.y - 6.4, 0.0), 3.9, 0.0, 0.22);\n  vaultB = max(vaultB, abs(p.x) - 3.7);\n  float vault = min(vaultA, vaultB);\n  // The web between the ribs, so the ceiling is a surface and not a cage.\n  float shell = abs(length(vec2(abs(p.x)*0.92, p.y - 6.4)) - 3.9) - 0.10;\n  shell = max(shell, 6.4 - p.y);\n  vault = min(vault, shell);\n\n  // Outer wall, and the clerestory openings cut through it.\n  float wall = abs(abs(p.x) - 5.4) - 0.30;\n  wall = max(wall, p.y - 12.0);\n  wall = max(wall, -(p.y - 0.0));\n  float win = archD(vec3(q.z, p.y - 8.4, 0.0), 1.35, 0.0, -1.0);   // negative thickness: a hole\n  win = max(win, abs(abs(p.x) - 5.4) - 0.9);\n  wall = max(wall, -win);\n\n  float floorD = p.y + 0.0;\n\n  vec2 d = vec2(pier, 1.0);\n  d = opU(d, vec2(arc, 1.0));\n  d = opU(d, vec2(vault, 2.0));\n  d = opU(d, vec2(wall, 3.0));\n  d = opU(d, vec2(floorD, 4.0));\n  d.x *= 0.80;\n  return d;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.40 + iParam0*0.60);\n\n  float z = t*3.4;\n  vec3 ro = vec3(sin(z*0.09)*1.5, 2.4 + sin(z*0.07)*0.65, z);\n  vec3 pPrev = vec3(sin((z-1.4)*0.09)*1.5, 0.0, z - 1.4);\n  vec3 pNext = vec3(sin((z+1.4)*0.09)*1.5, 0.0, z + 1.4);\n  vec3 ta = vec3(sin((z + 5.0)*0.09)*1.5, 4.2, z + 5.0);\n  float roll = autoBank(pPrev, vec3(ro.x, 0.0, ro.z), pNext, 5.5);\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.35 - iParam1*0.30));\n\n  vec2 h = raymarch(ro, rd, 60.0);\n  vec3 col = vec3(0.0);\n\n  // The light comes in sideways through the clerestory, and nowhere else.\n  vec3 sun = normalize(vec3(0.88, 0.34, 0.24));\n  vec3 warm = mix(vec3(1.00,0.82,0.55), vec3(1.00,0.70,0.40), a.pres*0.5);\n  vec3 cool = vec3(0.16,0.22,0.40);\n  vec3 fogC = mix(vec3(0.055,0.065,0.100), warm*0.16, 0.35 + a.pres*0.35);\n\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n\n    // Limestone: pale, dusty, and it takes a texture badly if it is too smooth.\n    float grain = triNoise(pos*1.6, n, 2.6)*0.6 + triNoise(pos*7.0, n, 5.0)*0.4;\n    vec3 albedo = mix(vec3(0.44,0.41,0.36), vec3(0.58,0.55,0.49), grain);\n    if (h.y == 4.0) albedo = mix(vec3(0.13,0.12,0.13), vec3(0.26,0.25,0.26), grain);   // slabs\n    float rough = clamp(0.72 - grain*0.20, 0.25, 0.95);\n    if (h.y == 4.0) rough = 0.40;\n\n    float ao = calcAO(pos, n);\n    float sh = softShadow(pos + n*0.02, sun, 0.05, 22.0, 7.0);\n\n    col += albedo*warm*max(dot(n, sun), 0.0)*sh*(3.6 + comp(a.low, 0.30)*2.2);\n    // Sky fill through the windows, bounced off the vault.\n    col += albedo*cool*(0.30 + 0.70*max(n.y, 0.0))*ao*(1.30 + a.pres*0.85);\n    col += warm*ggx(n, v, sun, rough)*sh*(0.35 + a.sib*1.4);\n    col *= 0.28 + 0.72*ao;\n\n    // The windows themselves: each bay takes a slice of the spectrum.\n    float bayIdx = floor(pos.z/5.2);\n    float e = SPEC(0.006 + fract(bayIdx*0.137)*0.56);\n    float glow = smoothstep(7.4, 9.4, pos.y)*smoothstep(6.2, 5.0, abs(pos.x));\n    col += ramp(vec3(0.20,0.35,1.00), vec3(1.00,0.30,0.35), vec3(1.00,0.85,0.35),\n                fract(bayIdx*0.137 + a.pres*0.2))*glow*(0.10 + pow(e, 1.5)*2.6);\n\n    col = mix(col, fogC, 1.0 - exp(-h.x*h.x*0.0016));\n  } else {\n    col = fogC;\n  }\n\n  // Light shafts from the clerestory, and dust in them. The low end thickens the\n  // air, so the whole nave breathes on the bass.\n  float shaft = 0.0;\n  for (int i = 0; i < 14; i++){\n    float d = 1.5 + float(i)*3.4;\n    if (d > h.x && h.y > 0.0) break;\n    vec3 sp = ro + rd*d;\n    // Only where a window would let light through: high, and in a bay.\n    float band = smoothstep(6.6, 9.0, sp.y)*smoothstep(12.6, 9.5, sp.y);\n    float bayw = pow(0.5 + 0.5*cos(sp.z*TAU/5.2), 6.0);\n    float lat = exp(-abs(abs(sp.x) - 2.2)*0.55);\n    shaft += band*bayw*lat*exp(-d*0.030)*(0.8 + fbm3(sp*0.35 + vec3(0.0, 0.0, t*0.2), 3)*0.9);\n  }\n  col += warm*shaft*(0.030 + comp(a.low, 0.30)*0.055 + pu*0.026);\n\n  // Vocals raise the whole interior: the stone warms and the shafts flare.\n  col += warm*a.voice*(0.35 + iParam2*0.55)*shaft*0.030;\n  col += warm*a.voice*0.045;\n\n  col = aces(col*1.15);\n  col *= 1.0 - smoothstep(0.60, 1.30, length(uv))*0.34;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "magma", n: 52, name: "Magma",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "Descending a basalt vault over a river of melt. The light comes from below, which is the opposite of every other room and the reason it looks unearthly. Colour is off the Planck locus: the melt is a real temperature field and the crust skinning over it is the same material a few hundred degrees cooler.",
    params: [{"name":"Speed","def":0.4},{"name":"Field of view","def":0.35},{"name":"Fissure","def":0.45}],
    reacts: [["sub","Surge in the flow; how far the crust splits"],["low","Temperature of the melt, and the glow on the rock"],["sib","Specular on wet basalt"],["air","Embers lifting off the flow"],["voice","Cracks the vault and floods the tube with light"],["hit","A pulse through the melt"]],
    body: "// MAGMA — descending a basalt vault over a river of melt. Everything visible is\n// lit by the melt itself, so the rock is only bright where it faces the flow and\n// the ceiling is nearly black — the light in a lava tube comes from below, which\n// is the opposite of every other room and the reason it looks unearthly. Colour\n// is off the Planck locus again: the melt is a genuine temperature field, and the\n// crust that skins over it is the same material a few hundred degrees cooler.\nvec3 blackbody(float K){\n  K = clamp(K, 1000.0, 6500.0)/100.0;\n  float r = K <= 66.0 ? 255.0 : 329.7*pow(K - 60.0, -0.1332);\n  float g = K <= 66.0 ? 99.47*log(K) - 161.12 : 288.12*pow(K - 60.0, -0.0755);\n  float b = K >= 66.0 ? 255.0 : (K <= 19.0 ? 0.0 : 138.52*log(K - 10.0) - 305.04);\n  return clamp(vec3(r, g, b)/255.0, 0.0, 1.0);\n}\nfloat lavaY(vec2 q, float t, float surge){\n  return -2.6 + (fbm3(vec3(q*0.30, t*0.22), 3) - 0.5)*(0.55 + surge*0.65)\n              + sin(q.y*0.55 - t*1.5)*0.10*(0.5 + surge);\n}\nvec2 map(vec3 p){\n  // The vault: a rough tube, columnar-jointed. Basalt cools into hexagons, so\n  // the wall is faceted rather than smooth.\n  float r = length(p.xy - vec2(sin(p.z*0.10)*2.2, cos(p.z*0.07)*1.4));\n  float tube = 6.2 - r;\n  // Columnar jointing, from a hex-ish cell field on the wall.\n  vec3 wq = p*0.55;\n  float cell = fbm3(wq*1.6, 4);\n  tube += (cell - 0.5)*1.35;\n  tube += fbm3(p*2.2, 3)*0.28;\n  // Stalactites hanging out of the roof.\n  vec3 sq = vec3(rep1(p.x, 3.4), p.y, rep1(p.z, 3.4));\n  float drip = sdCapsule(sq, vec3(0.0, 5.6, 0.0), vec3(0.0, 2.4 + fbm3(floor(p/3.4)*7.0, 2)*2.2, 0.0), 0.30);\n  float d = min(tube, drip);\n  return vec2(d*0.65, d == tube ? 1.0 : 2.0);\n}\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.40 + iParam0*0.60);\n  float surge = comp(a.sub, 0.32)*0.8 + comp(a.low, 0.30)*0.5;\n\n  float z = t*3.0;\n  vec3 ro = vec3(sin(z*0.10)*2.2, 1.4 + sin(z*0.09)*0.8, z);\n  vec3 pPrev = vec3(sin((z-1.2)*0.10)*2.2, 0.0, z-1.2);\n  vec3 pNext = vec3(sin((z+1.2)*0.10)*2.2, 0.0, z+1.2);\n  vec3 ta = vec3(sin((z+4.5)*0.10)*2.2, -0.4, z + 4.5);\n  float roll = autoBank(pPrev, vec3(ro.x, 0.0, ro.z), pNext, 5.0);\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.40 - iParam1*0.30));\n\n  vec3 col = vec3(0.004,0.003,0.004);\n  vec2 h = raymarch(ro, rd, 55.0);\n\n  // The melt surface, marched as a heightfield under the rock.\n  float lt = -1.0;\n  if (rd.y < 0.0){\n    float tp = 0.0, hp = ro.y - lavaY(ro.xz, t, surge);\n    for (int i = 0; i < 60; i++){\n      float d = 0.4 + float(i)*0.85;\n      vec3 sp = ro + rd*d;\n      float hh = sp.y - lavaY(sp.xz, t, surge);\n      if (hh < 0.0){ lt = mix(tp, d, hp/(hp - hh)); break; }\n      tp = d; hp = hh; if (d > 55.0) break;\n    }\n  }\n  bool lavaFirst = lt > 0.0 && (h.y < 0.0 || lt < h.x);\n\n  // The melt, and the crust skinning over it.\n  vec3 lavaCol = vec3(0.0); float lavaGlow = 0.0;\n  if (lt > 0.0){\n    vec3 lp = ro + rd*lt;\n    // Crust: cool plates carried on the flow, splitting where it stretches.\n    float flow = fbm3(vec3(lp.xz*0.32 + vec2(0.0, -t*0.55), t*0.10), 4);\n    float crack = abs(fract(flow*5.0) - 0.5)*2.0;\n    float crust = smoothstep(0.16, 0.52, crack);\n    float T = mix(1620.0 + surge*700.0, 720.0, crust*0.96);\n    lavaCol = blackbody(T)*(0.05 + pow(1.0 - crust, 2.0)*1.35)*(0.55 + surge*0.55);\n    lavaCol += blackbody(2200.0)*pu*0.45*pow(1.0 - crust, 2.0);\n    lavaGlow = (1.0 - crust);\n  }\n\n  if (h.y > 0.0 && !lavaFirst){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n    float grain = triNoise(pos*3.0, n, 4.0)*0.6 + triNoise(pos*11.0, n, 8.0)*0.4;\n    vec3 albedo = mix(vec3(0.020,0.019,0.021), vec3(0.075,0.070,0.068), grain);\n    float rough = clamp(0.85 - grain*0.30, 0.30, 0.98);\n    float ao = calcAO(pos, n);\n\n    // The only light is the melt, and it is underneath.\n    vec3 toL = normalize(vec3(0.0, -1.0, -0.25));\n    float dist = max(pos.y - lavaY(pos.xz, t, surge), 0.4);\n    vec3 lightCol = blackbody(1650.0 + surge*800.0)*(1.0/(1.0 + dist*dist*0.10));\n    float lsh = softShadow(pos + n*0.03, toL, 0.06, 9.0, 5.0);\n    col = albedo*lightCol*max(dot(n, toL), 0.0)*(2.2 + surge*1.8)*mix(0.30, 1.0, lsh);\n    col += albedo*lightCol*0.25*ao;                        // bounce\n    col += lightCol*ggx(n, v, toL, rough)*(0.55 + a.sib*1.6)*ao;\n    // Cracks in the rock still glowing where the melt has not yet drained.\n    float vein = smoothstep(0.62, 0.80, fbm3(pos*1.5, 4));\n    col += blackbody(1400.0)*vein*(0.05 + comp(a.low,0.30)*0.55)*ao;\n    col *= 0.30 + 0.70*ao;\n    col = mix(col, vec3(0.006,0.004,0.004), 1.0 - exp(-h.x*h.x*0.0018));\n  } else if (lavaFirst){\n    col = lavaCol;\n    col = mix(col, blackbody(1200.0)*0.05, 1.0 - exp(-lt*lt*0.0012));\n  }\n\n  // Heat haze and airborne ash, both densest just above the melt.\n  float glow = 0.0;\n  for (int i = 0; i < 12; i++){\n    float d = 1.0 + float(i)*3.6;\n    vec3 sp = ro + rd*d;\n    float above = max(sp.y - lavaY(sp.xz, t, surge), 0.0);\n    glow += exp(-above*0.55)*exp(-d*0.045)*(0.6 + fbm3(sp*0.5 + vec3(0.0, -t*1.2, 0.0), 3));\n  }\n  col += blackbody(1500.0 + surge*700.0)*glow*(0.007 + surge*0.012 + pu*0.008);\n\n  // Embers lifting off the flow on the air band.\n  vec2 eg = floor(uv*(120.0 + a.air*60.0) + vec2(0.0, -t*4.0));\n  col += blackbody(2400.0)*step(0.9955 - a.air*0.006, h21(eg))*(0.35 + a.air*2.0)\n       * smoothstep(0.5, -0.4, uv.y);\n\n  // Vocals crack the vault: a fissure opens and floods the tube with light.\n  col += blackbody(2900.0)*a.voice*(0.35 + iParam2*0.65)*glow*0.030;\n  col += blackbody(3100.0)*a.voice*0.10*lavaGlow;\n\n  col = aces(col*0.72);\n  col *= 1.0 - smoothstep(0.58, 1.28, length(uv))*0.38;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "shard-storm", n: 53, name: "Shard Storm",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "A field of crystal turning in the dark. Each shard actually refracts: the ray enters, bends, crosses the solid and bends again on the way out — and the three colour channels bend by different amounts, which is dispersion, and the reason a prism makes a spectrum.",
    params: [{"name":"Speed","def":0.35},{"name":"Field of view","def":0.35},{"name":"Dispersion","def":0.4}],
    reacts: [["pres","The studio strips the crystal reflects; facet rims"],["sib","How far the three channels come apart"],["air","Dust hanging in the field"],["voice","Brings the third strip up hard"],["hit","A white fracture flashes through the crystal"]],
    body: "// SHARD STORM — a field of crystal turning in the dark, and the camera through\n// the middle of it. Each shard is refractive: the ray enters, bends, crosses the\n// solid, bends again on the way out, and the three colour channels bend by\n// slightly different amounts because the index of refraction is not the same for\n// all of them. That last part is dispersion, and it is the reason a prism makes a\n// spectrum — done here properly rather than by tinting the edges.\n#define TAU 6.28318531\n\nfloat shardD(vec3 p, float seed, float grow){\n  // An octahedron cut by two planes: a crystal habit, not a generic blob.\n  vec3 q = p;\n  q.xz = r2(seed*3.1 + iTime*0.20)*q.xz;\n  q.xy = r2(seed*5.7 + iTime*0.14)*q.xy;\n  float s = 0.72 + fract(seed*7.3)*0.55;\n  float d = (abs(q.x) + abs(q.y*1.35) + abs(q.z)) - s*grow;\n  d *= 0.5773;\n  d = max(d, abs(q.y) - s*grow*1.05);        // clip the poles: a tabular habit\n  d = max(d, abs(q.x) - s*grow*0.95);        // and one prism face\n  return d;\n}\nvec2 map(vec3 p){\n  // A lattice of shards, one per cell, jittered so the field is not a grid.\n  float cell = 2.6;\n  vec3 id = floor(p/cell + 0.5);\n  vec3 q = p - cell*id;\n  float seed = h31(id);\n  vec3 jit = (vec3(h31(id + 1.7), h31(id + 3.9), h31(id + 5.3)) - 0.5)*0.9;\n  float e = SPEC(0.006 + fract(seed*3.0)*0.56);\n  float grow = 0.85 + e*0.65;\n  float d = shardD(q - jit, seed, grow);\n  return vec2(d*0.75, 1.0 + step(0.5, fract(seed*11.0)));\n}\n\nvec3 env(vec3 rd, float pres, float voice){\n  float up = clamp(rd.y*0.5 + 0.5, 0.0, 1.0);\n  vec3 c = ramp(vec3(0.010,0.014,0.040), vec3(0.05,0.09,0.22), vec3(0.16,0.10,0.34), up);\n  // Three studio strips, which is what the shards will be reflecting.\n  c += vec3(1.00,0.30,0.55)*pow(max(dot(rd, normalize(vec3( 0.7, 0.6,-0.4))), 0.0), 28.0)*(1.2 + pres*2.4);\n  c += vec3(0.20,0.80,1.00)*pow(max(dot(rd, normalize(vec3(-0.8, 0.2, 0.5))), 0.0), 20.0)*(0.9 + pres*1.6);\n  c += vec3(1.00,0.80,0.35)*pow(max(dot(rd, normalize(vec3( 0.1,-0.8, 0.6))), 0.0), 34.0)*(0.6 + voice*3.2);\n  return c;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.35 + iParam0*0.55);\n\n  // A drifting orbit that also translates, so shards keep arriving.\n  float z = t*2.4;\n  vec3 ro = vec3(sin(z*0.16)*2.6, cos(z*0.11)*1.8, z);\n  vec3 pPrev = vec3(sin((z-1.0)*0.16)*2.6, 0.0, z-1.0);\n  vec3 pNext = vec3(sin((z+1.0)*0.16)*2.6, 0.0, z+1.0);\n  vec3 ta = vec3(sin((z+3.5)*0.16)*2.6, cos((z+3.5)*0.11)*1.8, z + 3.5);\n  float roll = autoBank(pPrev, vec3(ro.x, 0.0, ro.z), pNext, 6.0) + t*0.05;\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.5 - iParam1*0.4));\n\n  vec3 col = env(rd, a.pres, a.voice);\n  vec2 h = raymarch(ro, rd, 26.0);\n\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n    float ndv = max(dot(n, v), 0.0);\n    float fr = fresnel(ndv, 0.09);\n\n    // Dispersion: one index per channel. Air to glass is about 1.5, and the\n    // spread here is exaggerated maybe fivefold so it reads at this scale.\n    float spread = 0.030 + a.sib*0.055 + iParam2*0.045;\n    float ior = 1.48;\n    vec3 rr = reflect(rd, n);\n    vec3 refr = vec3(0.0);\n    for (int k = 0; k < 3; k++){\n      float eta = 1.0/(ior + (float(k) - 1.0)*spread);\n      vec3 rdr = refract(rd, n, eta);\n      if (dot(rdr, rdr) < 1e-6) rdr = rr;                 // total internal reflection\n      // March the inside of the shard, then leave it.\n      vec3 ip = pos - n*0.02;\n      float it = 0.0;\n      for (int i = 0; i < 26; i++){\n        float d = -map(ip + rdr*it).x;                    // inside, the field is negative\n        if (d < 0.001) break;\n        it += max(d, 0.01);\n        if (it > 3.0) break;\n      }\n      vec3 ep = ip + rdr*it;\n      vec3 en = -calcNormal(ep);\n      vec3 outd = refract(rdr, en, ior + (float(k) - 1.0)*spread);\n      if (dot(outd, outd) < 1e-6) outd = reflect(rdr, en);\n      // Beer absorption through the body, tinted by the path length.\n      float absorb = exp(-it*(0.55 + 0.35*float(k)));\n      refr[k] = env(outd, a.pres, a.voice)[k]*absorb;\n    }\n\n    vec3 idc = floor((pos)/2.6 + 0.5);\n    float e = SPEC(0.006 + fract(h31(idc)*3.0)*0.56);\n\n    col = mix(refr, env(rr, a.pres, a.voice), fr);\n    // Edge glow: the facet rims catch every light in the room at once.\n    float edge = pow(1.0 - ndv, 5.0);\n    col += edge*(0.20 + pow(e, 1.4)*2.6)\n         * ramp(vec3(0.25,0.55,1.00), vec3(1.00,0.35,0.75), vec3(1.00,0.90,0.45), fract(h31(idc)*3.0));\n    col += vec3(1.0)*ggx(n, v, normalize(vec3(0.7,0.6,-0.4)), 0.06)*(2.5 + a.pres*4.0);\n    // A transient cracks the crystal: a white fracture flashes through it.\n    col += vec3(0.90,0.95,1.00)*pu*edge*2.2;\n    col = mix(col, env(rd, a.pres, a.voice)*0.6, 1.0 - exp(-h.x*h.x*0.0055));\n  }\n\n  // Fine dust hanging in the field, catching the strips.\n  vec2 dg = floor(uv*260.0 + vec2(t*3.0, 0.0));\n  col += step(0.9975 - a.air*0.004, h21(dg))*(0.25 + a.air*1.5)*vec3(0.8,0.9,1.0);\n\n  col = aces(col*0.88);\n  col *= 1.0 - smoothstep(0.58, 1.28, length(uv))*0.36;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "gimbal", n: 54, name: "Gimbal",
    series: "V", set: "vertigo", setTitle: "Vertigo",
    kit3d: true,
    line: "Seven nested rings, each turning inside the last about an axis perpendicular to it. Seven ratios, none commensurate, so the mechanism never returns to a pose it has held. Polished steel and brass, lit by three shop lights and nothing else.",
    params: [{"name":"Orbit","def":0.35},{"name":"Distance","def":0.35},{"name":"Gyro","def":0.4}],
    reacts: [["low","The blue key, and the gyro at the centre"],["pres","The warm key, and the fresnel on the silhouette"],["sib","The third light, raking"],["air","Dust in the shop light"],["voice","Spins the gyro up — light out through the nest"],["hit","A flash off the core"]],
    body: "// GIMBAL — a machine of nested rings, each turning inside the last about an axis\n// perpendicular to it, which is what a gimbal is and why the innermost one can\n// point anywhere. Seven rings, seven ratios, none of them commensurate, so the\n// mechanism never returns to a pose it has held before. Polished steel and\n// brass, lit by three shop lights and nothing else.\n#define TAU 6.28318531\n\nfloat ringD(vec3 p, float R, float th, float wd){\n  // A ring of rectangular section, not a torus — machined stock has flats.\n  float d = abs(length(p.xz) - R) - th;\n  return max(d, abs(p.y) - wd);\n}\n\nvec2 map(vec3 p){\n  vec2 res = vec2(1e9, -1.0);\n  vec3 q = p;\n  float T = iTime;\n  for (int i = 0; i < 7; i++){\n    float fi = float(i);\n    // Each ring turns about an axis at right angles to the one outside it, and\n    // at its own rate. Rates are fixed: a gimbal that stutters is a broken gimbal.\n    float rate = 0.19 + fi*0.077;\n    float ang = T*rate + fi*1.1;\n    if (mod(fi, 2.0) < 1.0) q.yz = r2(ang)*q.yz;\n    else                    q.xy = r2(ang)*q.xy;\n\n    float R  = 2.15 - fi*0.255;\n    float th = 0.048 + fi*0.004;\n    float wd = 0.085 - fi*0.006;\n    float e  = SPEC(0.006 + fi*0.075);\n    float d  = ringD(q, R, th, wd);\n    // Trunnions: the pins each ring turns on, and they must sit on the ring.\n    float pin = sdCyl(vec3(q.y, length(q.xz) - R, q.z*0.0 + q.x*0.0), 0.052, wd + 0.075);\n    pin = sdCapsule(q, vec3(R - 0.12, 0.0, 0.0), vec3(R + 0.14, 0.0, 0.0), 0.050);\n    pin = min(pin, sdCapsule(q, vec3(-R - 0.14, 0.0, 0.0), vec3(-R + 0.12, 0.0, 0.0), 0.050));\n    res = opU(res, vec2(min(d, pin), 1.0 + mod(fi, 2.0)));\n    // Graduation marks cut into the outer face of every other ring.\n    if (mod(fi, 3.0) < 1.0){\n      float a2 = atan(q.z, q.x);\n      float tick = max(abs(length(q.xz) - R - th*0.5) - 0.030,\n                       abs(rep1(a2, TAU/72.0))*R - 0.008);\n      tick = max(tick, abs(q.y) - wd*0.65);\n      res = opU(res, vec2(tick, 3.0));\n    }\n  }\n  // The gyro at the middle: a heavy disc, and the only thing not a ring.\n  vec3 gq = p;\n  gq.xz = r2(T*0.83)*gq.xz;\n  gq.yz = r2(0.42)*gq.yz;\n  float core = sdCyl(gq, 0.38, 0.10);\n  core = max(core, -sdCyl(gq, 0.20, 0.2));\n  core = min(core, sdSphere(p, 0.13));\n  res = opU(res, vec2(core, 4.0));\n  res.x *= 0.85;\n  return res;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.45);\n\n  // A close orbit that also rises and falls, so you read the depth of the nest.\n  float orb = t*0.30;\n  float rad = 5.4 - iParam1*1.4;\n  vec3 ro = vec3(sin(orb)*rad, 1.5 + sin(orb*0.63)*1.6, cos(orb)*rad);\n  vec3 ta = vec3(0.0, 0.0, 0.0);\n  float roll = sin(orb*0.41)*0.12;\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.9));\n\n  vec3 col = vec3(0.0);\n  // Three lights, three colours, and the room is otherwise black.\n  vec3 L1 = normalize(vec3( 0.55, 0.70, 0.45)), C1 = vec3(1.00,0.94,0.86);\n  vec3 L2 = normalize(vec3(-0.70, 0.25, 0.30)), C2 = vec3(0.30,0.55,1.00);\n  vec3 L3 = normalize(vec3( 0.10,-0.60,-0.75)), C3 = vec3(1.00,0.55,0.22);\n\n  vec2 h = raymarch(ro, rd, 24.0);\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n\n    // Steel for the odd rings, brass for the even, blued steel for the core.\n    vec3 f0 = h.y == 1.0 ? vec3(0.56,0.58,0.62)\n            : h.y == 2.0 ? vec3(0.78,0.62,0.28)\n            : h.y == 3.0 ? vec3(0.90,0.90,0.92) : vec3(0.32,0.36,0.48);\n    // Turning marks: a machined part is anisotropic, and the grain follows the cut.\n    float lathe = triNoise(pos*vec3(1.0, 60.0, 1.0), n, 3.0);\n    float rough = clamp(0.22 + lathe*0.30, 0.05, 0.75);\n    if (h.y == 3.0) rough = 0.12;\n    f0 *= 0.82 + lathe*0.30;\n\n    float ao = calcAO(pos, n);\n    float s1 = softShadow(pos + n*0.01, L1, 0.02, 8.0, 14.0);\n    float s2 = softShadow(pos + n*0.01, L2, 0.02, 8.0, 14.0);\n\n    // Metal: almost no diffuse, so this is nearly all specular.\n    col += C1*ggx(n, v, L1, rough)*f0*(4.2 + a.pres*4.5)*s1;\n    col += C2*ggx(n, v, L2, rough)*f0*(2.6 + comp(a.low, 0.30)*3.2)*s2;\n    col += C3*ggx(n, v, L3, rough)*f0*(1.8 + a.sib*3.0);\n    col += f0*(C1*max(dot(n,L1),0.0)*s1 + C2*max(dot(n,L2),0.0)*s2)*0.045;\n    // Fresnel rim, which on metal is what draws the silhouette.\n    col += f0*pow(1.0 - max(dot(n, v), 0.0), 4.0)*(0.35 + a.pres*1.1)*ao;\n    col *= 0.22 + 0.78*ao;\n\n    // Each ring carries its own bin as a filament of light in its groove.\n    float ringIdx = floor((2.15 - length(pos)*0.92)/0.255 + 0.5);\n    float e = SPEC(0.006 + clamp(ringIdx, 0.0, 6.0)*0.075);\n    col += mix(vec3(0.30,0.65,1.00), vec3(1.00,0.55,0.30), fract(ringIdx*0.37))\n         * pow(e, 1.6)*0.55*pow(1.0 - max(dot(n, v), 0.0), 2.0);\n\n    col = mix(col, vec3(0.006,0.007,0.012), 1.0 - exp(-h.x*h.x*0.0035));\n  }\n\n  // Vocals spin the gyro up: the core throws light out through the nest.\n  float coreGlow = exp(-length(cross(rd, normalize(-ro)))*length(ro)*2.6);\n  col += vec3(1.00,0.86,0.55)*a.voice*(1.4 + iParam2*1.6)*coreGlow;\n  col += vec3(1.00,0.92,0.75)*pu*0.10*coreGlow;\n  // Dust in the shop light.\n  vec2 dg = floor(uv*240.0 + vec2(t*1.4, -t*0.8));\n  col += step(0.9975 - a.air*0.004, h21(dg))*(0.20 + a.air*1.2)*vec3(1.0,0.95,0.88);\n\n  col = aces(col*0.95);\n  col *= 1.0 - smoothstep(0.55, 1.25, length(uv))*0.42;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "monolith", n: 55, name: "Monolith",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "A room with one opening. The camera does not move. The aperture is not a lamp but a hole into a lit volume, so the light has to travel through the air of the room to reach anything — the beam is marched, and the slab casts a real shadow through it. What you are mostly looking at is dust.",
    params: [{"name":"Turn","def":0.3},{"name":"Field of view","def":0.35},{"name":"Flood","def":0.45}],
    reacts: [["low","Weight of the light in the air"],["pres","How far the light reaches"],["air","The motes, individually"],["voice","The aperture floods and the room takes it"],["hit","A stir through the beam"]],
    body: "// MONOLITH — a room with one opening in it. The camera does not move; it takes\n// about four minutes for the light to change appreciably, and that is the piece.\n// The aperture is not a lamp: it is a hole into a lit volume, so the light has\n// to travel through the air of the room to reach anything, and the beam is\n// marched rather than faked. What you are looking at is mostly dust.\nvec2 map(vec3 p){\n  // The chamber: a box, entered from inside, so the field is negated.\n  float room = -sdBox(p - vec3(0.0, 3.2, 0.0), vec3(7.0, 3.6, 12.0));\n  // The aperture, cut through the far wall.\n  vec2 ap = abs(vec2(p.x, p.y - 3.4)) - vec2(2.35, 1.30);\n  float apD = min(max(ap.x, ap.y), 0.0) + length(max(ap, 0.0)) - 0.35;\n  float hole = max(apD, abs(p.z - 11.8) - 0.9);\n  room = max(room, -hole);\n\n  // The slab: standing, slightly proud of the floor, slightly off centre.\n  vec3 sq = p - vec3(-1.15, 2.05, 5.4);\n  sq.xz = r2(0.22)*sq.xz;\n  float slab = sdRoundBox(sq, vec3(0.60, 2.00, 0.22), 0.035);\n\n  // A bench along one wall, because a room needs something at human scale in it.\n  float bench = sdRoundBox(p - vec3(5.2, 0.42, 2.0), vec3(0.55, 0.06, 3.2), 0.03);\n  bench = min(bench, sdRoundBox(p - vec3(5.2, 0.20, 2.0), vec3(0.10, 0.20, 3.0), 0.02));\n\n  vec2 d = vec2(room, 1.0);\n  d = opU(d, vec2(slab, 2.0));\n  d = opU(d, vec2(bench, 3.0));\n  return d;\n}\n\nvec3 apertureLight(float t, Aud a){\n  // The colour beyond the opening. It turns over minutes, not seconds, and the\n  // audio only nudges where in the turn it currently sits.\n  float u = fract(t*0.010 + a.pres*0.06);\n  vec3 c = ramp(vec3(0.10,0.30,1.00), vec3(0.95,0.25,0.55), vec3(1.00,0.72,0.25), u);\n  c = mix(c, vec3(0.20,0.90,0.85), 0.5 + 0.5*sin(t*0.017 + 2.0));\n  return c*(1.0 + comp(a.low, 0.30)*0.55);\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.30 + iParam0*0.30);\n\n  // Locked off, with the faintest breath so it is not a photograph.\n  vec3 ro = vec3(0.0 + sin(t*0.037)*0.10, 1.62, -2.4 + sin(t*0.023)*0.12);\n  vec3 ta = vec3(0.0, 2.9, 11.0);\n  mat3 cm = camera(ro, ta, sin(t*0.019)*0.006);\n  vec3 rd = cm*normalize(vec3(uv, 1.55 - iParam1*0.30));\n\n  vec3 lightC = apertureLight(t, a);\n  vec2 h = raymarch(ro, rd, 34.0);\n  vec3 col = vec3(0.0);\n\n  // Does this ray leave through the opening? If so it sees the lit volume.\n  bool throughAperture = false;\n  if (rd.z > 0.0){\n    float tz = (11.8 - ro.z)/rd.z;\n    vec3 hp = ro + rd*tz;\n    vec2 ap = abs(vec2(hp.x, hp.y - 3.4)) - vec2(2.35, 1.30);\n    if (min(max(ap.x, ap.y), 0.0) + length(max(ap, 0.0)) - 0.35 < 0.0 && (h.y < 0.0 || tz < h.x))\n      throughAperture = true;\n  }\n\n  if (throughAperture){\n    // Not a flat colour: a soft vertical gradient with no edge you can find,\n    // which is the entire point of the form.\n    float g = clamp((rd.y*3.0 + 0.55), 0.0, 1.0);\n    col = lightC*(0.45 + g*0.75)*(1.05 + a.pres*0.7);\n    col += lightC*a.voice*(0.8 + iParam2*0.9);\n  } else if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    float grain = triNoise(pos*1.1, n, 2.2)*0.55 + triNoise(pos*9.0, n, 6.0)*0.45;\n    vec3 albedo = h.y == 2.0 ? mix(vec3(0.055,0.055,0.060), vec3(0.10,0.10,0.11), grain)\n                             : mix(vec3(0.20,0.20,0.21), vec3(0.30,0.30,0.31), grain);\n    float ao = calcAO(pos, n);\n\n    // One light, and it is an area: sample the aperture at five points so the\n    // shadows are soft the way a window makes them, not point-sharp.\n    vec3 lit = vec3(0.0);\n    for (int i = 0; i < 5; i++){\n      float fi = float(i);\n      vec3 lp = vec3((h21(vec2(fi, 1.0)) - 0.5)*4.2, 3.4 + (h21(vec2(fi, 5.0)) - 0.5)*2.2, 11.7);\n      vec3 L = lp - pos; float dist = length(L); L /= dist;\n      float sh = softShadow(pos + n*0.02, L, 0.05, dist - 0.2, 9.0);\n      lit += max(dot(n, L), 0.0)*sh/(1.0 + dist*dist*0.030);\n    }\n    lit /= 5.0;\n    col = albedo*mix(lightC, vec3(1.0), 0.45)*lit*(2.0 + comp(a.low, 0.30)*1.4);\n    col += albedo*lightC*0.020*ao;                  // the room bouncing off itself\n    col *= 0.25 + 0.75*ao;\n    col += albedo*lightC*a.voice*0.35*lit;\n  }\n\n  // The beam. This is the work: light made visible by what is floating in it.\n  float beam = 0.0;\n  for (int i = 0; i < 26; i++){\n    float d = 0.4 + float(i)*0.85;\n    if (h.y > 0.0 && d > h.x && !throughAperture) break;\n    vec3 sp = ro + rd*d;\n    if (sp.z > 11.8) break;\n    // Inside the cone the opening casts into the room.\n    float spread = 0.55 + (11.8 - sp.z)*0.16;\n    vec2 q = abs(vec2(sp.x, sp.y - 3.4)) - vec2(2.35, 1.30)*(1.0 + (11.8 - sp.z)*0.030);\n    float inCone = smoothstep(spread, -0.2, min(max(q.x, q.y), 0.0) + length(max(q, 0.0)));\n    // Occlusion: the slab casts a real shadow through the beam.\n    float occ = softShadow(sp, normalize(vec3(0.0, 3.4, 11.8) - sp), 0.1, 9.0, 6.0);\n    float dust = 0.55 + 0.75*fbm3(sp*0.55 + vec3(0.0, t*0.06, -t*0.10), 3);\n    beam += inCone*occ*dust*exp(-(11.8 - sp.z)*0.055);\n  }\n  col += lightC*beam*(0.0075 + comp(a.low, 0.30)*0.011 + a.voice*0.014 + pu*0.006);\n\n  // Motes, individually. They are the only fast thing in the room.\n  vec2 mg = floor(uv*(150.0 + a.air*60.0) + vec2(sin(t*0.4)*2.0, t*0.7));\n  col += step(0.9970 - a.air*0.005, h21(mg))*(0.20 + a.air*1.1)*lightC*0.6;\n\n  col = aces(col*0.82);\n  col *= 1.0 - smoothstep(0.62, 1.32, length(uv))*0.30;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "cloudline", n: 56, name: "Cloudline",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "Drifting through weather at the end of the day. The cloud is a real volume: the ray accumulates transmittance, and at each step takes a second short march toward the sun to find how much light survives. That inner march is why the tops are bright, the undersides blue, and the edges silver.",
    params: [{"name":"Drift","def":0.35},{"name":"Field of view","def":0.35},{"name":"Golden hour","def":0.4}],
    reacts: [["sub","Swell in the deck"],["low","Density of the cloud"],["pres","How hard the sun drives through"],["air","Ice catching the last of the light"],["voice","Turns the hour — the tops take the gold"]],
    body: "// CLOUDLINE — drifting through weather at the end of the day. The cloud is a\n// real volume, not a billboard: the ray steps through a density field and\n// accumulates transmittance, and at each step it takes a second short march\n// toward the sun to find out how much light survives to that point. That inner\n// march is why the tops are bright and the undersides are blue, and why the\n// silver edge appears when the sun is behind a bank — none of it is drawn, it\n// falls out of the light having to travel through the cloud to reach you.\nvec2 map(vec3 p){ return vec2(1e9, -1.0); }        // no surfaces: this one is all volume\n\nfloat cloudDens(vec3 p, float t, float swell, int oct){\n  // A deck between two heights, eroded by noise, drifting.\n  vec3 q = p*0.062 + vec3(t*0.020, 0.0, t*0.012);\n  float base = fbm3(q, oct);\n  float detail = fbm3(q*4.1 + 7.0, 3);\n  float shape = base - 0.30 + swell*0.05;\n  shape -= detail*0.10;\n  // Slab: fades in at the base, out at the top, so it has a floor and a ceiling.\n  float slab = smoothstep(-14.0, 0.0, p.y)*smoothstep(20.0, 7.0, p.y);\n  return clamp(shape, 0.0, 1.0)*slab*0.95;\n}\n\nvec3 skyC(vec3 rd, vec3 sun, float warm){\n  float up = clamp(rd.y*0.5 + 0.5, 0.0, 1.0);\n  vec3 hi = vec3(0.030,0.075,0.230);\n  vec3 lo = mix(vec3(0.42,0.26,0.17), vec3(0.58,0.42,0.28), warm);\n  vec3 c = ramp(lo, vec3(0.115,0.130,0.235), hi, up);\n  float sd = max(dot(rd, sun), 0.0);\n  c += vec3(1.00,0.72,0.42)*pow(sd, 9.0)*0.22;\n  c += vec3(1.00,0.88,0.66)*pow(sd, 2600.0)*4.0;\n  return c;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.28 + iParam0*0.35);\n\n  float swell = comp(a.sub, 0.32)*0.9 + comp(a.low, 0.30)*0.5;\n  int OCT = iResolution.y > 1300.0 ? 3 : 5;\n\n  // A long, slow, banked drift. Nothing in this piece hurries.\n  float z = t*5.5;\n  vec3 pth  = vec3(sin(z*0.045)*46.0, 52.0 + sin(z*0.031)*4.0, z);\n  vec3 pPre = vec3(sin((z-4.0)*0.045)*38.0, 0.0, z - 4.0);\n  vec3 pNex = vec3(sin((z+4.0)*0.045)*38.0, 0.0, z + 4.0);\n  vec3 ro = pth;\n  vec3 ta = ro + normalize(pNex - pPre)*20.0 + vec3(0.0, -13.0 + sin(t*0.06)*1.5, 0.0);\n  float roll = autoBank(pPre, vec3(pth.x, 0.0, pth.z), pNex, 0.22);\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.5 - iParam1*0.30));\n\n  vec3 sun = normalize(vec3(-0.62, 0.10, 0.78));\n  float warm = 0.30 + a.voice*0.65;\n  vec3 col = skyC(rd, sun, warm);\n\n  // March the volume. Front to back, accumulating transmittance.\n  float T = 1.0;\n  vec3 acc = vec3(0.0);\n  int STEPS = iResolution.y > 1300.0 ? 40 : 60;\n  float stepLen = 2.4;\n  float jitter = h21(C)*stepLen;                 // dither, or the slabs band\n  for (int i = 0; i < 64; i++){\n    if (i >= STEPS || T < 0.010) break;\n    float d = 1.0 + float(i)*stepLen + jitter;\n    vec3 sp = ro + rd*d;\n    if (sp.y < -12.0 || sp.y > 20.0) continue;\n    float dens = cloudDens(sp, t, swell, OCT);\n    if (dens <= 0.001) continue;\n\n    // Light march: five short steps toward the sun. This is the whole lighting\n    // model, and it is why the cloud has a shape rather than a colour.\n    float shadow = 0.0;\n    for (int j = 0; j < 5; j++){\n      vec3 lp = sp + sun*(0.8 + float(j)*2.2);\n      shadow += cloudDens(lp, t, swell, 3);\n    }\n    float lightT = exp(-shadow*0.55);\n    // Forward scattering: a cloud edge with the sun behind it goes silver.\n    float phase = 0.32 + 2.8*pow(max(dot(rd, sun), 0.0), 8.0);\n    vec3 lum = mix(vec3(1.00,0.72,0.44), vec3(1.00,0.88,0.72), warm)*lightT*phase*(1.45 + a.pres*0.95);\n    lum += vec3(0.10,0.16,0.30)*(0.30 + 0.70*smoothstep(-6.0, 18.0, sp.y))*0.14;   // sky fill from above\n    float alpha = 1.0 - exp(-dens*stepLen*0.30);\n    acc += lum*alpha*T;\n    T *= 1.0 - alpha;\n  }\n  col = col*T + acc;\n\n  // Airborne ice catching the last of the sun, on the air band.\n  vec2 ig = floor(uv*(190.0 + a.air*70.0) + vec2(t*2.2, t*0.6));\n  col += step(0.9975 - a.air*0.005, h21(ig))*(0.25 + a.air*1.4)*vec3(1.00,0.92,0.80)*T;\n\n  // Vocals turn the hour: the light goes gold and the tops take it.\n  col += vec3(1.00,0.78,0.42)*a.voice*(0.20 + iParam2*0.35)*(1.0 - T)*0.6;\n  col += vec3(1.0)*pu*0.020;\n\n  col = aces(col*0.86);\n  col *= 1.0 - smoothstep(0.62, 1.32, length(uv))*0.28;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "karesansui", n: 57, name: "Karesansui",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "A dry garden, raked, from the height of someone sitting on the veranda — the only place these are composed to be seen from. The raking is a real displacement of the ground, not a texture, which is why the low sun finds the ridges and the troughs stay in shadow.",
    params: [{"name":"Rate","def":0.3},{"name":"Field of view","def":0.35},{"name":"Late light","def":0.4}],
    reacts: [["low","Weight of the low sun on the gravel"],["pres","Warmth of the light"],["sib","Specular across the raked ridges"],["air","Mica in the gravel"],["voice","Draws one furrow that is not permanent, and warms the light"]],
    body: "// KARESANSUI — a dry garden, raked. The camera is fixed at the height of someone\n// sitting on the veranda, which is the only place these gardens are composed to\n// be seen from. Late raking light, so the furrows in the gravel read as relief\n// and the stones cast the long shadows they were sited for. The raking is a real\n// displacement of the ground surface, not a texture — which is why the light\n// finds the ridges and the troughs stay in shadow.\nvec2 map(vec3 p){ return vec2(1e9, -1.0); }\n\nfloat rakeH(vec2 q, float t, float draw){\n  // Concentric rings around each stone, and straight furrows between them.\n  vec2 c1 = vec2(-2.1, 1.4), c2 = vec2(2.6, -0.6), c3 = vec2(0.4, 4.2);\n  float d1 = length(q - c1), d2 = length(q - c2), d3 = length(q - c3);\n  float rings = 0.0;\n  rings = max(rings, sin(d1*7.5)*exp(-d1*0.30));\n  rings = max(rings, sin(d2*7.5)*exp(-d2*0.30));\n  rings = max(rings, sin(d3*7.5)*exp(-d3*0.28));\n  float straight = sin(q.x*7.5 + sin(q.y*0.30)*0.7);\n  float w = smoothstep(0.10, 0.85, min(min(d1, d2), d3)*0.20);\n  float h = mix(rings, straight, w);\n  // A ring the audio draws: the one furrow that is not permanent.\n  float dr = length(q);\n  h = mix(h, sin(dr*7.5 - t*0.55), draw*exp(-abs(dr - 3.2 - draw*1.4)*0.75)*0.85);\n  return h*0.028 + (vnoise(q*46.0) - 0.5)*0.007;      // plus the grain of the gravel\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.20 + iParam0*0.25);\n  float draw = a.voice;\n\n  // Seated height, looking slightly down. It breathes and does nothing else.\n  vec3 ro = vec3(0.0 + sin(t*0.031)*0.05, 0.92, -4.6);\n  vec3 ta = vec3(0.15, 0.05, 1.4);\n  mat3 cm = camera(ro, ta, sin(t*0.017)*0.004);\n  vec3 rd = cm*normalize(vec3(uv, 1.55 - iParam1*0.28));\n\n  // Low sun, from the left, raking across the furrows.\n  vec3 sun = normalize(vec3(-0.82, 0.20, 0.53));\n  vec3 sunC = mix(vec3(1.00,0.86,0.62), vec3(1.00,0.72,0.44), 0.35 + a.pres*0.35);\n  vec3 skyC = vec3(0.34,0.44,0.62);\n  vec3 col = ramp(vec3(0.28,0.30,0.36), vec3(0.16,0.19,0.26), vec3(0.07,0.09,0.14),\n                  clamp(rd.y*1.6 + 0.2, 0.0, 1.0));\n  col += sunC*pow(max(dot(rd, sun), 0.0), 40.0)*0.35;\n\n  // The wall at the far side, which is what gives a dry garden its edge.\n  float wallT = (2.8 - ro.y)/max(-rd.y, 1e-4);\n  float gz = (rd.y < 0.0) ? (ro.y/max(-rd.y, 1e-4)) : -1.0;\n\n  if (gz > 0.0){\n    vec3 pos = ro + rd*gz;\n    if (pos.z < 9.0){\n      // Refine against the raked surface: a short secant march on the height.\n      for (int i = 0; i < 6; i++){\n        float hh = rakeH(pos.xz, t, draw);\n        float err = pos.y - hh;\n        pos += rd*(err/max(-rd.y, 1e-3))*0.85;\n      }\n      float e2 = 0.012;\n      float h0 = rakeH(pos.xz, t, draw);\n      vec3 n = normalize(vec3(h0 - rakeH(pos.xz + vec2(e2, 0.0), t, draw), e2,\n                              h0 - rakeH(pos.xz + vec2(0.0, e2), t, draw)));\n      // Gravel: pale, dusty, weakly specular, and it sparkles a little.\n      float g = vnoise(pos.xz*80.0);\n      vec3 albedo = mix(vec3(0.52,0.50,0.46), vec3(0.68,0.66,0.60), g);\n      float dif = max(dot(n, sun), 0.0);\n      // Stones cast into the gravel — three of them, sited as the garden is.\n      float sh = 1.0;\n      for (int i = 0; i < 3; i++){\n        vec2 sc = i == 0 ? vec2(-2.1, 1.4) : (i == 1 ? vec2(2.6, -0.6) : vec2(0.4, 4.2));\n        float sr = i == 0 ? 0.52 : (i == 1 ? 0.38 : 0.30);\n        vec2 toS = pos.xz - sc;\n        float alongSun = dot(toS, -sun.xz);\n        float perp = length(toS + sun.xz*alongSun);\n        sh = min(sh, smoothstep(sr*0.55, sr*1.25, perp) + smoothstep(0.0, -0.5, alongSun));\n      }\n      sh = clamp(sh, 0.0, 1.0);\n      col = albedo*sunC*dif*sh*(1.5 + comp(a.low, 0.30)*0.85);\n      col += albedo*skyC*(0.35 + 0.65*n.y)*0.55;\n      col += sunC*ggx(n, -rd, sun, 0.55)*sh*(0.25 + a.sib*1.2)*0.8;\n      // Mica in the gravel, catching the low sun.\n      col += sunC*step(0.9965 - a.air*0.006, h21(floor(pos.xz*300.0)))*(0.35 + a.air*1.8)*sh;\n      col = mix(col, vec3(0.16,0.19,0.26), 1.0 - exp(-pos.z*pos.z*0.0022));\n    }\n  }\n\n  // The stones. Three, plus moss on the lee side of each.\n  for (int i = 0; i < 3; i++){\n    vec3 sc = i == 0 ? vec3(-2.1, 0.0, 1.4) : (i == 1 ? vec3(2.6, 0.0, -0.6) : vec3(0.4, 0.0, 4.2));\n    float sr = i == 0 ? 0.52 : (i == 1 ? 0.38 : 0.30);\n    // Intersect a squashed sphere directly — cheaper than adding it to the field.\n    vec3 oc = ro - sc - vec3(0.0, sr*0.55, 0.0);\n    vec3 sd2 = rd/vec3(1.0, 0.72, 1.0);\n    vec3 oc2 = oc/vec3(1.0, 0.72, 1.0);\n    float b = dot(oc2, sd2), c2 = dot(oc2, oc2) - sr*sr, disc = b*b - dot(sd2,sd2)*c2;\n    if (disc > 0.0){\n      float tt = (-b - sqrt(disc))/dot(sd2, sd2);\n      if (tt > 0.0 && (gz < 0.0 || tt < gz + 0.4)){\n        vec3 pos = ro + rd*tt;\n        vec3 n = normalize((pos - sc - vec3(0.0, sr*0.55, 0.0))/vec3(1.0, 0.72, 1.0));\n        float rough = triNoise(pos*8.0, n, 6.0);\n        n = normalize(n + (vec3(rough) - 0.5)*0.35);\n        vec3 albedo = mix(vec3(0.13,0.13,0.14), vec3(0.26,0.25,0.24), rough);\n        // Moss, on the side the sun does not reach.\n        float moss = smoothstep(0.15, -0.35, dot(n, sun))*smoothstep(0.2, 0.7, n.y)\n                   * smoothstep(0.45, 0.62, triNoise(pos*4.0, n, 3.0));\n        albedo = mix(albedo, vec3(0.10,0.20,0.09), moss*0.85);\n        col = albedo*sunC*max(dot(n, sun), 0.0)*(1.6 + comp(a.low,0.30)*0.9);\n        col += albedo*skyC*(0.35 + 0.65*n.y)*0.60;\n        col += sunC*ggx(n, -rd, sun, 0.62)*(0.20 + a.sib*0.9);\n        col = mix(col, vec3(0.16,0.19,0.26), 1.0 - exp(-pos.z*pos.z*0.0022));\n      }\n    }\n  }\n\n  // Vocals: the light warms, and one furrow is drawn out from the centre.\n  col = mix(col, mix(col, vec3(1.00,0.82,0.52), 0.55), a.voice*(0.25 + iParam2*0.40));\n  col += vec3(1.0)*pu*0.014;\n\n  col = aces(col*0.98);\n  col *= 1.0 - smoothstep(0.62, 1.32, length(uv))*0.30;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "reef", n: 58, name: "Reef",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "Drifting through water that makes its own light. There is no sun at this depth, so the only illumination is the animals — and the water takes their colour and gives it back dimmer and bluer, wavelength by wavelength, which is why anything distant is blue whatever colour it is.",
    params: [{"name":"Drift","def":0.35},{"name":"Field of view","def":0.35},{"name":"Chain","def":0.45}],
    reacts: [["low","Ambient from the colonies"],["pres","Which colony is lit, and how brightly"],["sib","Wet specular on the branches"],["air","Marine snow"],["voice","A siphonophore runs its chain overhead, end to end"],["hit","Plankton fire"]],
    body: "// REEF — drifting through water that makes its own light. Everything here is\n// emissive: there is no sun at this depth, so the only illumination is the\n// animals, and the water between them takes their colour and gives it back\n// dimmer and bluer. That absorption is wavelength-dependent — red goes first,\n// then green — which is why anything more than a few metres away is blue\n// whatever colour it actually is.\n#define TAU 6.28318531\n\nvec2 map(vec3 p){\n  vec2 res = vec2(1e9, -1.0);\n  // Coral: a repeated branching mass, each colony a little different.\n  vec3 id = floor(p/7.0 + 0.5);\n  vec3 q = p - 7.0*id;\n  float seed = h31(id);\n  if (seed > 0.35){\n    vec3 base = vec3((seed - 0.5)*3.0, -2.4 + fract(seed*13.0)*1.2, (fract(seed*7.0) - 0.5)*3.0);\n    float col_ = 1e9;\n    for (int i = 0; i < 5; i++){\n      float fi = float(i);\n      float s2 = h31(id + fi*3.7);\n      float ang = s2*TAU;\n      float ln = 0.9 + s2*1.5;\n      vec3 tip = base + vec3(cos(ang), 1.6, sin(ang))*ln;\n      col_ = opSmoothU(col_, sdCapsule(q, base, tip, 0.10 + s2*0.10), 0.28);\n      vec3 t2 = tip + vec3(cos(ang + 1.9), 1.0, sin(ang + 1.9))*ln*0.55;\n      col_ = opSmoothU(col_, sdCapsule(q, tip, t2, 0.055 + s2*0.05), 0.18);\n    }\n    // Surface texture: coral is polyps, not a smooth branch.\n    col_ += (fbm3(q*7.0, 3) - 0.5)*0.055;\n    res = opU(res, vec2(col_, 1.0));\n  }\n  // The floor, far below and gently uneven.\n  float fl = p.y + 4.2 - fbm3(p*0.12, 3)*1.6;\n  res = opU(res, vec2(fl, 2.0));\n  res.x *= 0.8;\n  return res;\n}\n\n// Absorption through water. Red is gone in metres, blue survives.\nvec3 waterAbsorb(float d){ return exp(-d*vec3(0.140, 0.055, 0.030)); }\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.25 + iParam0*0.35);\n\n  // A slow swim, with the gentle roll of something neutrally buoyant.\n  float z = t*2.0;\n  vec3 pth  = vec3(sin(z*0.09)*4.5, 0.4 + sin(z*0.13)*0.9, z);\n  vec3 pPre = vec3(sin((z-1.5)*0.09)*4.5, 0.0, z - 1.5);\n  vec3 pNex = vec3(sin((z+1.5)*0.09)*4.5, 0.0, z + 1.5);\n  vec3 ro = pth;\n  vec3 ta = ro + normalize(pNex - pPre)*5.0 + vec3(0.0, -0.25, 0.0);\n  float roll = autoBank(pPre, vec3(pth.x, 0.0, pth.z), pNex, 3.0) + sin(t*0.23)*0.05;\n  mat3 cm = camera(ro, ta, roll);\n  vec3 rd = cm*normalize(vec3(uv, 1.45 - iParam1*0.30));\n\n  vec3 col = vec3(0.002,0.006,0.014);\n  vec2 h = raymarch(ro, rd, 40.0);\n\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n    vec3 id = floor(pos/7.0 + 0.5);\n    float seed = h31(id);\n    float e = SPEC(0.006 + fract(seed*5.0)*0.56);\n\n    vec3 hue = ramp(vec3(0.10,0.85,1.00), vec3(0.55,0.30,1.00), vec3(1.00,0.35,0.65),\n                    fract(seed*3.0 + a.pres*0.12));\n    if (h.y == 2.0) hue = vec3(0.10,0.30,0.42);\n\n    // The colony pulses along its length: a wave travelling out to the tips.\n    float pulse = pow(0.5 + 0.5*sin(pos.y*2.4 - t*2.2 + seed*TAU), 5.0);\n    float emit = h.y == 1.0 ? (0.12 + pow(e, 1.2)*3.4)*(0.30 + pulse*1.10) : 0.014;\n    col = hue*emit;\n\n    // Ambient from every other colony, approximated as a soft upward fill.\n    float ao = calcAO(pos, n);\n    col += hue*0.13*ao*(0.3 + comp(a.low, 0.30)*0.9);\n    col += vec3(0.10,0.35,0.55)*max(n.y, 0.0)*0.045*ao;\n    // Wet specular, from the general glow above.\n    col += vec3(0.55,0.85,1.00)*ggx(n, v, normalize(vec3(0.2, 1.0, 0.1)), 0.28)*(0.25 + a.sib*1.3)*ao;\n    col *= 0.30 + 0.70*ao;\n    col *= waterAbsorb(h.x);\n  }\n\n  // Marine snow and plankton: the snow always falls, the plankton fire on hits.\n  for (int k = 0; k < 2; k++){\n    float fk = float(k);\n    vec2 sg = floor(uv*(90.0 + fk*110.0) + vec2(sin(t*0.5 + fk)*1.5, t*(0.9 + fk*0.7)));\n    float hh = h21(sg + fk*17.0);\n    if (hh > 0.972) col += vec3(0.45,0.72,0.88)*(0.10 + a.air*0.85)*(1.0 - fk*0.4);\n  }\n  vec2 pg = floor(uv*(130.0 + pu*40.0) + floor(iTime*7.0));\n  col += step(0.9950 - pu*0.012, h21(pg))*(0.20 + pu*2.2)*vec3(0.35,0.95,1.00);\n\n  // The water itself scatters what the colonies emit — a soft, dim volume.\n  float glow = 0.0;\n  for (int i = 0; i < 10; i++){\n    float d = 1.0 + float(i)*3.4;\n    vec3 sp = ro + rd*d;\n    glow += exp(-abs(sp.y + 1.2)*0.30)*exp(-d*0.055)*(0.5 + fbm3(sp*0.3 + vec3(0.0, t*0.1, 0.0), 2));\n  }\n  col += vec3(0.06,0.30,0.45)*glow*(0.014 + comp(a.low, 0.30)*0.022);\n\n  // Vocals: a siphonophore runs its chain overhead, end to end.\n  float chain = 0.0;\n  for (int i = 0; i < 12; i++){\n    float u = float(i)/11.0;\n    vec3 cp = vec3(mix(-9.0, 9.0, u), 3.4 + sin(u*6.0 + t*0.4)*0.9, ro.z + 7.0);\n    vec3 toC = cp - ro;\n    float along = dot(toC, rd);\n    float perp = length(toC - rd*along);\n    if (along > 0.0) chain += exp(-perp*perp*7.0)*SPEC(0.006 + u*0.55)*exp(-along*0.05);\n  }\n  col += vec3(0.40,1.00,0.85)*chain*a.voice*(1.1 + iParam2*1.2);\n\n  col = aces(col*1.05);\n  col *= 1.0 - smoothstep(0.58, 1.28, length(uv))*0.40;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "orrery", n: 59, name: "Orrery",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "A brass planetarium turning in a dark room. The periods are the real ones, scaled — the ratios between the arms are the ratios between the years — so the pattern they make passing each other is the pattern the planets actually make. It takes a long time to repeat, and never exactly.",
    params: [{"name":"Orbit","def":0.3},{"name":"Distance","def":0.35},{"name":"Lamp","def":0.4}],
    reacts: [["low","The little sun at the centre"],["pres","The lamp on the brass"],["air","Dust in the lamplight"],["voice","Raises the lamp — the brass warms and the room comes up"],["hit","A catch of light on the arms"]],
    body: "// ORRERY — a brass planetarium turning in a dark room, lit by one lamp standing\n// in for the sun and by the small warm bulb at its centre. The periods are the\n// real ones, scaled: the ratios between the arms are the ratios between the\n// years, so the pattern the arms make as they pass each other is the pattern the\n// planets actually make. It takes a long time to repeat, and it never repeats\n// exactly, which is the reason these machines were built.\n#define TAU 6.28318531\n\n// Orbital periods in Earth years, and the radii the model uses.\nfloat period(int i){\n  if (i == 0) return 0.241;   // Mercury\n  if (i == 1) return 0.615;   // Venus\n  if (i == 2) return 1.000;   // Earth\n  if (i == 3) return 1.881;   // Mars\n  if (i == 4) return 4.200;   // a compressed Jupiter\n  return 8.400;               // and a compressed Saturn\n}\nfloat radiusOf(int i){ return 0.62 + float(i)*0.42; }\n\nvec2 map(vec3 p){\n  vec2 res = vec2(1e9, -1.0);\n  float T = iTime*0.11;\n\n  // The base: a turned brass column on a plinth.\n  float col_ = sdCyl(p - vec3(0.0, -1.55, 0.0), 0.14, 0.85);\n  col_ = min(col_, sdCyl(p - vec3(0.0, -2.34, 0.0), 0.62, 0.075));\n  col_ = min(col_, sdCyl(p - vec3(0.0, -2.20, 0.0), 0.46, 0.055));\n  res = opU(res, vec2(col_, 1.0));\n\n  // The sun at the centre, on its own small pillar.\n  res = opU(res, vec2(sdSphere(p, 0.20), 4.0));\n\n  for (int i = 0; i < 6; i++){\n    float fi = float(i);\n    float R = radiusOf(i);\n    float ang = T*TAU/period(i) + fi*1.7;\n    vec2 dir = vec2(cos(ang), sin(ang));\n    float y = -0.55 + fi*0.075;\n\n    // The arm, and the collar it turns in.\n    vec3 ap = p - vec3(0.0, y, 0.0);\n    float arm = sdCapsule(ap, vec3(0.0, 0.0, 0.0), vec3(dir.x*R, 0.0, dir.y*R), 0.028);\n    arm = min(arm, sdCyl(ap, 0.075, 0.030));\n    res = opU(res, vec2(arm, 1.0));\n\n    // The planet, and a ring on the outermost.\n    vec3 pp = p - vec3(dir.x*R, y + 0.085, dir.y*R);\n    float pr = 0.052 + fi*0.014 + SPEC(0.006 + fi*0.075)*0.030;\n    res = opU(res, vec2(sdSphere(pp, pr), 2.0 + mod(fi, 2.0)));\n    if (i == 5){\n      vec3 rq = pp; rq.yz = r2(0.38)*rq.yz;\n      float ring = abs(sdTorus(rq, vec2(pr*2.1, 0.006)));\n      res = opU(res, vec2(ring - 0.004, 1.0));\n    }\n    // A graduated ring on the plinth marking each orbit.\n    float track = max(abs(length(p.xz) - R) - 0.010, abs(p.y + 2.26) - 0.012);\n    res = opU(res, vec2(track, 5.0));\n  }\n  res.x *= 0.85;\n  return res;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.20 + iParam0*0.30);\n\n  // A slow, low orbit. Nothing sudden.\n  float orb = t*0.12;\n  float rad = 5.6 - iParam1*1.4;\n  vec3 ro = vec3(sin(orb)*rad, 0.75 + sin(orb*0.7)*0.85, cos(orb)*rad);\n  mat3 cm = camera(ro, vec3(0.0, -0.35, 0.0), sin(orb*0.5)*0.03);\n  vec3 rd = cm*normalize(vec3(uv, 2.0));\n\n  vec3 col = vec3(0.004,0.004,0.007);\n  vec3 lamp = normalize(vec3(-0.55, 0.62, 0.40));\n  vec3 lampC = vec3(1.00,0.88,0.68);\n  vec3 sunPos = vec3(0.0);\n\n  vec2 h = raymarch(ro, rd, 22.0);\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    vec3 v = -rd;\n\n    // Brass, aged. Two metals and a painted sphere or two.\n    vec3 f0 = h.y == 1.0 ? vec3(0.80,0.62,0.28)\n            : h.y == 2.0 ? vec3(0.58,0.62,0.70)\n            : h.y == 3.0 ? vec3(0.72,0.48,0.32)\n            : h.y == 4.0 ? vec3(1.00,0.82,0.42) : vec3(0.66,0.54,0.30);\n    float patina = triNoise(pos*5.0, n, 4.0)*0.6 + triNoise(pos*18.0, n, 9.0)*0.4;\n    f0 *= 0.72 + patina*0.50;\n    float rough = clamp(0.20 + patina*0.34, 0.06, 0.80);\n\n    float ao = calcAO(pos, n);\n    float sh = softShadow(pos + n*0.008, lamp, 0.02, 8.0, 16.0);\n\n    col = f0*lampC*ggx(n, v, lamp, rough)*(4.5 + a.pres*4.5)*sh;\n    col += f0*lampC*max(dot(n, lamp), 0.0)*sh*0.10;\n    // The little sun at the centre lights everything from within the machine.\n    vec3 toSun = sunPos - pos; float sd = length(toSun); toSun /= sd;\n    float ssh = softShadow(pos + n*0.008, toSun, 0.02, sd - 0.25, 10.0);\n    vec3 sunC = vec3(1.00,0.78,0.40)*(1.0 + comp(a.low, 0.30)*1.6);\n    col += f0*sunC*max(dot(n, toSun), 0.0)*ssh/(1.0 + sd*sd*0.55)*2.4;\n    col += sunC*ggx(n, v, toSun, rough)*ssh/(1.0 + sd*sd*0.5)*(1.2 + a.pres*2.0);\n    // Rim, and the ambient of a dark room, which is almost nothing.\n    col += f0*pow(1.0 - max(dot(n, v), 0.0), 4.0)*0.25*ao;\n    col += f0*vec3(0.05,0.06,0.10)*ao*0.35;\n    col *= 0.25 + 0.75*ao;\n\n    if (h.y == 4.0) col += vec3(1.00,0.80,0.42)*(1.6 + comp(a.low, 0.30)*2.6);\n    // Each planet carries its own bin, as a warm filament under the paint.\n    if (h.y == 2.0 || h.y == 3.0){\n      float ri = clamp(floor((length(pos.xz) - 0.62)/0.42 + 0.5), 0.0, 5.0);\n      float e = SPEC(0.006 + ri*0.075);\n      col += mix(vec3(0.35,0.65,1.00), vec3(1.00,0.55,0.30), fract(ri*0.37))*pow(e, 1.5)*0.85;\n    }\n    col = mix(col, vec3(0.005,0.005,0.009), 1.0 - exp(-h.x*h.x*0.0055));\n  }\n\n  // The sun at the middle throws a glow into the dark room.\n  float sunGlow = exp(-length(cross(rd, normalize(sunPos - ro)))*length(ro - sunPos)*3.2);\n  col += vec3(1.00,0.76,0.38)*sunGlow*(0.35 + comp(a.low, 0.30)*0.85);\n  // Vocals raise the lamp: the brass warms and the room comes up.\n  col += vec3(1.00,0.86,0.55)*a.voice*(0.30 + iParam2*0.50)*sunGlow*1.6;\n  col += vec3(1.00,0.90,0.70)*a.voice*0.030;\n  // Dust in the lamplight.\n  vec2 dg = floor(uv*220.0 + vec2(t*0.9, -t*0.5));\n  col += step(0.9972 - a.air*0.005, h21(dg))*(0.20 + a.air*1.2)*vec3(1.00,0.92,0.78);\n  col += vec3(1.0)*pu*0.016;\n\n  col = aces(col*0.98);\n  col *= 1.0 - smoothstep(0.55, 1.28, length(uv))*0.44;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
  {
    id: "meridian-3d", n: 60, name: "Meridian",
    series: "V", set: "stillpoint", setTitle: "Stillpoint",
    kit3d: true,
    line: "The surrealist plane from Series I, with the plane actually there. It is a real mirror — the reflection is a second march of the same scene, so the bodies occlude each other correctly in it and the horizon takes the sky. Two suns, because one would be a landscape.",
    params: [{"name":"Dolly","def":0.3},{"name":"Field of view","def":0.35},{"name":"Column","def":0.45}],
    reacts: [["sub","Height of the bodies"],["low","Warmth of the low sun"],["pres","The second sun; the sky"],["sib","Specular on stone"],["air","Dust in the low light"],["voice","Raises a column of light behind the horizon"]],
    body: "// MERIDIAN — the surrealist plane from Series I, now with the plane actually\n// there. It is a real mirror: the reflection is a second march of the same\n// scene, not a flipped copy, so the bodies occlude each other correctly in it\n// and the horizon takes the sky. Two suns, because one would be a landscape.\n// The camera dollies forward for four minutes and does nothing else.\n#define TAU 6.28318531\n\nvec3 bodyPos(int i, float t){\n  float fi = float(i);\n  return vec3(-2.9 + fi*1.45,\n              1.05 + 0.30*sin(t*0.21 + fi*1.9) + SPEC(0.006 + fi*0.075)*0.35,\n              0.6 + sin(fi*2.3)*1.1);\n}\nvec2 map(vec3 p){\n  vec2 res = vec2(p.y, 1.0);                       // the plane\n  float t = iTime*0.35;\n  for (int i = 0; i < 5; i++){\n    float r = 0.34 + float(i)*0.055;\n    res = opU(res, vec2(sdSphere(p - bodyPos(i, t), r), 2.0 + float(i)));\n  }\n  // The doorway with nothing behind it.\n  vec3 dq = p - vec3(4.6, 1.55, -1.2);\n  dq.xz = r2(-0.35)*dq.xz;\n  float frame = sdRoundBox(dq, vec3(0.62, 1.55, 0.10), 0.03);\n  frame = max(frame, -sdBox(dq, vec3(0.44, 1.30, 0.5)));\n  res = opU(res, vec2(frame, 8.0));\n  return res;\n}\n\nvec3 skyOf(vec3 rd, vec3 s1, vec3 s2, float pres, float voice){\n  float up = clamp(rd.y*0.5 + 0.5, 0.0, 1.0);\n  vec3 c = ramp(vec3(0.52,0.28,0.22), vec3(0.20,0.17,0.30), vec3(0.030,0.035,0.105), up);\n  c = mix(c, mix(c, vec3(1.00,0.80,0.62), 0.35), pres*0.5);\n  c += vec3(1.00,0.72,0.42)*pow(max(dot(rd, s1), 0.0), 20.0)*0.40;\n  c += vec3(1.00,0.86,0.62)*pow(max(dot(rd, s1), 0.0), 3000.0)*5.0;\n  c += vec3(0.62,0.78,1.00)*pow(max(dot(rd, s2), 0.0), 60.0)*(0.25 + pres*0.65);\n  c += vec3(0.85,0.92,1.00)*pow(max(dot(rd, s2), 0.0), 6000.0)*4.0;\n  // Stars, faint, in the upper sky. Two suns and stars: the impossible bit.\n  vec2 sg = floor(rd.xz/max(abs(rd.y) + 0.05, 0.05)*46.0);\n  c += step(0.9965, h21(sg))*smoothstep(0.25, 0.75, rd.y)*(0.25 + voice*0.9)*vec3(0.9,0.93,1.0);\n  return c;\n}\n\nvec3 shade(vec3 pos, vec3 n, vec3 rd, float id, vec3 s1, vec3 s2, Aud a){\n  vec3 v = -rd;\n  vec3 c1 = vec3(1.00,0.78,0.50)*(1.4 + comp(a.low, 0.30)*1.0);\n  vec3 c2 = vec3(0.55,0.72,1.00)*(0.8 + a.pres*1.1);\n  float ao = calcAO(pos, n);\n  float sh1 = softShadow(pos + n*0.01, s1, 0.03, 14.0, 8.0);\n  float sh2 = softShadow(pos + n*0.01, s2, 0.03, 14.0, 8.0);\n\n  float idx = clamp(id - 2.0, 0.0, 4.0);\n  float e = SPEC(0.006 + idx*0.075);\n  vec3 albedo = id == 8.0 ? vec3(0.06,0.06,0.08)\n              : mix(vec3(0.72,0.70,0.68), ramp(vec3(0.30,0.55,1.00), vec3(0.85,0.45,1.00),\n                                               vec3(1.00,0.80,0.45), idx*0.25), 0.35 + e*0.45);\n  float rough = 0.42;\n  float grain = triNoise(pos*6.0, n, 5.0);\n  albedo *= 0.85 + grain*0.30;\n\n  vec3 col = albedo*(c1*max(dot(n, s1), 0.0)*sh1 + c2*max(dot(n, s2), 0.0)*sh2);\n  col += albedo*vec3(0.30,0.34,0.52)*(0.35 + 0.65*max(n.y, 0.0))*ao*0.55;\n  col += c1*ggx(n, v, s1, rough)*sh1*(0.55 + a.sib*1.4);\n  col += c2*ggx(n, v, s2, rough)*sh2*(0.35 + a.sib*1.0);\n  // Each body glows a little from within, on its own bin.\n  if (id >= 2.0 && id <= 6.0)\n    col += ramp(vec3(0.30,0.55,1.00), vec3(0.85,0.45,1.00), vec3(1.00,0.80,0.45), idx*0.25)\n         * pow(e, 1.5)*1.5;\n  col *= 0.30 + 0.70*ao;\n  return col;\n}\n\nvoid mainImage(out vec4 o, in vec2 C){\n  Aud a = plajahAudio(); float pu = plajahPunch();\n  vec2 uv = (C - 0.5*iResolution.xy)/iResolution.y;\n  float t = iTime*(0.22 + iParam0*0.28);\n\n  vec3 ro = vec3(sin(t*0.05)*0.6, 1.25 + sin(t*0.037)*0.10, -6.2 + t*0.35);\n  vec3 ta = vec3(0.4, 1.05, 1.0 + t*0.35);\n  mat3 cm = camera(ro, ta, sin(t*0.031)*0.012);\n  vec3 rd = cm*normalize(vec3(uv, 1.6 - iParam1*0.30));\n\n  vec3 s1 = normalize(vec3(-0.72, 0.075, 0.68));\n  vec3 s2 = normalize(vec3( 0.62, 0.42, 0.66));\n\n  vec3 col;\n  vec2 h = raymarch(ro, rd, 60.0);\n  if (h.y > 0.0){\n    vec3 pos = ro + rd*h.x;\n    vec3 n = calcNormal(pos);\n    if (h.y == 1.0){\n      // The plane: polished, so what you see is a second march of the scene.\n      vec3 rr = reflect(rd, n);\n      vec3 rc;\n      vec2 rh = raymarch(pos + n*0.02, rr, 46.0);\n      if (rh.y > 0.0){\n        vec3 rp = pos + n*0.02 + rr*rh.x;\n        rc = shade(rp, calcNormal(rp), rr, rh.y, s1, s2, a);\n        rc = mix(rc, skyOf(rr, s1, s2, a.pres, a.voice), 1.0 - exp(-rh.x*rh.x*0.0016));\n      } else {\n        rc = skyOf(rr, s1, s2, a.pres, a.voice);\n      }\n      // Not a perfect mirror: it has a stone floor under the polish.\n      float grain = triNoise(pos*2.2, n, 3.0);\n      vec3 base = mix(vec3(0.055,0.050,0.062), vec3(0.11,0.10,0.12), grain);\n      float fr = fresnel(max(dot(n, -rd), 0.0), 0.05);\n      col = mix(base*0.55, rc, clamp(fr*8.0, 0.05, 0.92));\n      // Long shadows across it, which is what tells you the suns are low.\n      col *= 0.35 + 0.65*softShadow(pos + n*0.02, s1, 0.05, 18.0, 6.0);\n    } else {\n      col = shade(pos, n, rd, h.y, s1, s2, a);\n    }\n    col = mix(col, skyOf(rd, s1, s2, a.pres, a.voice), 1.0 - exp(-h.x*h.x*0.0011));\n  } else {\n    col = skyOf(rd, s1, s2, a.pres, a.voice);\n  }\n\n  // Vocals raise a column of light behind the horizon and align the bodies.\n  float colBeam = exp(-abs(uv.x - 0.06)*(9.0 - a.voice*3.5))*smoothstep(-0.35, 0.55, uv.y);\n  col += vec3(1.00,0.90,0.70)*a.voice*(0.55 + iParam2*0.75)*colBeam*0.85;\n  // Dust in the low light.\n  vec2 dg = floor(uv*200.0 + vec2(t*0.7, -t*0.3));\n  col += step(0.9975 - a.air*0.005, h21(dg))*(0.20 + a.air*1.1)*vec3(1.00,0.94,0.86);\n  col += vec3(1.0)*pu*0.016;\n\n  col = aces(col*0.90);\n  col *= 1.0 - smoothstep(0.60, 1.30, length(uv))*0.34;\n  o = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
  },
];

/** Full, self-contained source for a work: preamble(s) + body. */
export function signatureSource(w: SignatureWork): string {
  return (w.kit3d ? SIGNATURE_KIT + '\n\n' + SIGNATURE_KIT_3D : SIGNATURE_KIT) + '\n\n' + w.body;
}

/** The set a work belongs to, for grouping in a browser. */
export const SIGNATURE_SETS: { id: string; series: string; title: string; blurb: string }[] = [
  {
    "id": "surge",
    "series": "I",
    "title": "Surge",
    "blurb": "Built for a full room. The transient channel strikes, and nothing in these six is allowed to white out on the drop."
  },
  {
    "id": "drift",
    "series": "I",
    "title": "Drift",
    "blurb": "Built to be left running. Nothing moves faster than weather, and the audio changes the light rather than the tempo."
  },
  {
    "id": "combustion",
    "series": "II",
    "title": "Combustion",
    "blurb": "Six works with a source: a woodblock print, a gold ground, an op-art plate, a Bauhaus composition, a leaded window, a loaded brush."
  },
  {
    "id": "vespers",
    "series": "II",
    "title": "Vespers",
    "blurb": "Six quiet ones, and the hardest so far — a colour field and a lit aperture have nowhere to hide behind motion."
  },
  {
    "id": "dilate",
    "series": "III",
    "title": "Dilate",
    "blurb": "Six that come apart. Circle inversions, an infinite regress, a Julia set walked around its own edge, and a frame that will not hold still."
  },
  {
    "id": "numen",
    "series": "III",
    "title": "Numen",
    "blurb": "Six that hold still. A plate of sand answering to the frequency driving it, a tiling of the hyperbolic plane, and the inside of a closed eye."
  },
  {
    "id": "impetus",
    "series": "IV",
    "title": "Impetus",
    "blurb": "Six with something happening to them: a chain reaction, a break shot, a machine under load, a struck drop, a fire, a confined plasma."
  },
  {
    "id": "equilibrium",
    "series": "IV",
    "title": "Equilibrium",
    "blurb": "Six that have settled into a law. A wake, a field, a year, an interference pattern, a correlated pair, and a solid that will not fit in the room."
  },
  {
    "id": "vertigo",
    "series": "V",
    "title": "Vertigo",
    "blurb": "Six flown. Real geometry, raymarched, with a camera on a path that banks by the lateral acceleration of the path itself. These are the expensive ones."
  },
  {
    "id": "stillpoint",
    "series": "V",
    "title": "Stillpoint",
    "blurb": "Six held. A room with one opening, a raked garden, a brass orrery — mostly a locked camera and light doing the work."
  }
];
