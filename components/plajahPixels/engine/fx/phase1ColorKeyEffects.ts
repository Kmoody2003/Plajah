// Phase 1 color, film restoration and matte utilities.
export const PHASE1_COLOR_KEY_EFFECTS: any[] = [
  {
    id: 'skinrefine', name: 'Skin Refine', category: 'color', version: 1,
    summary: 'Hue-aware complexion refinement with natural saturation, warmth and luminance protection.',
    params: [{ key: 'amount', label: 'Amount', min: 0, max: 1, default: .55, step: .01 }, { key: 'warmth', label: 'Warmth', min: -1, max: 1, default: .12, step: .01 }, { key: 'sat', label: 'Skin Saturation', min: .5, max: 1.5, default: 1.02, step: .01 }, { key: 'even', label: 'Tone Evenness', min: 0, max: 1, default: .24, step: .01 }, { key: 'range', label: 'Hue Range', min: .02, max: .3, default: .12, step: .005 }],
    presets: [{ id: 'natural', name: 'Natural Skin', description: 'Subtle correction that respects real complexion variation.', params: { amount: .48, warmth: .08, sat: 1.01, even: .18, range: .11 } }, { id: 'golden', name: 'Golden Complexion', description: 'Warm luminous editorial skin treatment.', params: { amount: .68, warmth: .32, sat: 1.08, even: .3, range: .14 } }, { id: 'neutralize', name: 'Neutralize Cast', description: 'Reduces unwanted cool or green complexion casts.', params: { amount: .72, warmth: .16, sat: .94, even: .34, range: .16 } }],
    glsl: `vec4 fx(vec2 uv){vec4 b=inp(uv);vec3 h=rgb2hsv(b.rgb);float d=abs(h.x-.075);d=min(d,1.0-d);float key=(1.0-smoothstep(P4,P4+.06,d))*smoothstep(.08,.28,h.y)*smoothstep(.08,.95,h.z);vec3 target=hsv2rgb(vec3(fract(h.x-P1*.025),clamp(h.y*P2,0.0,1.0),h.z));vec2 p=2.0/uResolution;vec3 avg=(inp(uv+vec2(p.x,0)).rgb+inp(uv-vec2(p.x,0)).rgb+inp(uv+vec2(0,p.y)).rgb+inp(uv-vec2(0,p.y)).rgb)/4.0;target=mix(target,avg,P3);return vec4(mix(b.rgb,target,key*P0),b.a);}`,
  },
  {
    id: 'autobalance', name: 'Auto Balance', category: 'color', version: 1,
    summary: 'Guided white-balance and exposure correction with protected neutrals and adjustable strength.',
    params: [{ key: 'temperature', label: 'Temperature', min: -1, max: 1, default: 0, step: .01 }, { key: 'tint', label: 'Tint', min: -1, max: 1, default: 0, step: .01 }, { key: 'exposure', label: 'Exposure', min: -2, max: 2, default: 0, step: .01 }, { key: 'neutralProtect', label: 'Neutral Protect', min: 0, max: 1, default: .4, step: .01 }, { key: 'strength', label: 'Strength', min: 0, max: 1, default: 1, step: .01 }],
    presets: [{ id: 'warm-fix', name: 'Fix Warm Cast', description: 'Corrects strong tungsten warmth toward neutral.', params: { temperature: -.42, tint: .02, exposure: .08, neutralProtect: .56, strength: .8 } }, { id: 'green-fix', name: 'Fix Green Cast', description: 'Adds magenta balance for fluorescent or reflected green.', params: { temperature: .02, tint: .38, exposure: 0, neutralProtect: .42, strength: .82 } }, { id: 'cool-fix', name: 'Fix Cool Cast', description: 'Restores warmth to overly blue daylight footage.', params: { temperature: .36, tint: .04, exposure: .06, neutralProtect: .5, strength: .8 } }],
    glsl: `vec4 fx(vec2 uv){vec4 b=inp(uv);vec3 c=b.rgb*exp2(P2);float chrom=max(max(c.r,c.g),c.b)-min(min(c.r,c.g),c.b);float protect=mix(1.0,chrom,P3);vec3 balanced=c*vec3(1.0+P0*.18,1.0+P1*.12,1.0-P0*.18);balanced.g*=1.0+P1*.08;return vec4(mix(b.rgb,balanced,P4*protect),b.a);}`,
  },
  {
    id: 'bleachbypass', name: 'Bleach Bypass', category: 'color', version: 1,
    summary: 'Silver-retention film process with dense contrast, restrained color and highlight control.',
    params: [{ key: 'amount', label: 'Amount', min: 0, max: 1, default: .65, step: .01 }, { key: 'contrast', label: 'Contrast', min: .7, max: 1.8, default: 1.18, step: .01 }, { key: 'sat', label: 'Color Retention', min: 0, max: 1, default: .38, step: .01 }, { key: 'density', label: 'Silver Density', min: 0, max: 1, default: .6, step: .01 }, { key: 'rolloff', label: 'Highlight Rolloff', min: 0, max: 1, default: .52, step: .01 }],
    presets: [{ id: 'classic', name: 'Classic Bypass', description: 'Dense cinematic silver-retention treatment.', params: { amount: .72, contrast: 1.22, sat: .34, density: .68, rolloff: .62 } }, { id: 'war-drama', name: 'War Drama', description: 'Hard low-color documentary drama.', params: { amount: .88, contrast: 1.38, sat: .18, density: .82, rolloff: .48 } }, { id: 'fashion-silver', name: 'Fashion Silver', description: 'Cleaner editorial contrast with retained skin color.', params: { amount: .52, contrast: 1.14, sat: .58, density: .48, rolloff: .72 } }],
    glsl: `vec4 fx(vec2 uv){vec4 b=inp(uv);float l=dot(b.rgb,vec3(.2126,.7152,.0722));vec3 base=mix(vec3(l),b.rgb,P2);vec3 silver=mix(base,base*vec3(l),P3);silver=(silver-.435)*P1+.435;silver=mix(silver,silver/(1.0+silver),smoothstep(.6,1.2,max(max(silver.r,silver.g),silver.b))*P4);return vec4(mix(b.rgb,clamp(silver,0.0,1.0),P0),b.a);}`,
  },
  {
    id: 'dayfornight', name: 'Day for Night', category: 'color', version: 1,
    summary: 'Convincing night conversion with sky suppression, moonlit color and practical-light protection.',
    params: [{ key: 'amount', label: 'Night', min: 0, max: 1, default: .78, step: .01 }, { key: 'exposure', label: 'Exposure', min: -3, max: 0, default: -1.35, step: .01 }, { key: 'blue', label: 'Moonlight', min: 0, max: 1, default: .42, step: .01 }, { key: 'sky', label: 'Sky Control', min: 0, max: 1, default: .58, step: .01 }, { key: 'practicals', label: 'Protect Lights', min: 0, max: 1, default: .72, step: .01 }],
    presets: [{ id: 'moonlit', name: 'Moonlit Blue', description: 'Natural blue-hour night with readable shadows.', params: { amount: .78, exposure: -1.3, blue: .48, sky: .62, practicals: .75 } }, { id: 'urban-night', name: 'Urban Night', description: 'Neutral dark city night with protected lamps.', params: { amount: .82, exposure: -1.55, blue: .22, sky: .5, practicals: .9 } }, { id: 'silver-night', name: 'Silver Night', description: 'Desaturated classic cinema night.', params: { amount: .9, exposure: -1.7, blue: .3, sky: .72, practicals: .58 } }],
    glsl: `vec4 fx(vec2 uv){vec4 b=inp(uv);float l=dot(b.rgb,vec3(.2126,.7152,.0722));vec3 n=b.rgb*exp2(P1);n=mix(n,n*vec3(.62,.76,1.0),P2);float sky=smoothstep(.5,1.0,uv.y)*smoothstep(.32,.85,b.b)*P3;n*=1.0-sky*.35;float hi=smoothstep(.68,1.0,l)*P4;n=mix(n,b.rgb,hi);return vec4(mix(b.rgb,n,P0),b.a);}`,
  },
  {
    id: 'spatialdenoise', name: 'Spatial Denoise', category: 'color', version: 1,
    summary: 'Edge-aware chroma/luma noise reduction for fast preview and single-frame restoration.',
    params: [{ key: 'luma', label: 'Luma Reduction', min: 0, max: 1, default: .32, step: .01 }, { key: 'chroma', label: 'Chroma Reduction', min: 0, max: 1, default: .52, step: .01 }, { key: 'radius', label: 'Radius', min: 1, max: 8, default: 2, step: .5 }, { key: 'edge', label: 'Edge Preserve', min: .01, max: .5, default: .12, step: .01 }, { key: 'detail', label: 'Detail Recovery', min: 0, max: 1, default: .52, step: .01 }],
    presets: [{ id: 'clean-iso', name: 'Clean High ISO', description: 'Balanced luma/chroma cleanup for camera noise.', params: { luma: .42, chroma: .62, radius: 2.5, edge: .1, detail: .55 } }, { id: 'chroma-speckle', name: 'Chroma Speckle', description: 'Targets dancing color noise while preserving luminance.', params: { luma: .14, chroma: .86, radius: 2, edge: .12, detail: .7 } }, { id: 'gentle', name: 'Gentle Cleanup', description: 'Subtle transparent denoise before sharpening.', params: { luma: .2, chroma: .36, radius: 1.5, edge: .16, detail: .72 } }],
    glsl: `vec3 yc(vec3 c){float y=dot(c,vec3(.2126,.7152,.0722));return vec3(y,c.r-y,c.b-y);} vec3 iyc(vec3 v){float r=v.x+v.y,b=v.x+v.z,g=(v.x-.2126*r-.0722*b)/.7152;return vec3(r,g,b);} vec4 fx(vec2 uv){vec2 p=P2/uResolution;vec4 b=inp(uv);vec3 base=yc(b.rgb),sum=base;float w=1.0;for(int i=0;i<8;i++){float a=float(i)*.785398;vec3 s=yc(inp(uv+vec2(cos(a),sin(a))*p).rgb);float rw=exp(-abs(s.x-base.x)/P3);sum+=s*rw;w+=rw;}vec3 avg=sum/w;vec3 outv=vec3(mix(base.x,avg.x,P0),mix(base.y,avg.y,P1),mix(base.z,avg.z,P1));vec3 c=iyc(outv)+(b.rgb-iyc(avg))*P4;return vec4(clamp(c,0.0,1.0),b.a);}`,
  },
  {
    id: 'regrain', name: 'Regrain', category: 'color', version: 1,
    summary: 'Luminance-aware monochrome or color grain that integrates with the image instead of sitting on top.',
    params: [{ key: 'amount', label: 'Amount', min: 0, max: 1, default: .22, step: .01 }, { key: 'size', label: 'Size', min: .4, max: 5, default: 1.2, step: .1 }, { key: 'color', label: 'Color Grain', min: 0, max: 1, default: .18, step: .01 }, { key: 'shadow', label: 'Shadow Grain', min: 0, max: 2, default: 1.1, step: .01 }, { key: 'highlight', label: 'Highlight Grain', min: 0, max: 2, default: .42, step: .01 }],
    presets: [{ id: 'fine-35', name: 'Fine 35mm', description: 'Tight natural finishing grain.', params: { amount: .16, size: .8, color: .12, shadow: 1, highlight: .35 } }, { id: 'organic-16', name: 'Organic 16mm', description: 'Visible textured grain with subtle color variation.', params: { amount: .36, size: 1.7, color: .28, shadow: 1.25, highlight: .55 } }, { id: 'clean-digital', name: 'Clean Digital Texture', description: 'Very fine grain to reduce sterile digital smoothness.', params: { amount: .08, size: .55, color: .06, shadow: .75, highlight: .22 } }],
    glsl: `float h(vec2 p){return fract(sin(dot(p,vec2(12.9898,78.233)))*43758.5453);} vec4 fx(vec2 uv){vec4 b=inp(uv);vec2 q=floor(uv*uResolution/P1)+floor(uTime*24.0);float m=h(q)-.5;vec3 col=vec3(m,h(q+17.0)-.5,h(q+41.0)-.5);vec3 n=mix(vec3(m),col,P2);float l=dot(b.rgb,vec3(.2126,.7152,.0722));float response=mix(P3,P4,smoothstep(0.0,1.0,l));return vec4(clamp(b.rgb+n*P0*response,0.0,1.0),b.a);}`,
  },
  {
    id: 'filmdamage', name: 'Film Damage', category: 'color', version: 1,
    summary: 'Controllable dust, scratches, flicker, weave and exposure damage with deterministic frame behavior.',
    params: [{ key: 'dust', label: 'Dust', min: 0, max: 1, default: .18, step: .01 }, { key: 'scratches', label: 'Scratches', min: 0, max: 1, default: .1, step: .01 }, { key: 'flicker', label: 'Flicker', min: 0, max: .3, default: .025, step: .005 }, { key: 'weave', label: 'Gate Weave', min: 0, max: 12, default: 1.5, step: .1 }, { key: 'age', label: 'Age', min: 0, max: 1, default: .25, step: .01 }],
    presets: [{ id: 'clean-print', name: 'Clean Print', description: 'Subtle living film texture.', params: { dust: .08, scratches: .025, flicker: .008, weave: .6, age: .08 } }, { id: 'aged-reel', name: 'Aged Reel', description: 'Visible dust, scratches and warm density shifts.', params: { dust: .42, scratches: .28, flicker: .06, weave: 2.8, age: .55 } }, { id: 'damaged-archive', name: 'Damaged Archive', description: 'Heavy distressed historical-film treatment.', params: { dust: .75, scratches: .62, flicker: .16, weave: 6, age: .82 } }],
    glsl: `float h(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5);} vec4 fx(vec2 uv){float f=floor(uTime*24.0);vec2 off=vec2((h(vec2(f,2.0))-.5)*P3,(h(vec2(f,7.0))-.5)*P3)/uResolution;vec4 b=inp(uv+off);float dust=step(1.0-P0*.025,h(floor(uv*uResolution/3.0)+f))*smoothstep(.5,0.0,length(fract(uv*uResolution/3.0)-.5));float sx=floor(uv.x*uResolution.x);float scratch=step(1.0-P1*.01,h(vec2(sx,floor(f/5.0))))*step(.85,h(vec2(uv.y*40.0,f)));float fl=1.0+(h(vec2(f,9.0))-.5)*P2;vec3 age=mix(b.rgb,b.rgb*vec3(1.08,.96,.78),P4);age=age*fl+dust*.7-scratch*.5;return vec4(clamp(age,0.0,1.0),b.a);}`,
  },
  {
    id: 'spillkiller', name: 'Spill Killer', category: 'utility', version: 1,
    summary: 'Targeted green/blue spill removal with skin-safe replacement color and luminance preservation.',
    params: [{ key: 'hue', label: 'Spill Hue', min: 0, max: 1, default: .333, step: .001 }, { key: 'range', label: 'Range', min: .01, max: .4, default: .14, step: .005 }, { key: 'amount', label: 'Amount', min: 0, max: 1, default: .75, step: .01 }, { key: 'balance', label: 'Replacement Warmth', min: -1, max: 1, default: .1, step: .01 }, { key: 'satMin', label: 'Minimum Saturation', min: 0, max: 1, default: .12, step: .01 }],
    presets: [{ id: 'green', name: 'Green Spill', description: 'Natural green-screen spill suppression.', params: { hue: .333, range: .14, amount: .78, balance: .14, satMin: .12 } }, { id: 'blue', name: 'Blue Spill', description: 'Blue-screen edge contamination cleanup.', params: { hue: .61, range: .13, amount: .72, balance: .22, satMin: .1 } }, { id: 'strong-green', name: 'Strong Green Spill', description: 'Aggressive cleanup for reflective wardrobe and edges.', params: { hue: .333, range: .22, amount: .94, balance: .18, satMin: .06 } }],
    glsl: `vec4 fx(vec2 uv){vec4 b=inp(uv);vec3 h=rgb2hsv(b.rgb);float d=abs(h.x-P0);d=min(d,1.0-d);float key=(1.0-smoothstep(P1,P1+.08,d))*smoothstep(P4,P4+.12,h.y);float l=dot(b.rgb,vec3(.2126,.7152,.0722));vec3 repl=mix(vec3(l),vec3(l*1.05,l*.98,l*.9),P3*.5+.5);return vec4(mix(b.rgb,repl,key*P2),b.a);}`,
  },
  {
    id: 'mattetools', name: 'Matte Refine', category: 'utility', version: 1,
    summary: 'Alpha choke/expand, softness and edge recovery in one compositing-safe matte utility.',
    params: [{ key: 'choke', label: 'Choke / Expand', min: -1, max: 1, default: 0, step: .01 }, { key: 'soft', label: 'Softness', min: 0, max: 20, default: 1.5, step: .1 }, { key: 'contrast', label: 'Matte Contrast', min: .2, max: 3, default: 1, step: .01 }, { key: 'edge', label: 'Edge Recover', min: 0, max: 1, default: .3, step: .01 }],
    presets: [{ id: 'clean-edge', name: 'Clean Edge', description: 'Gentle choke and feather for chroma keys.', params: { choke: .08, soft: 1.5, contrast: 1.18, edge: .42 } }, { id: 'hair-soft', name: 'Hair Soft', description: 'Preserves translucent fine edges.', params: { choke: -.04, soft: 2.8, contrast: .82, edge: .72 } }, { id: 'hard-core', name: 'Hard Core', description: 'Tight solid matte for graphics and garbage mattes.', params: { choke: .18, soft: .5, contrast: 1.8, edge: .08 } }],
    glsl: `vec4 fx(vec2 uv){vec2 p=P1/uResolution;vec4 b=inp(uv);float a=(inp(uv+vec2(p.x,0)).a+inp(uv-vec2(p.x,0)).a+inp(uv+vec2(0,p.y)).a+inp(uv-vec2(0,p.y)).a+b.a*4.0)/8.0;a=clamp((a-.5-P0*.2)*P2+.5,0.0,1.0);a=mix(a,b.a,P3*smoothstep(.05,.95,b.a));return vec4(b.rgb,a);}`,
  },
  {
    id: 'colorspacetransform', name: 'Color Space Transform', category: 'color', version: 1,
    summary: 'Native log/linear/display transforms with gamut compression and tone mapping boundaries.',
    params: [{ key: 'input', label: 'Input Curve', min: 0, max: 4, default: 0, step: 1 }, { key: 'output', label: 'Output Curve', min: 0, max: 3, default: 0, step: 1 }, { key: 'exposure', label: 'Exposure', min: -4, max: 4, default: 0, step: .01 }, { key: 'gamut', label: 'Gamut Compress', min: 0, max: 1, default: .5, step: .01 }, { key: 'tone', label: 'Tone Map', min: 0, max: 1, default: .6, step: .01 }],
    presets: [{ id: 'log3-rec709', name: 'Log3 → Rec.709', description: 'General modern camera-log normalization.', params: { input: 1, output: 0, exposure: 0, gamut: .55, tone: .68 } }, { id: 'logc-rec709', name: 'LogC → Rec.709', description: 'ARRI-style log normalization starting point.', params: { input: 2, output: 0, exposure: 0, gamut: .52, tone: .62 } }, { id: 'aces-display', name: 'ACES-like Display', description: 'Wide-gamut scene-linear to display rendering.', params: { input: 3, output: 0, exposure: 0, gamut: .72, tone: .78 } }],
    glsl: `vec3 decode(vec3 c,float m){if(m<.5)return pow(max(c,0.0),vec3(2.2));if(m<1.5)return max((pow(vec3(10.0),(c-.42)/.22)-.01),0.0);if(m<2.5)return max((pow(vec3(10.0),(c-.39)/.25)-.012),0.0);return max(c,0.0);} vec4 fx(vec2 uv){vec4 b=inp(uv);vec3 c=decode(b.rgb,P0)*exp2(P2);float mx=max(max(c.r,c.g),c.b);c=mix(c,c/(1.0+max(c-1.0,0.0)),P3*smoothstep(1.0,4.0,mx));c=mix(c,c/(1.0+c),P4);c=pow(max(c,0.0),vec3(1.0/2.2));return vec4(clamp(c,0.0,1.0),b.a);}`,
  },
];
