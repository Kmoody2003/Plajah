import http from 'node:http'; import fs from 'node:fs'; import path from 'node:path';
http.createServer((req,res)=>{const root=path.resolve('dist-firstlight');const file=path.resolve(root,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!file.startsWith(root+path.sep)){res.writeHead(403).end();return;}try{res.setHeader('Content-Type',file.endsWith('.js')?'text/javascript':file.endsWith('.css')?'text/css':'text/html');res.end(fs.readFileSync(file));}catch{res.writeHead(404).end();}}).listen(3101,'127.0.0.1',()=>console.log('Firstlight static preview ready'));
